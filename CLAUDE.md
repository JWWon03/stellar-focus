# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

집중/공부 타이머 + 항성계 수집 게임. 세션을 완주하면 가챠 토큰을 얻고, 뽑은 천체를 궤도에 배치해 자기만의 항성계를 키운다. 사용자 대면 규칙 요약은 `README.md`, 구조 설명 문서는 `docs.html` 참고.

| 파일 | 내용 |
|---|---|
| `index.html` | 앱 전체 (CSS·JS 포함, 이것 하나로 동작) |
| `supabase/schema.sql` | 계정/클라우드 저장용 Postgres 스키마 + RPC 함수 |
| `docs.html` | 처음 보는 사람을 위한 구조 설명 문서 |
| `README.md` | 규칙 요약, 모바일 앱 래핑 방법 |
| `textures/` | 실제 관측 텍스처 13종 + 토성 고리 + 출처·라이선스 문서 |
| `manifest.webmanifest` | PWA 매니페스트 (홈 화면 설치·standalone) |
| `sw.js` | 서비스워커 — 오프라인 캐시 + 알림 통로 |
| `icons/` | 설치 아이콘 (192·512·maskable·apple-touch) |
| `mobile/` | 안드로이드 래퍼 (Capacitor). 빌드 방법은 `mobile/README.md` |

## 명령

빌드·번들러·테스트·의존성이 **없다.**

```bash
start index.html
```

배포는 GitHub Pages. `main`에 push하면 1분 내로 https://jwwon03.github.io/stellar-focus/ 에 자동 반영된다.

```bash
git add -A && git commit -m "설명" && git push
```

배포본이 로컬과 같은지 확인 (PowerShell):

```powershell
$r = Invoke-WebRequest "https://jwwon03.github.io/stellar-focus/" -UseBasicParsing; $r.RawContentStream.Position = 0; (Get-FileHash -InputStream $r.RawContentStream -Algorithm SHA256).Hash -eq (Get-FileHash "index.html" -Algorithm SHA256).Hash
```

전역 `S`(상태)와 `startPhase` / `pull` / `renderSystem` / `renderGacha` / `sceneBodies` 등이 window에 노출돼 있어, 콘솔에서 상태를 심어 특정 화면을 바로 띄울 수 있다. 수동 테스트 시 유용하다. 단 **가짜 토큰으로 `AUTH`를 심으면 안 된다** — 클라우드 푸시가 인증 실패를 받아 자동 로그아웃되면서 상태가 초기화된다. 클라우드를 건드리지 않으려면 `cloudPush`를 빈 함수로 덮어쓸 것.

RPC를 직접 두드려 서버만 확인할 수도 있다:

```powershell
Invoke-RestMethod -Uri 'https://bssawuumjkxvzyrmvscd.supabase.co/rest/v1/rpc/sf_login' -Method Post -Headers @{apikey=$key} -ContentType 'application/json' -Body '{"p_id":"__probe__","p_pin":"0000"}'
```

## 외부 리소스 제약

CDN 스크립트와 웹폰트를 **일절 추가하지 않는다.** 궤도 화면의 모든 그래픽은 캔버스에 절차적으로 그린다. 오프라인 동작과 "파일 하나 더블클릭으로 실행"이 이 프로젝트의 핵심 성질이고, 나중에 Capacitor로 모바일 앱 래핑할 때의 전제이기도 하다.

예외가 둘 있고, 둘 다 **없어도 앱이 동작하도록** 만들어져 있다.

`textures/`의 이미지는 도감 구체 뷰어에서만 쓰이며 **그 천체를 열 때 지연 로드**된다. 파일이 없거나 로드에 실패하면 조용히 절차적 렌더링으로 넘어가므로, index.html 하나만 있어도 앱은 정상 동작한다. CC BY 4.0 자료이므로 `textures/ATTRIBUTION.md`와 도감 화면의 출처 표기를 지울 것.

또 하나의 예외가 **선택 기능인 클라우드 동기화**다. 계정 기능은 Supabase REST를 `fetch`로 직접 부르지만 라이브러리는 쓰지 않으며, `CLOUD` 상수가 비어 있거나 서버가 죽어도 앱은 오프라인으로 완전히 동작해야 한다.

## 구조

`index.html`의 `<script>`는 번호가 붙은 섹션으로 나뉜다 (`/* === N. 이름 === */`). 파일이 크므로 수정 전 해당 섹션 주석으로 위치를 잡을 것.

0 유틸 · 1 천체 데이터 풀 · 2 상태/저장 · 3 천체 렌더러 · 4 메인 캔버스 · 5 타이머 · 6 모달 · 7 인트로 · 8 가챠 · 9 항성계 설정 · 10 세션 완료 · 11 위젯 바인딩 · 13 계정/클라우드 · 12 부팅

(13이 12보다 앞에 있다. 부팅이 마지막에 오도록 나중에 끼워 넣은 결과다.)

## 알아야 할 불변조건

여러 곳에 흩어져 있어 한 파일만 읽어선 안 보이는 것들.

**색 헬퍼는 반드시 hex를 반환한다.** `shade` / `lighten` / `mix`의 반환값이 다시 `hx()` / `rgba()`로 들어가기 때문에, `rgb(...)` 문자열을 돌려주면 파싱이 깨져 캔버스가 조용히 잘못 그려진다. 새 색 헬퍼를 만들 때도 `toHex()`를 거칠 것.

**알파를 두 번 곱하지 말 것.** `strokeStyle`에 이미 알파가 든 `rgba(...)`를 넣고 `globalAlpha`까지 걸면 실효 알파가 곱해져 선이 사라진다. 동반성 궤도가 안 보이던 원인이 이것이었다. 알파를 인자로 받는 그리기 함수에는 불투명 색을 넘긴다.

**작은 캔버스에 천체를 그릴 때 헤일로가 잘린다.** 항성 코로나는 기본 반경의 6.6배, 발광체 글로우는 2.5배, 고리는 2.55배까지 뻗는다. 캔버스 밖으로 나가면 알파가 0이 아닌 지점에서 잘려 **사각형 테두리**가 보인다. `drawStar`의 `extra.haloK`로 코로나를 줄이고, 본체 반경은 캔버스 절반 나누기 배율 이하로 잡는다. 썸네일은 `paintThumb`, 미리보기는 `paintPreview`에 이미 보정값이 들어 있다.

**궤도 물리.** `phase`는 "기준 궤도의 누적 회전수"다. 기준 궤도 = 행성이 배치된 가장 바깥 슬롯. 집중 세션 동안 기준 궤도가 정확히 1바퀴를 돌고(`phase = _startPhase + 진행률`), 안쪽 행성은 케플러 제3법칙 `revs = phase * (a_ref/a)^1.5`로 더 빠르게 돈다. 대기 중에는 240초에 1바퀴로 천천히 표류. 이 "타이머 시간 = 1바퀴 공전"은 제품의 핵심 컨셉이므로 바꾸지 말 것.

궤도는 원이 아니라 **타원**이다. 슬롯마다 장반경·이심률·경사·승교점·근점이 `orbitEl(i)`에 결정론적으로 고정돼 있고, `orbitPos()`가 케플러 방정식을 뉴턴법으로 풀어 위치를 낸다(제1·2법칙 — 근점에서 빨라진다). 궤도 경로·꼬리는 모두 이 함수를 샘플링해 그리므로, 궤도 모양을 바꾸려면 `orbitEl`만 손대면 된다.

**카메라 `VIEW`.** `tilt`(0=face-on, π/2=edge-on), `az`(방위각), `zoom`, `ox/oy`(추적 오프셋), `follow`(추적 대상 key). `project()`가 az로 z축 둘레를 돌린 뒤 tilt로 눕히고 `depth`를 함께 돌려주며, 그리기 순서는 이 depth로 정렬한다(화면 y 정렬이 아니다). 매 프레임 지수 보간으로 부드럽게 따라간다. `fitZoom()`은 **현재 tilt를 반영**해 배율을 계산한다 — edge-on일 때 세로가 눌리므로 그만큼 더 당겨야 화면이 찬다.

드래그 회전의 부호는 "손으로 붙잡고 돌리는" 감각에 맞춰져 있다(`azTo -= dx`, `tiltTo -= dy`). 바꾸기 전에 화면 좌표로 방향을 확인할 것 — 처음 구현 때 두 축 모두 반대로 넣었다가 고쳤다.

**항성 질량중심.** `starPositions()`가 실제 쌍성처럼 공통 질량중심을 원점에 두고 각 별의 위치를 낸다. 동반성이 둘이면 **계층적 삼중성**(안쪽 쌍성 + 그 질량중심을 도는 바깥 동반성)으로 푼다 — 실제 삼중성계가 안정적으로 존재하는 방식이다. 질량은 `PALETTE[].m`(중심별, 분광형 기준)과 `STAR_POOL[].m`에서 온다. 궤도선은 각 별이 **자기가 속한 질량중심** 둘레를 돌도록 그려야 한다(원점 기준으로 그리면 안쪽 쌍성이 어긋난다). **검증법**: 모든 별의 질량가중 위치 평균이 (0,0)이어야 한다.

**타이머는 절대 시각(`endAt`) 기준이다.** 남은 시간을 감산하지 않고 매번 `endAt - Date.now()`로 계산한다. 덕분에 탭이 백그라운드로 내려가거나 앱이 절전에 들어가도 어긋나지 않는다. 부팅 시 이미 만료된 세션은 즉시 완료 처리하고 토큰을 지급한다. 이 방식을 감산 방식으로 되돌리지 말 것.

**집중 중 밝기 / 리빌.** 처음에는 집중 중 행성·위성이 완전한 검은 구체(밝기 0)였다. 흐림·모눈이 깔리면서 아예 안 보이게 되어 **`FOCUS_LIT`(0.62)** 로 바꿨다 — 색과 표면은 보이되 완주 후보다는 어둡다. **중심별은 예외로 항상 밝다** — 사용자가 명시적으로 요청한 사항이니 되돌리지 말 것. 완료 시 `reveal` 웨이브가 중심에서 퍼지며 `litAt(a)`가 0.62 → 1로 올린다. 휴식 중에는 전부 밝다.

**집중 중 잠금.** `isLocked()`가 참이면 가챠·항성계 설정·모드·시간 입력이 모두 막히고 [종료] 버튼만 남는다. 차단은 `disabled` 속성이 아니라 각 핸들러의 `guard()`가 한다 — `disabled`면 클릭 이벤트가 오지 않아 안내 문구를 띄울 수 없기 때문이다. 새 메뉴를 추가하면 핸들러 첫 줄에 `if(guard())return;`을 넣을 것.

**집중 중에는 시점이 Face-on 으로 완전히 잠긴다.** 세션을 시작하면 `tiltTo=TILT_FACE, azTo=0`으로 맞추고, 드래그(`pointermove`)·Face-on/Edge-on 버튼(`viewLocked()`)·탭 추적(`endDrag`)이 모두 막힌다. 관측 장비 화면이라 내려다보는 시점을 유지해야 한다. **확대축소만 열려 있다** — 이건 의도적이다.

**완료 팝업은 모달이 아니다.** `showTokenPopup()`은 7초 뒤 자동으로 닫히며 그 동안 타이머가 계속 흐른다(포모도로는 휴식이 즉시 시작된다). 토큰은 팝업과 무관하게 이미 지급돼 있고 확인 버튼은 조기 닫기일 뿐이다. 이걸 블로킹 모달로 되돌리면 요구사항이 깨진다.

**`confirm()` / `alert()` 를 쓰지 말 것.** 홈 화면에 추가한 PWA나 일부 모바일 웹뷰에서 조용히 차단되어 `false`를 돌려준다 — 버튼이 먹통으로 보이는 원인이다. 확인이 필요하면 `await ask(제목, 설명, 버튼라벨, danger)`를 쓴다. 확인창은 다른 모달 위에 겹치므로 z-index가 더 높고 배경 막을 옅게 깐다.

**회전한 요소는 클릭 영역도 회전한다.** 다이얼의 진행률 SVG가 `rotate(-90deg)` 되어 있어 히트 영역이 원래 박스 밖으로 삐져나와 위쪽 입력창을 덮었다. 장식용 요소에 `transform`을 걸면 `pointer-events:none`을 함께 줄 것.

**가챠 연출 중에는 `pulling`이 참이다.** 이때 뽑기 버튼이 잠기고 건너뛰기 버튼만 뜬다. 새 연출을 시작할 때 `cancelAnimationFrame(_warpRaf)`로 이전 프레임 루프를 반드시 끊어야 한다 — 안 끊으면 연타 시 여러 루프가 겹쳐 돌며 결과가 즉시 튀어나온다.

**전체 초기화는 클라우드까지 지워야 한다.** 로컬만 지우면 로그인 상태에서 다음 부팅에 `cloudSyncOnBoot()`가 되살려 초기화가 안 된 것처럼 보인다. 빈 상태를 만든 뒤 `cloudPush(true)`로 덮어쓴다.

**계정은 선택 기능이다.** `CLOUD.url`/`CLOUD.key`가 비어 있으면 계정 메뉴가 "미설정"으로 뜨고 앱은 완전히 오프라인으로 돈다. 이 성질을 깨지 말 것 — 서버가 죽어도 게임은 돌아가야 한다. 로그인하면 저장 키가 `stellarFocus.v1.<id>`로 갈라져 한 기기에서도 계정별 항성계가 분리된다. 서버 접근은 `supabase/schema.sql`의 RPC 함수로만 하며 테이블은 RLS로 잠겨 있다(정책을 만들지 않는 것이 곧 차단이다). publishable 키는 공개되어도 되는 값이다. 로컬과 클라우드에 서로 다른 데이터가 있으면 `adoptCloud()`가 사용자에게 선택을 받는다 — 조용히 덮어쓰지 말 것.

**비교는 `canon()`으로 표준형을 만든 뒤 한다.** 서버에 있는 데이터는 예전 스키마라 필드가 없거나 키 순서가 다를 수 있다. 날것끼리 `JSON.stringify` 로 비교하면 늘 "다르다"가 나와 **부팅할 때마다 동기화 창이 뜬다** — `star.namePos` 를 추가했을 때 실제로 그랬다. `canon()`이 키를 정렬하고 없는 필드와 `null` 을 같게 보며, 보유 목록은 정렬해 순서를 지운다. **`S` 의 지속 필드를 추가·변경할 때 이 지문이 여전히 성립하는지 확인할 것.**

**기록(`stats`)은 정체성에서 뺀다.** 기록이 조금 어긋났다고 "어느 항성계를 쓸까요?" 를 띄우는 건 과하다. 지문이 같으면 `mergeStats()`로 큰 쪽을 살려 합치고 조용히 올린다. 타이머 런타임 상태도 제외한다(기기마다 다르므로).

**동기화 창은 "정말 갈라졌을 때"만 떠야 한다.** 판단 순서가 정해져 있다.

1. 지문이 같으면 → 기록만 합치고 끝.
2. `updatedAt`(로컬 저장마다 갱신)과 기기 로컬의 `syncedAt`(마지막 성공 시각)을 비교해, 한쪽에만 새 변경이 있으면 그쪽을 조용히 채택한다. 한 기기만 쓰면 여기서 끝난다.
3. 시각을 못 믿으면(옛 기록엔 `updatedAt` 이 없다) `dominates()`로 진행도를 본다. 보유 천체 수와 세션 수는 되돌아가지 않는 값이라, 한쪽이 둘 다 크거나 같고 하나라도 앞서면 그쪽이 뒤쪽을 포함한다 — 잃을 게 없으니 물어볼 이유도 없다. **토큰은 뽑으면 줄어서 진행도가 못 된다.**
4. 그래도 못 정하면 그때만 `adoptCloud()`가 묻는다. 창에 `diffFields()`로 **무엇이 달라서 묻는지** 표시한다.

`adoptCloud`의 두 갈래는 **둘 다 푸시해야 한다.** 클라우드를 골랐을 때 올리지 않으면 서버엔 옛 스키마가 그대로 남아 다음 부팅에 또 어긋난다.

**푸시는 4초 디바운스라 앱을 나가면 날아간다.** 모바일은 화면을 벗어나는 순간 프로세스가 얼어붙어 예약된 `setTimeout` 이 실행되지 않는다. 클라우드가 뒤처지면 다음 부팅에 동기화 창이 뜬다. `flushPush()`가 `visibilitychange`(document 에서 난다)와 `pagehide` 에서 밀린 것을 즉시 밀어 넣는다.

**관리자 계정.** `ADMIN_IDS`에 든 아이디로 로그인하면 토큰 무제한(`∞` 표시), 집중 시간 1분부터 설정 가능(`limMin()`), 천체 종류별 도감 초기화가 열린다. **클라이언트 판정이라 진짜 권한 통제가 아니다** — 테스트 편의 기능이다. 도감을 초기화할 때는 보유 목록만 지우지 말고 궤도 배치도 함께 해제할 것(`resetOneDex`). 안 그러면 유령 천체가 남는다.

**localStorage 는 접근만 해도 예외를 던질 수 있다.** 시크릿 모드나 저장소 차단 환경이 그렇다. 반드시 `lsGet` / `lsSet` / `lsDel` 래퍼로만 접근한다.

**저장 스키마.** 키 하나에 전체 상태를 넣는다. `load()`가 방어적 복구를 한다 — 존재하지 않는 천체 id 제거, 슬롯 배열 길이 보정, 행성 없는 슬롯의 위성 제거. **풀에서 id를 바꾸거나 지우면, 그리고 슬롯 구조를 바꾸면 기존 사용자 데이터가 깨진다.** `load()`의 복구 로직을 함께 갱신하거나 키 버전을 올릴 것.

**반응형 분기점 760px이 CSS와 JS 양쪽에 있다.** `geom()`이 `W < 760`일 때 항성계 중심을 위로(`H*0.32`) 올리고 반경을 줄여 좌하단 타이머 위젯을 피한다. CSS `@media (max-width:760px)`도 같은 값이다. 한쪽만 바꾸면 폰에서 위젯이 중심별을 덮는다.

**설정 화면의 설명은 ⓘ 로 접는다.** 행마다 설명을 깔면 문구가 화면을 채워 정작 토글이 안 보인다. `INFO[키]` 에 HTML 을 담고 `<button class="iBtn" data-info="키">` 를 제목 옆에 두면 `bindInfo(root)` 가 연결한다. `SETTINGS` 의 `d` 가 그대로 설명이므로 문구는 한 군데에만 있다.

- 풍선(`#infoPop`)은 `position:fixed` 라 **모달 본문이 스크롤돼도 따라오지 않는다.** 스크롤·리사이즈·바깥 누름에서 닫는다. 스크롤 감시는 **캡처 단계**(`addEventListener('scroll',…,true)`)로 걸 것 — 모달 본문의 스크롤 이벤트는 window 까지 올라오지 않는다.
- ⓘ 는 설정 행 안에 있으므로 클릭에서 **`stopPropagation` 을 반드시 할 것.** 안 하면 설명을 보려다 설정이 토글된다.
- 열기 전에 `left/top` 을 0 으로 되돌리고 재야 폭이 제대로 나온다. 이전 위치가 화면 끝이면 잘못 잰다.
- 상태 문구는 접지 않는다 — `완료 알림` 행의 "차단됨 · 브라우저 사이트 설정에서 풀어야 합니다" 는 설명이 아니라 지금 상태다.

**설정 화면의 행 핸들러는 `[data-k]` 로 좁혀 잡는다.** `renderSettings()`가 `$$('#setBody .setRow')` 를 통째로 훑으면 알림 행(`#rowNotif`)까지 잡혀 `renderPwaRows()`가 건 핸들러를 덮어쓴다. 그러면 권한 요청은 일어나지 않고 `S[undefined]` 만 토글되어, **켠 것처럼 보였다가 다시 들어가면 꺼져 있다.** 실제로 그 버그가 있었다. 설정에 특수한 행을 추가할 때 이 규칙을 지킬 것.

**`ask()` 는 밖에서 취소할 수 있어야 한다.** 버튼으로만 풀리게 두면 뒤로가기 등으로 창이 닫혔을 때 약속이 영원히 안 풀리고, `onBack` 의 `_exiting` 이 참으로 굳어 **그때부터 뒤로가기가 통째로 죽는다.** `_askRes` 로 취소 통로를 남기고 `_exiting` 은 `finally` 로 푼다. 확인창이 떠 있는 동안에도 덫을 깔아 둬야 그 사이 뒤로가기가 앱 밖으로 나가지 않는다.

**뒤로가기가 안 먹는다는 보고를 받으면 설정 맨 아래 진단줄부터 확인할 것.** `버전 · 뒤로가기 준비됨/미설정 (히스토리 길이) · 앱/홈화면/브라우저` 가 표시된다. "미설정"이면 덫이 사라진 것이고, "준비됨"인데도 반응이 없으면 그 환경이 `popstate` 를 안 주는 것이다. 참고로 **열린 창이 있으면 뒤로가기는 그 창부터 닫는다** — 동기화 창처럼 뭔가 떠 있었다면 첫 번째 뒤로가기는 종료를 묻지 않는다.

**덫은 반드시 사용자 조작 중에 깔아야 한다.** Chrome 은 사용자 조작 없이 `pushState` 로 만든 항목을 **건너뛸 수 있는 것으로 표시한다**(history manipulation intervention — 뒤로가기를 가두는 사이트를 막으려는 장치). 부팅하자마자 깔면 뒤로가기가 그 항목을 건너뛰어 **앱이 그대로 닫힌다.** 실제로 그래서 안 먹었다. `pointerdown`/`touchstart`/`keydown`/`click` 에서만 깐다. **`pageshow` 나 `visibilitychange` 로 깔면 안 된다** — 조작 없이 만든 항목이 하나라도 생기면 `trapArmed()` 가 참이 되어, 정작 조작 때 제대로 된 항목을 못 깐다.

**뒤로가기는 히스토리 덫으로 받는다.** 부팅 때 `pushTrap()`이 항목을 하나 밀어 넣고 `popstate`에서 `onBack()`이 받는다. 열린 모달이 있으면 그것부터 닫고, 촬영 모드면 빠져나오고, 아무것도 없을 때만 `ask()`로 종료를 묻는다. **처리할 때마다 덫을 다시 깔아야** 다음 뒤로가기도 받을 수 있다. `pushTrap()`은 이미 깔려 있으면 겹쳐 쌓지 않고(`trapArmed()`), `visibilitychange`·`focus`·`pageshow` 에서도 다시 깐다 — 앱을 오래 떠나 있으면 브라우저가 히스토리를 정리하는 경우가 있다. Capacitor 가 있으면 `App.backButton` 도 같은 `onBack()` 으로 보낸다. 종료를 고르면 `exitApp()`이 밀린 동기화를 즉시 밀어 넣은 뒤 Capacitor `App.exitApp()`을 시도한다 — 브라우저는 스크립트로 창을 닫지 못하는 경우가 많아 그때는 안내만 한다.

**`APP_VER`은 설정 화면 맨 아래에 표시된다.** 사용자가 어느 빌드를 쓰는지 확인하는 유일한 수단이다. 배포했는데 증상이 그대로라는 보고를 받으면 먼저 이 값을 물어볼 것 — 서비스워커나 HTTP 캐시 때문에 옛 파일을 쓰고 있을 수 있다. **동작을 바꾸는 배포에서는 이 값을 올릴 것.**

**HUD 4모서리 배치는 제품 요구사항이다.** 사용자가 직접 스케치한 배치이므로 임의로 옮기지 말 것.

- 좌상단: 날짜/시각 위젯 (계정 버튼이 그 안에 들어 있다)
- 우상단: 토큰 수 + 세로 메뉴 스택 — 가챠 · 항성계 · 도감 · 카메라 · 설정
- 좌하단: 타이머 위젯 (모드는 탭, 오른쪽에 사운드/접기)
- 우하단: Face-on · Edge-on · 확대축소

메뉴 접기 버튼은 메뉴 스택과 **가로 폭이 같은 납작한 알약**이고, 접혀 있으면 아래쪽(펼치기) 펼치면 위쪽(접기)을 가리킨다. 타이머 위젯을 접으면 **남은 시간과 펼치기 버튼만** 남는다(`.tMini`) — 모드 탭까지 남기면 접은 의미가 없다. 우하단 버튼은 좁은 화면에서 타이머 위젯과 겹치지 않는 크기여야 한다(Face-on 86×29, 줌 27×24).

**집중 시간은 다이얼 가운데를 눌러 고친다.** 예전에는 위에 별도 입력 행이 있고 아래 다이얼에 같은 값이 또 보여 헷갈렸다. **평소에는 `25:00` 표시 그대로이고, 누르면 그 자리가 입력창(`.bigIn`)으로 바뀐다**(`_editing`). 빠져나오면 다시 표시로 돌아간다 — `blur` 만 믿지 말고 `change` 에서도 닫을 것, 환경에 따라 `blur` 가 안 오는 경우가 있다. 포모도로는 설정 항목이 넷이라 예전처럼 본문에 둔다. 최소값 안내는 시작 버튼 바로 위(`#tHint`)에 있다.

**집중 중 전체 보기(`#vbFit`)는 배율만 맞춘다.** `resetView()`는 기울기·방위각까지 되돌려 Face-on 잠금을 깨고 화면이 돌아간다.

**글자 아이콘(`⤢` 등)을 쓰지 말 것.** 폰트 대체에 따라 기기마다 모양이 달라진다 — 모바일에서 전체 보기 아이콘이 다르게 보인다는 보고가 그 원인이었다. 인라인 SVG 로 고정한다.

**도감은 최상위 메뉴다.** 예전에는 가챠 창 안의 토글이라 사용자가 찾지 못했다. 다시 숨기지 말 것.

**텍스처 굽기는 절대 통짜로 돌리지 말 것.** 절차적 텍스처 한 장이 `PROC_W*PROC_H`(512×256) 픽셀짜리 동기 루프다. 그대로 돌리면 그 시간 내내 프레임이 통째로 멈춘다 — 폰에서 천체 하나당 0.5초 이상이고, 항성계가 다 차면 그런 게 10번 넘게 온다. `bakeSlice()`가 매 프레임 `BAKE_MS`(5ms)만큼 위도 줄만 채우고 넘긴다. 총 시간은 같지만 화면이 계속 돈다.

- 새 코드에서 `makeProcTexture()`(통짜)를 직접 부르지 말 것. `readyTexture(b, 화면반지름)`로 예약하고 준비될 때까지 null 을 받아 넘어가야 한다. 실제로 `pumpSprites()`가 이걸 어겨서 시간 분할을 우회하고 있었다 — 썸네일 하나 구울 때마다 200ms씩 멈췄다.
- 큐는 `_texPri`(화면상 반지름)로 정렬한다. 중심별이 절차적인데 위성부터 구우면 눈에 띈다.
- **텍스처는 쓰일 크기별로 굽는다.** `PROC_TIERS`(160×80 / 320×160 / 512×256)에서 `procTier(px)`가 고르고, 캐시 키는 `id@단계`다. 경도 180도가 화면 px 만큼의 텍셀로 덮이면 1:1 → 폭 ≈ 지름×2. 74px 썸네일에 512폭을 구우면 16배를 버리는 셈이고, 그게 도감이 느리게 채워지던 이유였다. `readyTexture(b, 화면지름)`에 **반지름이 아니라 지름**을 넘길 것.
- 요청한 단계가 아직이면 `bestProc()`이 이미 구워둔 더 작은 단계를 내준다. 빈 화면 대신 흐린 그림으로 버티다 올라간다.
- **모달은 전면을 덮고 blur(10px)까지 걸린다.** 그 뒤 궤도를 60fps로 그릴 이유가 없어 세 프레임에 한 번만 그리고(`hidden()`), 굽기 예산을 5ms→13ms로 올린다. 도감 채우는 시간이 2.6초에서 0.47초로 줄었다.
- 스프라이트 큐(`spriteSlice`)도 프레임 예산 안에서 여러 장씩 처리한다. 한 장마다 `setTimeout`을 걸면 타이머 지연만으로 몇 초가 샌다. 텍스처가 아직이면 예약만 하고 뒤로 미루되, 큐를 한 바퀴 다 돌아도 진전이 없을 때만 한 프레임 쉰다 — 고정 지연을 주면 그게 직렬로 쌓인다.
- 성능을 의심할 땐 프레임 간격 히스토그램을 볼 것. 중앙값은 멀쩡한데 33ms 넘는 프레임 몇 개가 버벅임의 정체인 경우가 많다.

**촬영 모드(`body.capture`).** 카메라 버튼을 누르면 HUD가 사라지고 우하단에 **설정 · 촬영 · 나가기**만 남는다. 설정을 누르면 `#capPanel`이 열리고 그 안에 비율 키(전체/1:1/4:3/3:4/9:16/16:9) · 시점(Face-on·Edge-on·전체 보기) · 배치(일렬 정렬) · 표시 토글(별 이름·궤도선)이 들어 있다.

- `capRatio`가 `full`이 아니면 `captureRect()`가 잘릴 영역을 계산하고 `drawCaptureGuide()`가 바깥을 어둡게 덮고 삼분할선을 그린다. **가이드는 `render()` 맨 끝에서 그린다** — 천체보다 먼저 그리면 천체가 그 위에 얹혀 틀이 안 보인다.
- 촬영은 `_hideGuide=true`로 두고 `render(clock)`을 한 번 더 돌린 뒤 `drawImage`로 잘라낸다. 이 플래그를 빼먹으면 삼분할선이 사진에 찍힌다.
- 궤도선 토글은 `globalAlpha=0`이 아니라 **블록 자체를 건너뛴다**. `strokeStarRing`이 내부에서 `globalAlpha`를 덮어써서 동반성 궤도만 살아남기 때문이다.
- `fitZoom()`은 촬영 중이면 화면이 아니라 `captureRect()` 안에 맞춘다. 세로 비율에서 계가 틀 밖으로 삐져나가면 안 된다.
- 촬영 모드를 나가면 `showLabels`/`showOrbits`/`capRatio`를 되돌린다. 촬영용 토글이 평소 화면까지 따라가면 궤도선이 사라진 채로 남는다.
- `syncCapUI()`는 `updateViewUI()`에서 불린다(드래그로 시점이 바뀌어도 버튼 상태가 따라오도록). 부팅 중 먼저 불릴 수 있어 `#capRatios`가 비어 있으면 즉시 빠져나온다 — 안 그러면 `let capRatio` TDZ에 걸린다.
- **일렬 정렬(`capAlign`).** 모든 천체를 같은 황경 `ALIGN_LON`(π, 화면 왼쪽)에 놓는다. `alignRevs()`가 케플러 방정식을 거꾸로 풀어(진근점이각 → 이심근점이각 → 평균근점이각) 그 위치가 되는 `revs`를 낸다. 동반성은 `ALIGN_LON+π`에 둔다 — 같은 편에 두면 SEP_OUT(0.30)과 첫 궤도(a=0.30)가 정확히 겹친다. 궤도 경사(≤0.25rad)로 1도 미만 어긋나는 건 무시한다. 정렬 중에는 공전이 멈추므로 선의 화면 방향은 카메라 방위각 드래그로 정한다.
- **중심별 이름표.** `S.star.namePos`(화면 비율 좌표)가 있으면 자유 배치, 없으면 별 아래에 붙는다. **자유 배치는 사진 캡션이라 촬영 중에만 유효하다** — 평소 화면까지 따라가면 촬영 창을 나가도 이름이 엉뚱한 자리에 남는다(실제 보고). `drawStarName`이 `inCapture()`로 갈라 밖에서는 늘 별 아래에 붙인다. 보일지 말지도 두 갈래다: 촬영 중에는 촬영 패널의 `showLabels`, 평소에는 설정의 `S.showName`. `showLabels`는 촬영에 들어갈 때 `S.showName`에서 시작하고 나갈 때 되돌릴 것이 없다(밖에서는 읽지 않는다). `_nameBox`에 히트박스를 남기고, **촬영 중에만** `pointerdown`에서 시점 회전보다 먼저 가로챈다 — 평소에도 열어두면 탭 추적·시점 드래그와 싸운다. 자유 배치는 사진 캡션이라 15px + 자간을 준다.
- **촬영 중에는 탭 추적을 막는다.** `followBody`가 확대·이동을 따라오게 해서 잡아둔 구도가 날아간다. 선택 링도 `_hideGuide`면 그리지 않는다.
- `cv.toDataURL()` 결과를 모달에 띄우고 다운로드 링크를 건다 — 모바일에서 `<a download>`가 막히는 경우가 있어 이미지를 직접 보여줘 길게 눌러 저장할 수 있게 했다.

## 밸런스 수치

조정 요청이 들어올 만한 값들.

| 항목 | 상수 | 현재 |
|---|---|---|
| 가챠 확률 | `RATE` | ★5 4% / ★4 22% / ★3 74% |
| 천장 | `PITY4`, `PITY5` | 10회 / 30회 (배너별 독립) |
| 등급 분포 | 각 POOL | ★3 37 · ★4 31 · ★5 19 |
| 배치 상한 | `MAX_PLANET`, `MAX_MOON`, `MAX_COMP` | 7 / 행성당 2 / 2 |
| 수집 총량 | 각 POOL | 항성 29 · 행성 30 · 위성 28 (총 87) |
| 포모도로 보너스 | `onPhaseDone` | `useLong`이 켜진 경우 `every`회차마다 토큰 2개 |
| 최소 집중 시간 | `MIN_FOCUS`, `LIMITS` | 10분 (관리자는 1분, 휴식은 1분, 긴 휴식은 5분부터) |
| 완료 팝업 시간 | `POP_MS` | 7초 |
| 기본 시점 / 줌 한계 | `TILT_DEFAULT`, `ZOOM_MIN/MAX` | 0.90rad · 0.45~5 |
| 동반성 궤도 간격 | `SEP_IN`, `SEP_OUT` | 0.165 / 0.30 (기준 장반경 단위) |
| PIN 자릿수 / 잠금 | `schema.sql` | 4~6자리 · 10회 실패 시 15분 |

타이머 입력은 **자연수만** 받는다(`validateField`). 소수점이나 문자를 넣으면 경고 후 직전 값으로 되돌린다.

가챠는 **중복이 나오지 않는다.** 미보유 풀에서만 뽑고, 해당 등급이 소진되면 상위 등급부터 대체한다(`pull()`). 이건 사용자가 명시적으로 요구한 규칙이다. 회귀 검증법: 배너를 비우고 풀 크기만큼 연속 추첨하면 중복 없이 전량이 나와야 한다.

**천체는 전부 실존 천체다.** `st` 배열의 지름·질량·온도·거리는 실측값이므로 **임의로 바꾸지 말 것.** 각 항목은 `n`(한글명) `en`(영문/기호) `d`(가챠 카드용 한 줄) `dd`(도감용 4~5줄) `st`(제원 표)를 갖는다. 행성 배너에는 태양계 13종 외에 실제 외계행성 17종이 들어 있다 — 사진이 없어 관측된 질량·온도·조성에 근거해 그린다. 항성의 `m`은 질량중심 계산에 직접 쓰이므로 값을 바꾸면 궤도가 달라진다.

**도감 구체 뷰어.** `TEX_IDS`에 든 천체만 실제 텍스처를 쓴다. 파일명은 천체 id와 정확히 같아야 하며(`textures/<id>.jpg`) 정거원통도법 이미지여야 한다. `drawGlobe()`가 픽셀 단위로 구면 매핑을 하는데 300×300에서 약 5 ms 걸린다 — 캔버스를 더 키우면 제곱으로 늘어나니 주의. 고리가 있는 천체는 구체를 절반 크기 오프스크린에 그린 뒤 앞뒤 고리 사이에 합성한다. **모달을 닫을 때 `stopGlobe()`를 반드시 부를 것** — 안 그러면 rAF 루프가 계속 돈다. Solar System Scope가 `_fictional`로 표기한 상상도는 실제 사진으로 오해될 수 있어 의도적으로 제외했다.

**절차적 천체 텍스처.** 실제 사진이 없는 73종은 `makeProcTexture()`가 정거원통도법 지도를 코드로 굽는다. 결과가 실제 텍스처와 같은 `{w,h,data}` 형식이라 같은 구면 뷰어를 그대로 타며, 그래서 사진 없는 천체도 드래그 회전이 된다. 768×384에 평균 400ms이고 `_procCache`에 캐시된다.

노이즈는 **구면 위에서 3D로 샘플링한다**(`vnoise3`/`fbm3`/`ridged3`). 2D로 하면 경도 0도에 이음매가 생기고 극에서 뭉개진다. `PROC_RECIPE`에 손으로 다듬은 천체(이오·타이탄·가니메데·트리톤)가 있고, 나머지는 `procGeneric()`이 `f` 플래그와 색으로 조립한다.

**옥타브 수와 문턱값은 함께 움직인다.** 옥타브를 바꾸면 fbm 출력 분포가 달라져 `sstep` 문턱이 엉뚱한 곳에 걸린다 — 이오와 가니메데를 6에서 5로 잘못 옮겼다가 표면이 뭉개진 적이 있다. 수식을 손댈 때는 렌더 결과를 눈으로 확인할 것.

**절차적 텍스처는 세로로 뒤집지 않는다.** 파이썬 프로토타입과 방향을 맞춘 결과이며, 실제 텍스처(북극이 0행)와는 규칙이 다르다. 두 경로를 섞지 말 것.

**구형이 아닌 천체가 있다.** 자체 중력으로 둥글어질 만큼 크지 않은 천체는 `SHAPE` 테이블에 실측 삼축 치수에서 뽑은 축 비(`ax`)와 표면 요철 세기(`lump`)를 둔다. 하우메아는 자전이 빨라 늘어난 것이라 매끄럽고(lump 거의 0), 히페리온·포보스 같은 것은 울퉁불퉁하다. 요철은 방향별 반지름을 `shapeMap()`이 저해상도 맵으로 한 번 구워 두고 이중선형으로 샘플링한다 — 픽셀마다 노이즈를 돌리면 회전 중 프레임이 무거워진다. 궤도 화면(`drawBody`)에서는 요철까지 가지 않고 축 비만큼 눌러 그린다.

**대기 산란은 대기가 있는 천체에만.** `drawGlobe`의 `opt.atmos`가 가장자리 빛번짐을 넣는데, 대기 없는 암석에 주면 가스에 싸인 것처럼 보인다. `f.atmos`/`f.ocean`/`f.glow`가 있을 때만 켠다. 요철 천체의 음영은 밝기 배율이 아니라 형상 맵의 기울기로 **법선을 틀어** 계산한다 — 밝기만 조절하면 물렁해 보인다.

**항성은 자체 발광이다.** `opt.emis`가 켜지면 램버트 대신 주연감광(`1-0.62*(1-mu)`)을 쓴다. 표면은 `starSurface()`가 쌀알무늬와 흑점으로 만든다. **흑점 양은 질량이 아니라 색온도로 정한다** — 질량으로 하면 무겁고 차가운 적색초거성을 놓친다. 코로나는 `drawStar`로 먼저 깔고 그 위에 구체를 얹는다. 블랙홀과 중성자별은 표면이랄 것이 없어 기존 연출을 유지한다.

**고리에는 행성 그림자가 필요하다.** 없으면 종이를 오려 붙인 것처럼 평평해 보인다. 고리 영역으로 클립한 뒤 별 반대 방향으로 어두운 그라디언트를 깐다. 텍스처의 푸른 기운도 덜어낸다 — 실제 고리 입자는 얼음과 암석이라 따뜻한 회백색이다.

**고리는 도감·궤도·썸네일이 같은 원반을 쓴다.** `ringDisc(id)`가 실제 텍스처를 위에서 내려다본 원반으로 한 번 구워 캐시한다. **앞뒤 반쪽의 밝기를 다르게 하면 안 된다** — 가로로 이음매가 생긴다. 반쪽으로 자르는 것은 가림 순서 때문이며 밝기는 같아야 한다. 원본 텍스처는 셰이더로 밝히라고 만든 어두운 값이라 `buildRingDisc`에서 1.95배 올린다. 납작한 정도는 궤도 화면에서는 카메라 기울기를 따르고(고리는 궤도면에 있다), 썸네일은 고정 각도를 쓴다.

**자전축 기울기는 `ax`(도)에 실측값으로 둔다.** 그리는 쪽에서는 캔버스를 그만큼 돌리고 **광원 각도를 같은 만큼 되돌린다** — 안 그러면 조명이 함께 돌아가 별이 엉뚱한 쪽에 있는 것처럼 보인다. 고리도 같이 돌려야 한다. 천왕성(97.8도)이 누워서 돌고 고리가 수직으로 서는 것이 이 처리의 확인 지점이다.

**텍스처를 얹을 항성은 본체 원반을 그리지 않는다**(`extra.noBody`). `drawStar`의 본체 그라디언트는 중심이 왼쪽 위로 치우쳐 있어 가장자리 색이 방향마다 다르다. 텍스처가 안티에일리어싱으로 가장자리 한 픽셀을 반투명하게 남기면 그 틈으로 아래 원반이 **한쪽만** 밝은 호로 비친다. 코로나만 깔고 그 위에 표면을 얹을 것. 텍스처가 아직 없으면 예전대로 본체를 그려 빈 구멍을 막는다.

**`drawGlobe`는 캔버스 안쪽 2px을 비우고 구를 그린다.** 그대로 확대해 얹으면 가장자리에 아래 그림이 링으로 남는다 — 반지름 10px이면 20%가 링이다. 합성할 때 반드시 `globeFit(need)`를 곱할 것.

**꼬리 대신 궤도선.** 예전에는 행성·위성 뒤에 짧은 꼬리를 그렸는데 궤도가 어디인지 읽히지 않았다. 위성도 행성과 같은 `strokeOrbit` 실선을 쓰되 더 옅게(0.13) 깐다.

**배경은 순수한 검정이다.** 사진(ESO 은하수)과 절차적 성운·반짝이 별을 모두 시도했다가 걷어냈다 — 항성계가 주인공인데 배경이 시선을 나눠 가져간다. `buildBg()`는 `#04050b` 한 겹만 칠한다.

**궤도 화면에서 행성·위성 주변에는 빛을 그리지 않는다.** 표면 렌더링만으로 충분하고, 헤일로가 있으면 지저분해진다. 중심별과 동반성의 코로나는 유지한다.

**중심별은 다른 천체의 절반 크기다**(`g.R*VIEW.zoom*.035`). 스킨이 없으면 `centralStarBody()`가 고른 색으로 가상 천체를 만들어 표면 텍스처를 굽는다 — id 에 색이 들어 있어 색을 바꾸면 자동으로 다시 구워진다.

**실제 사진은 도감과 무관하게 불러와야 한다.** 예전에는 `showBodyDetail`에서만 `loadTexture`를 불러, 도감에서 그 천체를 열기 전까지 궤도 화면이 절차적 그림으로 남았다(천왕성이 못생겨 보이던 원인). `readyTexture()`가 `TEX_IDS` 소속이면 즉시 로드를 시작하고 도착 전까지만 절차적 그림을 쓴다. **새 렌더 경로를 만들 때 이 함수를 거치는지 확인할 것.**

**동반성도 중심별과 같은 표면을 입힌다.** 예전에는 `drawStar`만 불러 뭉갠 얼룩으로 보였다(시리우스가 못생겨 보이던 원인). 블랙홀·펄서는 표면이 없으므로 제외한다.

**고리 텍스처는 `drawRing`이 직접 지연 로드한다.** 예전에는 도감 구체 뷰어에서만 불러서, 도감을 한 번도 열지 않으면 궤도 화면의 토성 고리가 대체 그림으로 남았다. 로드가 끝나면 `_ringDiscs` 캐시를 비워 다시 굽는다.

**중심별과 동반성은 같은 크기다.** 반지름 상한을 낮게 잡으면 확대해도 커지지 않으니 주의(예전에 32px 상한 때문에 블랙홀이 고정 크기로 보였다). 블랙홀은 원반이 지평선의 4.6배까지 뻗으므로 그림자를 넘겨받은 반지름의 30%로 잡아야 전체 크기가 다른 별과 비슷해진다.

**블랙홀은 인터스텔라의 가르강튀아 형태다.** 얇은 강착원반이 그림자 앞뒤를 지나고, 렌즈된 후광이 그림자를 감싸며, 광자 고리와 도플러 밝기차가 있다. 원반 각도는 카메라에 반응하되 **0.38을 넘지 않게 눌러** 늘 원반다움을 유지한다 — 완전히 열리면 밝은 덩어리가 되어 블랙홀로 안 보인다.

**자전축과 강착원반은 카메라를 따라야 한다.** 자전축은 궤도면에 수직이므로 관측자 위도를 `π/2 - VIEW.tilt`로 넘긴다 — 위에서 보면 극이, 옆에서 보면 적도가 보여야 한다. 화면 기준으로 고정하면 시점을 바꿔도 늘 옆으로만 도는 것처럼 보인다. 블랙홀 강착원반도 같은 이유로 `extra.diskSquash`에 `cos(VIEW.tilt)`를 넘겨 face-on에서 원, edge-on에서 선이 되게 한다.

**궤도 화면은 매 프레임 실시간으로 그린다.** 미리 구운 프레임을 골라 쓰면 자전이 뚝뚝 끊긴다(12장이면 30도씩 튄다). 천체 크기가 작아 실시간 렌더가 오히려 싸다 — 기본 배율에서 프레임당 1.5ms, 3.5배 확대에서 10.7ms. 크기별 스크래치 캔버스(`scratchFor`)와 ImageData(`globeImageData`)를 재사용하는 것이 핵심이다. **매 프레임 `createImageData`를 부르면 7배 느려진다.** 텍스처가 아직 없으면 `readyTexture()`가 굽기를 예약하고 그동안 기존 절차적 그림으로 대체한다. 광원 방향은 `opt.light`로 넘겨 별 쪽을 비추게 한다.

**썸네일은 여전히 스프라이트를 쓴다.** 매 프레임 구면 매핑을 돌리면 무거우므로 천체마다 한 번 구워(`bodySprite`) 재사용한다. 스프라이트의 광원은 고정이고, 그릴 때 `lightAng-SPRITE_LIGHT`만큼 회전시켜 밝은 면이 별을 향하게 한다 — 구는 회전 대칭이라 이렇게 해도 조명이 맞는다. 자전 속도는 위성이 1(조석 고정), 행성이 6이다 — 실제 자전 주기를 그대로 쓰면 어지럽다. 굽는 작업은 `pumpSprites()`가 한 프레임에 하나씩 처리하고, 준비 전에는 기준 프레임이나 기존 절차적 그림으로 대체한다. 실제 텍스처가 뒤늦게 도착하면 `invalidateSprites()`로 다시 굽는다.

**도감에서는 천체 주변의 빛을 그리지 않는다.** 코로나와 대기 글로우가 정사각형 캔버스 밖으로 넘쳐 네모로 잘려 보이기 때문이다. 궤도 화면은 배경이 넓어 문제가 없으므로 그대로 둔다. 표면이 없는 블랙홀·펄서만 자기 연출을 쓰되 `thumbFitRadius()`로 전체가 캔버스 안에 들어오게 반지름을 줄인다 — 펄서 빔은 반지름의 7배까지 뻗으므로 그냥 그리면 반드시 잘린다.

**썸네일과 중심별 미리보기도 같은 스프라이트를 쓴다.** `paintThumb`은 준비 전에는 간단한 그림을 그리고 `onSpriteReady()`로 다시 그린다. 중심별에 스킨을 씌우면 `starLook().skinBody`로 그 항성의 실제 표면까지 입힌다 — 색만 바꾸면 스킨을 씌운 티가 나지 않는다.

**중심별은 색 또는 스킨이다.** `S.star.skin`이 항성 id면 그 항성의 색·연출·질량을 그대로 따르고, 없으면 `S.star.color`를 쓰며 질량은 `massFromColor()`가 색조에서 추정한다. 렌더링·질량중심·미리보기는 모두 `starLook()`을 거쳐야 한다 — `S.star.color`를 직접 읽으면 스킨이 무시된다.

## PWA와 알림

**`navigator.serviceWorker.ready` 를 조건식에 쓰지 말 것.** 이건 Promise 라 **항상 참**이다. `if(navigator.serviceWorker.ready)` 로 갈래를 타면 서비스워커가 등록되지 않은 환경에서 그 약속이 영영 안 풀려 **알림이 오류도 없이 사라진다.** 실제로 그 버그로 알림이 통째로 죽어 있었다. `swReg()`가 1.5초 제한을 걸고, 실패하면 페이지 알림으로 떨어진다.

**설정에 알림 자가진단이 있다.** `지금 알림 테스트`(알림 자체가 되는지)와 `10초 뒤 알림`(백그라운드에서 오는지)이 나뉘어 있다. "알림이 안 온다"는 보고를 받으면 이 둘 중 어디서 막히는지부터 가르게 할 것. 진단줄의 `SW 활성/없음/등록실패` 도 같이 본다.

**알림은 반드시 `showNotif()`를 거친다.** 홈 화면에 추가한 PWA, 특히 iOS 에서는 페이지의 `new Notification()`이 동작하지 않고 **서비스워커 등록본의 `showNotification()`만** 통한다. `showNotif()`가 서비스워커를 먼저 시도하고 실패하면 페이지 알림으로 떨어진다.

**화면이 꺼지면 rAF 가 멈춘다.** 완료 판정이 렌더 루프에 있으므로 탭이 뒤로 가면 알림이 제때 안 뜬다. `armNotify()`가 세션 시작 시 `endAt`에 맞춰 `setTimeout`을 걸어 둔다. 시작·정지·초기화·부팅(진행 중 세션 복구)에서 짝을 맞출 것 — `disarmNotify()`가 반대다.

**웹에서는 앱을 완전히 종료하면 알림이 오지 않는다.** 예약 알림을 보내려면 푸시 서버(Web Push + VAPID)가 필요한데 이 앱에는 없다. 설정 화면 `알림과 설치` 옆 ⓘ(`INFO.notify`)에 이 한계를 명시해 두었으니 지우지 말 것 — "알림이 안 온다"는 문의의 대부분이 이 경우다.

**안드로이드 래퍼에서는 이 한계가 없다.** `mobile/`의 Capacitor 앱은 `@capacitor/local-notifications`로 **OS에 알림을 예약**하므로 앱을 종료해도 뜬다(`allowWhileIdle`로 도즈 모드까지 관통). `nativeArm()`/`nativeDisarm()`이 그 경로이고, `isNative()`가 거짓인 브라우저에서는 통째로 꺼져 기존 웹 경로만 돈다. **두 경로가 동시에 알림을 띄우지 않도록** 웹 타이머는 `isNative()`면 스스로 빠지고, 렌더 루프가 완료를 처리하면 `nativeDisarm()`으로 예약을 지운다.

Capacitor 플러그인은 번들러 없이 `window.Capacitor.Plugins.<이름>`으로 부른다 — 이 프로젝트에 빌드 단계를 들이지 않기 위한 선택이다. `mobile/sync-web.mjs`가 루트에서 웹 자산을 긁어가며 **서비스워커 등록을 끈다**(네이티브 WebView 는 로컬 파일을 읽으므로 캐시가 불필요하고, sw 가 끼면 앱을 새로 깔아도 낡은 화면이 남는다).

**서비스워커 갱신 규칙이 자산마다 다르다.** `index.html`은 네트워크 우선(캐시 우선이면 배포해도 낡은 앱을 계속 쓴다), 텍스처·아이콘은 캐시 우선. 캐시 이름에 `VER`이 들어 있어 올리면 옛 캐시가 지워진다. **`sw.js`를 고치면 `VER`을 올릴 것.**

`manifest.webmanifest`의 `start_url`/`scope`/아이콘 경로는 모두 **상대 경로**다. GitHub Pages 가 `/stellar-focus/` 하위에 배포하므로 절대 경로로 바꾸면 설치가 깨진다.

서비스워커는 `file://`과 일부 내장 브라우저에서 등록되지 않는다. 그건 정상이고 앱은 그대로 동작해야 한다.

## 카이퍼 벨트

`kbelt()`가 소천체 1150개를 **도넛(토러스) 단면**에 결정론적으로 뿌린다. 관 각도 `th`와 관 반지름 `rt`를 뽑아 궤도 반지름과 **궤도 경사**를 정하는데, 경사가 곧 관의 세로 두께다. `rt`에 `sqrt`를 쓰는 이유는 원 면적이 반지름 제곱에 비례해서다 — 균등하게 뽑으면 중심축만 빽빽해진다. 이심률을 눌러(0.004~0.03) 띠가 뭉개지지 않게 했다.

벨트는 **배치된 가장 바깥 궤도**(`beltRef()`)를 기준으로 깔리므로 행성이 늘면 함께 밀려난다. `fitZoom()`도 `KB_OUT`까지 포함해 화면을 맞춘다 — 안 그러면 레이더 표적이 화면 밖으로 나간다.

- **점을 원으로 그리지 말 것.** 예전에 어두운 원 안에 밝은 원을 겹쳐 명암을 냈는데 유리구슬 하이라이트가 되어 **비눗방울처럼** 보였다. 지금은 소천체마다 고정된 불규칙 7각형 윤곽(`shape`)을 갖고 한 겹으로만 칠한다. 1px 미만이면 윤곽이 안 보이므로 그때만 원으로 그린다.
- 입체감은 **개체별 밝기**로만 낸다. 카메라 방향 `V`(= `project()`의 depth 방향)와 별 방향의 사이각으로 보이는 밝은 면의 비율을 구한다 — 별 너머는 보름달, 이쪽 편은 초승달이다.
- 1150개에 그라디언트를 쓰면 프레임이 무너진다. **[재질][밝기] 버킷으로 묶어 여덟 번의 칠하기**로 끝낸다. 크기는 멱법칙(`0.30+2.0·r^2.6`)이라 대부분 작은 소행성이고 왜소행성급은 드물다.

## 집중 연출

`S.intro`(`none`/`scope`/`comet`/`transit`/`probe`)로 고른다. 네 연출 모두 **남은 시간을 화면으로 읽을 수 있게** 만들어져 있다 — 표적 수, 혜성까지의 거리, 광도 곡선의 진행, 탐사선의 항해 거리.

- **혜성 근일점(`comet`)** — 이심률 0.86 타원. 케플러 시간을 그대로 쓰면 원일점 근처에서 아주 느려 세션의 3분의 2를 화면 밖에서 보낸다. 그래서 **거리를 진행도에 비례해 줄이고 그 거리의 진근점이각을 궤도 방정식에서 거꾸로 구한다** — 경로는 정확한 타원, 다가오는 속도는 타이머에 비례, 끝나는 순간 근일점이다. 꼬리는 둘이다: 이온 꼬리는 곧게 뻗고 매듭이 흐르며, 먼지 꼬리는 **지나온 궤도 위치에서 반태양 방향으로 밀어낸 점들을 이어** 휘어진 부채꼴을 만든다. 근일점은 `beltRef()*0.22` — 안쪽까지 파고들어야 '가장 가까워지는' 순간이 산다. 화면 밖이면 표식도 수치도 그리지 않는다.
- **선명하게 남길 영역은 원으로 오린다.** `applyScopeOptics`가 `{x,y,r}`을 받으면 원, `{x,y,w,h}`면 사각형으로 자른다. 천체를 남길 때 사각형으로 자르면 창문처럼 보여 어색하다 — 브래킷 창(벨트 탐사)만 사각형이 맞다.
- **캔버스에 패널을 놓을 때 HUD 좌표를 읽을 것.** HUD 는 DOM 이라 캔버스 위에 뜬다. 광도 곡선을 고정 좌표에 뒀다가 좌상단 날짜 위젯에 가려진 적이 있다. `$('#clock').getBoundingClientRect().bottom` 을 읽어 그 아래에 놓는다.
- **엄폐(`transit`)** — **궤도면을 옆에서 봐야 보인다.** 이 모드만 `TILT_EDGE`로 잠그되, `tiltTo` 만 바꾸면 지수 보간 때문에 처음 1초가 비스듬하다. **`tilt` 도 함께 즉시 맞출 것.** 통과 전부터 밝기가 완전히 회복될 때까지 이어지고, 광도 곡선은 **관측된 구간만** 그린다. 깊이 → 반지름비, 통과 시간 → 궤도 거리와 주기, 끝에 절대 광도와 거리를 차례로 뽑는다. 중심별과 미지의 천체만 선명하게 남기고 모눈은 초록이다.
- **탐사선(`probe`)** — **왕복** 항해다. 발사 → 중력도움 1 → 목적지 도달 → 탐사 → 중력도움 2 → 귀환. **갈 때도 올 때도 행성 곁을 스치며**, 행성이 그 사이 계속 공전하므로 두 랑데부 지점이 서로 다르다. 목적지는 카이퍼 벨트의 특정 소천체다.

  **시간을 손으로 나누지 말 것.** 예전에는 구간 경계를 상수로 박고(`qA1=0.20` …) 완급 곡선을 따로 먹였는데, 그러면 화면에서 움직이는 속도와 옆에 찍히는 km/s 가 **서로 다른 것을 가리킨다** — 숫자는 11 km/s 로 제일 빠른데 화면에서는 제일 느리게 기어갔다. 완급 곡선을 지수로 만들려는 시도도 실패한다: 1 보다 크면 구간 시작에서 멈추고, 1 보다 작으면 변화율이 무한대라 경계에서 53px 씩 튄다.

  지금은 순서를 뒤집었다. ① 경로의 모양을 정하고(극좌표 선형 보간, 완급 없음) ② 각 점의 속력을 **비스-비바 식**으로 정한 뒤 ③ 시간을 `dt = ds/v` 로 적분해 얻는다(`pbMeasure`). 빠른 곳은 적은 시간에 지나므로 `화면이동 ÷ 진행도 = ds/dt = v` — 화면과 수치가 정확히 비례한다(집중 시점이 face-on 으로 잠겨 있어 화면 거리가 실제 거리에 비례하는 것이 전제다). **구간 길이도 물리가 정한다.** `PB.gamma` 를 1 미만으로 낮추면 완급이 완만해지지만 비례는 깨진다.

  **스윙바이는 구간마다 장반경(`pbSemi`)이 달라지는 것으로 표현된다** — 실제로 중력도움이 하는 일이 궤도 에너지를 행성에서 빌리는 것이다. 그래서 별도의 가속 연출 없이 1번 뒤에 저절로 빨라지고 2번 뒤에 저절로 느려진다. 장거리 순항 두 구간은 원일점을 목적지 너머(`aphK`)에 두어 도착·출발이 기어가지 않게 한다.

  **랑데부는 순환이라 되풀이해 수렴시킨다.** 지점이 도착 시각에 달렸는데 그 시각을 구하려면 다시 지점이 필요하다. **곧바로 새 몫으로 갈아타면 안 된다** — 중력도움 행성은 한 세션에 여러 바퀴를 돌아 경계가 7% 만 움직여도 랑데부가 70도 넘게 튀고, 두 값 사이를 오가며 영영 수렴하지 않는다. 절반씩 옮겨 눌러 앉힌다(`pbTable`, 최대 20회, 세션당 1~6ms). **검증법**: 모든 구간에서 `화면이동(px) ÷ 표시속력(km/s)` 이 같아야 하고(현재 구간 간 3.5% 이내), 경계에서 탐사선과 행성의 화면 거리가 2px 이내여야 한다.

  탐사 구간만은 물리가 아니라 연출이 시간을 정한다(`PB.survey`) — '멈춰 서서 관측하는' 대목이라 거리로 나누면 0 으로 붕괴한다. 그동안 탐사선은 목적지에 붙어 함께 돌고, 표시 속력은 그 천체의 공전 속도다.

  **큰 원이나 브래킷으로 감싸면 실제보다 훨씬 커 보이므로** 목적지는 회색 점 하나에 십자 표식만 얹는다. 탐사 중에는 탐사선이 목적지에 붙어 있어 두 수치 상자가 겹치므로 하나로 합친다. 중력도움 천체는 **사용자가 실제로 배치한 행성**이라 공전한다 — 지금 자리가 아니라 **도착할 자리(랑데부)를 겨냥**한다.

**`fx-preview.html`은 연출 실험장이다.** 본편에 넣기 전에 진행도를 슬라이더로 밀어 보며 판단하는 용도로 만들었다. 새 연출을 검토할 때 여기서 먼저 세우면 본편을 건드리지 않고 돌려볼 수 있다. **시작할 때 잠깐이 아니라 세션이 흐르는 내내 지속된다.** 별도 루프를 돌리지 않고 `render()` 맨 끝에 얹는다 — 연출용 rAF를 따로 돌리면 기존 루프와 겹쳐 프레임이 어긋난다.

**벨트 탐사(`scope`)** — 카이퍼 벨트의 소천체를 사진 프레임(네 모서리 브래킷)으로 포착하고 황경·황위를 띄운다. **남은 표적 수가 곧 남은 시간이다**: 세션마다 `rollRadarMax()`가 7~13개를 뽑고 진행도에 따라 하나씩 놓아 끝에는 하나만 남는다. 씨앗이 `S.timer.endAt`이라 같은 세션에서는 새로고침해도 같은 값이 나온다.

- 표적은 **초기 황경 순으로 정렬한 후보**(`_kb.pick`)에서 균등 간격으로 뽑는다. 그냥 인덱스로 고르면 화면 한쪽에 뭉쳐 프레임과 좌표가 겹쳐 읽을 수 없다.
- 브래킷은 **맥동하지 않는다**. 크기·밝기가 고정이어야 '포착'으로 읽힌다. 마지막까지 남는 표적일수록 또렷한 것은 고정값이다.
- 좌표 라벨은 오른쪽에 자리가 없으면 왼쪽에 붙인다 — 안 그러면 폰에서 잘린다.
- 상태 문구는 `drawScopeReadout()`이 **Face-on 버튼 좌표를 읽어** 그 위에 오른쪽 맞춤으로 놓는다. HUD 위치가 화면 폭에 따라 달라지기 때문이다.

**그리는 순서가 곧 선명도다.** `applyScopeOptics`가 그때까지의 화면을 통째로 흐리게 만들므로, **선명해야 하는 것은 반드시 그 뒤에 그린다** — 브래킷·수치·상태 메시지·모눈·스캔라인, 그리고 항성 테두리(`_starRim`)가 그렇다. 항성 테두리를 천체와 함께 그렸다가 같이 뭉개진 적이 있어, 좌표만 기억해 두고 뒤에서 그린다.

**표적 라벨은 겹치면 아무것도 못 읽는다.** 표적이 최대 13개에 5줄씩이라 충돌이 필연이다. `taken` 목록에 이미 놓은 자리를 기억해 두고 오른쪽·왼쪽·위·아래 순으로 빈 곳을 찾으며, 자리가 없으면 식별 부호만 남긴다. 예약 영역은 4px 부풀려 블록끼리 붙지 않게 한다.

**브래킷과 라벨을 잇는 선을 다시 넣지 말 것.** 라벨이 겹침을 피해 자리를 옮기면 그 선이 옆 표적의 라벨을 대각선으로 가로지른다 — 충돌 회피가 라벨 사각형만 예약하고 선 경로는 모르기 때문이다. 라벨은 늘 브래킷 바로 옆에 붙으므로 선 없이도 짝이 분명하다. 굳이 넣으려면 선분과 모든 예약 영역의 교차를 검사하는 2패스가 필요하다.

**거리 척도는 기준 궤도 = 30 AU**(`AU_REF`)다. 그래야 벨트가 35~43 AU 로 실제 카이퍼 벨트(30~50 AU)와 맞는다. `kbSpec(o)`가 표적마다 색·상태 메시지·지름(km)·SNR 을 id 씨앗으로 고정 배정한다 — 같은 천체는 늘 같은 값이 나와야 관측 기록처럼 읽힌다.

**스캔라인·노이즈는 타일을 구워 패턴으로 반복한다.** 매 프레임 픽셀을 돌리면 무겁다. 노이즈는 프레임마다 타일 위치만 밀어 지글거림을 만든다.

**집중 중 항성은 코로나를 줄이고 테두리를 세운다**(`haloK:1.55` + `strokeStarRim`). 대기·휴식·촬영 중에는 기존의 부드러운 코로나를 유지한다 — 사용자가 촬영은 예전 모습이 낫다고 했다.

**관측 장비 화면(`applyScopeOptics`)** — 브래킷 안쪽만 선명하고 나머지는 흐리며 그 위에 모눈이 깔린다. 캔버스에는 영역별 흐림이 없으므로 **지금까지 그린 장면을 통째로 복사**해 두고, 흐리게 한 번 깔고 표적 사각형으로 클립해 선명한 원본을 되살린다. 브래킷과 좌표는 이 처리 **뒤에** 그려야 선명하다. 흐림은 GPU 가속이라 274만 픽셀(DPR 3)에서도 0.18ms다.

시작 직후 `APPROACH_MS`(2.2초) 동안 `stepStartFx()`가 시점을 직접 몬다. 기본 지수 보간에 맡기면 0.5초 만에 붙어 다가가는 느낌이 사라진다. `frame()`의 보간 직후, `render()` 앞에서 불러야 한다. 타이머는 연출과 무관하게 이미 시작돼 있다(절대 시각 기준).

## 남은 작업

사용자가 요청했으나 아직 안 한 것들.

- 가챠 배너에 **실제 성운/은하 배경** (말머리성운, 게성운, 안드로메다, 은하수)

이미지 도입은 "단계적으로" 하기로 했다. 도감 텍스처 뷰어를 먼저 만들어 보고, 만족스러우면 궤도 화면까지 WebGL로 확장한다. 실제 사진이 존재하는 것은 태양계 천체뿐이며 태양 외의 항성은 표면 사진이 없다는 점을 전제로 설계할 것.
