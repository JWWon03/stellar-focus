# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

집중/공부 타이머 + 항성계 수집 게임. 세션을 완주하면 가챠 토큰을 얻고, 뽑은 천체를 궤도에 배치해 자기만의 항성계를 키운다. 사용자 대면 규칙 요약은 `README.md`, 구조 설명 문서는 `docs.html` 참고.

| 파일 | 내용 |
|---|---|
| `index.html` | 앱 전체 (CSS·JS 포함, 이것 하나로 동작) |
| `supabase/schema.sql` | 계정/클라우드 저장용 Postgres 스키마 + RPC 함수 |
| `docs.html` | 처음 보는 사람을 위한 구조 설명 문서 |
| `README.md` | 규칙 요약, 모바일 앱 래핑 방법 |
| `textures/` | 실제 관측 텍스처 13종 + 토성 고리 + 출처·라이선스 문서 |

## 명령

빌드·번들러·테스트·의존성이 **없다.**

```bash
start index.html
```

배포는 GitHub Pages. `main`에 push하면 1분 내로 https://jwwon03.github.io/stellar-focus/ 에 자동 반영된다.

```bash
git add -A && git commit -m "설명" && git push
```

배포본이 로컬과 같은지 확인 (PowerShell):

```powershell
$r = Invoke-WebRequest "https://jwwon03.github.io/stellar-focus/" -UseBasicParsing; $r.RawContentStream.Position = 0; (Get-FileHash -InputStream $r.RawContentStream -Algorithm SHA256).Hash -eq (Get-FileHash "index.html" -Algorithm SHA256).Hash
```

전역 `S`(상태)와 `startPhase` / `pull` / `renderSystem` / `renderGacha` / `sceneBodies` 등이 window에 노출돼 있어, 콘솔에서 상태를 심어 특정 화면을 바로 띄울 수 있다. 수동 테스트 시 유용하다. 단 **가짜 토큰으로 `AUTH`를 심으면 안 된다** — 클라우드 푸시가 인증 실패를 받아 자동 로그아웃되면서 상태가 초기화된다. 클라우드를 건드리지 않으려면 `cloudPush`를 빈 함수로 덮어쓸 것.

RPC를 직접 두드려 서버만 확인할 수도 있다:

```powershell
Invoke-RestMethod -Uri 'https://bssawuumjkxvzyrmvscd.supabase.co/rest/v1/rpc/sf_login' -Method Post -Headers @{apikey=$key} -ContentType 'application/json' -Body '{"p_id":"__probe__","p_pin":"0000"}'
```

## 외부 리소스 제약

CDN 스크립트와 웹폰트를 **일절 추가하지 않는다.** 궤도 화면의 모든 그래픽은 캔버스에 절차적으로 그린다. 오프라인 동작과 "파일 하나 더블클릭으로 실행"이 이 프로젝트의 핵심 성질이고, 나중에 Capacitor로 모바일 앱 래핑할 때의 전제이기도 하다.

예외가 둘 있고, 둘 다 **없어도 앱이 동작하도록** 만들어져 있다.

`textures/`의 이미지는 도감 구체 뷰어에서만 쓰이며 **그 천체를 열 때 지연 로드**된다. 파일이 없거나 로드에 실패하면 조용히 절차적 렌더링으로 넘어가므로, index.html 하나만 있어도 앱은 정상 동작한다. CC BY 4.0 자료이므로 `textures/ATTRIBUTION.md`와 도감 화면의 출처 표기를 지울 것.

또 하나의 예외가 **선택 기능인 클라우드 동기화**다. 계정 기능은 Supabase REST를 `fetch`로 직접 부르지만 라이브러리는 쓰지 않으며, `CLOUD` 상수가 비어 있거나 서버가 죽어도 앱은 오프라인으로 완전히 동작해야 한다.

## 구조

`index.html`의 `<script>`는 번호가 붙은 섹션으로 나뉜다 (`/* === N. 이름 === */`). 파일이 크므로 수정 전 해당 섹션 주석으로 위치를 잡을 것.

0 유틸 · 1 천체 데이터 풀 · 2 상태/저장 · 3 천체 렌더러 · 4 메인 캔버스 · 5 타이머 · 6 모달 · 7 인트로 · 8 가챠 · 9 항성계 설정 · 10 세션 완료 · 11 위젯 바인딩 · 13 계정/클라우드 · 12 부팅

(13이 12보다 앞에 있다. 부팅이 마지막에 오도록 나중에 끼워 넣은 결과다.)

## 알아야 할 불변조건

여러 곳에 흩어져 있어 한 파일만 읽어선 안 보이는 것들.

**색 헬퍼는 반드시 hex를 반환한다.** `shade` / `lighten` / `mix`의 반환값이 다시 `hx()` / `rgba()`로 들어가기 때문에, `rgb(...)` 문자열을 돌려주면 파싱이 깨져 캔버스가 조용히 잘못 그려진다. 새 색 헬퍼를 만들 때도 `toHex()`를 거칠 것.

**알파를 두 번 곱하지 말 것.** `strokeStyle`에 이미 알파가 든 `rgba(...)`를 넣고 `globalAlpha`까지 걸면 실효 알파가 곱해져 선이 사라진다. 동반성 궤도가 안 보이던 원인이 이것이었다. 알파를 인자로 받는 그리기 함수에는 불투명 색을 넘긴다.

**작은 캔버스에 천체를 그릴 때 헤일로가 잘린다.** 항성 코로나는 기본 반경의 6.6배, 발광체 글로우는 2.5배, 고리는 2.55배까지 뻗는다. 캔버스 밖으로 나가면 알파가 0이 아닌 지점에서 잘려 **사각형 테두리**가 보인다. `drawStar`의 `extra.haloK`로 코로나를 줄이고, 본체 반경은 캔버스 절반 나누기 배율 이하로 잡는다. 썸네일은 `paintThumb`, 미리보기는 `paintPreview`에 이미 보정값이 들어 있다.

**궤도 물리.** `phase`는 "기준 궤도의 누적 회전수"다. 기준 궤도 = 행성이 배치된 가장 바깥 슬롯. 집중 세션 동안 기준 궤도가 정확히 1바퀴를 돌고(`phase = _startPhase + 진행률`), 안쪽 행성은 케플러 제3법칙 `revs = phase * (a_ref/a)^1.5`로 더 빠르게 돈다. 대기 중에는 240초에 1바퀴로 천천히 표류. 이 "타이머 시간 = 1바퀴 공전"은 제품의 핵심 컨셉이므로 바꾸지 말 것.

궤도는 원이 아니라 **타원**이다. 슬롯마다 장반경·이심률·경사·승교점·근점이 `orbitEl(i)`에 결정론적으로 고정돼 있고, `orbitPos()`가 케플러 방정식을 뉴턴법으로 풀어 위치를 낸다(제1·2법칙 — 근점에서 빨라진다). 궤도 경로·꼬리는 모두 이 함수를 샘플링해 그리므로, 궤도 모양을 바꾸려면 `orbitEl`만 손대면 된다.

**카메라 `VIEW`.** `tilt`(0=face-on, π/2=edge-on), `az`(방위각), `zoom`, `ox/oy`(추적 오프셋), `follow`(추적 대상 key). `project()`가 az로 z축 둘레를 돌린 뒤 tilt로 눕히고 `depth`를 함께 돌려주며, 그리기 순서는 이 depth로 정렬한다(화면 y 정렬이 아니다). 매 프레임 지수 보간으로 부드럽게 따라간다. `fitZoom()`은 **현재 tilt를 반영**해 배율을 계산한다 — edge-on일 때 세로가 눌리므로 그만큼 더 당겨야 화면이 찬다.

드래그 회전의 부호는 "손으로 붙잡고 돌리는" 감각에 맞춰져 있다(`azTo -= dx`, `tiltTo -= dy`). 바꾸기 전에 화면 좌표로 방향을 확인할 것 — 처음 구현 때 두 축 모두 반대로 넣었다가 고쳤다.

**항성 질량중심.** `starPositions()`가 실제 쌍성처럼 공통 질량중심을 원점에 두고 각 별의 위치를 낸다. 동반성이 둘이면 **계층적 삼중성**(안쪽 쌍성 + 그 질량중심을 도는 바깥 동반성)으로 푼다 — 실제 삼중성계가 안정적으로 존재하는 방식이다. 질량은 `PALETTE[].m`(중심별, 분광형 기준)과 `STAR_POOL[].m`에서 온다. 궤도선은 각 별이 **자기가 속한 질량중심** 둘레를 돌도록 그려야 한다(원점 기준으로 그리면 안쪽 쌍성이 어긋난다). **검증법**: 모든 별의 질량가중 위치 평균이 (0,0)이어야 한다.

**타이머는 절대 시각(`endAt`) 기준이다.** 남은 시간을 감산하지 않고 매번 `endAt - Date.now()`로 계산한다. 덕분에 탭이 백그라운드로 내려가거나 앱이 절전에 들어가도 어긋나지 않는다. 부팅 시 이미 만료된 세션은 즉시 완료 처리하고 토큰을 지급한다. 이 방식을 감산 방식으로 되돌리지 말 것.

**검은 구체 / 리빌.** 집중 중에는 `isFocusDark()`가 참이라 행성·위성·동반성이 검은 구체가 된다. **중심별은 예외로 항상 밝다** — 사용자가 명시적으로 요청한 사항이니 되돌리지 말 것. 완료 시 `reveal` 웨이브가 중심에서 퍼지고 `litAt(a)`가 장반경별로 밝기를 결정한다. 휴식 중에는 전부 밝다.

**집중 중 잠금.** `isLocked()`가 참이면 가챠·항성계 설정·모드·시간 입력이 모두 막히고 [종료] 버튼만 남는다. 차단은 `disabled` 속성이 아니라 각 핸들러의 `guard()`가 한다 — `disabled`면 클릭 이벤트가 오지 않아 안내 문구를 띄울 수 없기 때문이다. 새 메뉴를 추가하면 핸들러 첫 줄에 `if(guard())return;`을 넣을 것. 카메라 조작(줌·회전·추적)은 의도적으로 잠그지 않는다.

**완료 팝업은 모달이 아니다.** `showTokenPopup()`은 7초 뒤 자동으로 닫히며 그 동안 타이머가 계속 흐른다(포모도로는 휴식이 즉시 시작된다). 토큰은 팝업과 무관하게 이미 지급돼 있고 확인 버튼은 조기 닫기일 뿐이다. 이걸 블로킹 모달로 되돌리면 요구사항이 깨진다.

**`confirm()` / `alert()` 를 쓰지 말 것.** 홈 화면에 추가한 PWA나 일부 모바일 웹뷰에서 조용히 차단되어 `false`를 돌려준다 — 버튼이 먹통으로 보이는 원인이다. 확인이 필요하면 `await ask(제목, 설명, 버튼라벨, danger)`를 쓴다. 확인창은 다른 모달 위에 겹치므로 z-index가 더 높고 배경 막을 옅게 깐다.

**회전한 요소는 클릭 영역도 회전한다.** 다이얼의 진행률 SVG가 `rotate(-90deg)` 되어 있어 히트 영역이 원래 박스 밖으로 삐져나와 위쪽 입력창을 덮었다. 장식용 요소에 `transform`을 걸면 `pointer-events:none`을 함께 줄 것.

**가챠 연출 중에는 `pulling`이 참이다.** 이때 뽑기 버튼이 잠기고 건너뛰기 버튼만 뜬다. 새 연출을 시작할 때 `cancelAnimationFrame(_warpRaf)`로 이전 프레임 루프를 반드시 끊어야 한다 — 안 끊으면 연타 시 여러 루프가 겹쳐 돌며 결과가 즉시 튀어나온다.

**전체 초기화는 클라우드까지 지워야 한다.** 로컬만 지우면 로그인 상태에서 다음 부팅에 `cloudSyncOnBoot()`가 되살려 초기화가 안 된 것처럼 보인다. 빈 상태를 만든 뒤 `cloudPush(true)`로 덮어쓴다.

**계정은 선택 기능이다.** `CLOUD.url`/`CLOUD.key`가 비어 있으면 계정 메뉴가 "미설정"으로 뜨고 앱은 완전히 오프라인으로 돈다. 이 성질을 깨지 말 것 — 서버가 죽어도 게임은 돌아가야 한다. 로그인하면 저장 키가 `stellarFocus.v1.<id>`로 갈라져 한 기기에서도 계정별 항성계가 분리된다. 서버 접근은 `supabase/schema.sql`의 RPC 함수로만 하며 테이블은 RLS로 잠겨 있다(정책을 만들지 않는 것이 곧 차단이다). publishable 키는 공개되어도 되는 값이다. 로컬과 클라우드에 서로 다른 데이터가 있으면 `adoptCloud()`가 사용자에게 선택을 받는다 — 조용히 덮어쓰지 말 것. 비교는 `fingerprint()`로 하며 타이머 런타임 상태는 제외한다(기기마다 다르므로).

**관리자 계정.** `ADMIN_IDS`에 든 아이디로 로그인하면 토큰 무제한(`∞` 표시), 집중 시간 1분부터 설정 가능(`limMin()`), 천체 종류별 도감 초기화가 열린다. **클라이언트 판정이라 진짜 권한 통제가 아니다** — 테스트 편의 기능이다. 도감을 초기화할 때는 보유 목록만 지우지 말고 궤도 배치도 함께 해제할 것(`resetOneDex`). 안 그러면 유령 천체가 남는다.

**localStorage 는 접근만 해도 예외를 던질 수 있다.** 시크릿 모드나 저장소 차단 환경이 그렇다. 반드시 `lsGet` / `lsSet` / `lsDel` 래퍼로만 접근한다.

**저장 스키마.** 키 하나에 전체 상태를 넣는다. `load()`가 방어적 복구를 한다 — 존재하지 않는 천체 id 제거, 슬롯 배열 길이 보정, 행성 없는 슬롯의 위성 제거. **풀에서 id를 바꾸거나 지우면, 그리고 슬롯 구조를 바꾸면 기존 사용자 데이터가 깨진다.** `load()`의 복구 로직을 함께 갱신하거나 키 버전을 올릴 것.

**반응형 분기점 760px이 CSS와 JS 양쪽에 있다.** `geom()`이 `W < 760`일 때 항성계 중심을 위로(`H*0.32`) 올리고 반경을 줄여 좌하단 타이머 위젯을 피한다. CSS `@media (max-width:760px)`도 같은 값이다. 한쪽만 바꾸면 폰에서 위젯이 중심별을 덮는다.

**HUD 4모서리 배치는 제품 요구사항이다.** 사용자가 직접 스케치한 배치이므로 임의로 옮기지 말 것.

- 좌상단: 날짜/시각 위젯 (계정 버튼이 그 안에 들어 있다)
- 우상단: 토큰 수 + 세로 메뉴 스택 — 가챠 · 항성계 · 도감 · 카메라 · 설정
- 좌하단: 타이머 위젯 (모드는 탭, 오른쪽에 사운드/접기)
- 우하단: Face-on · Edge-on · 확대축소

**도감은 최상위 메뉴다.** 예전에는 가챠 창 안의 토글이라 사용자가 찾지 못했다. 다시 숨기지 말 것.

**텍스처 굽기는 절대 통짜로 돌리지 말 것.** 절차적 텍스처 한 장이 `PROC_W*PROC_H`(512×256) 픽셀짜리 동기 루프다. 그대로 돌리면 그 시간 내내 프레임이 통째로 멈춘다 — 폰에서 천체 하나당 0.5초 이상이고, 항성계가 다 차면 그런 게 10번 넘게 온다. `bakeSlice()`가 매 프레임 `BAKE_MS`(5ms)만큼 위도 줄만 채우고 넘긴다. 총 시간은 같지만 화면이 계속 돈다.

- 새 코드에서 `makeProcTexture()`(통짜)를 직접 부르지 말 것. `readyTexture(b, 화면반지름)`로 예약하고 준비될 때까지 null 을 받아 넘어가야 한다. 실제로 `pumpSprites()`가 이걸 어겨서 시간 분할을 우회하고 있었다 — 썸네일 하나 구울 때마다 200ms씩 멈췄다.
- 큐는 `_texPri`(화면상 반지름)로 정렬한다. 중심별이 절차적인데 위성부터 구우면 눈에 띈다.
- `PROC_W/H`는 도감 구체(300px 캔버스, 구 지름 276px) 기준이다. 512폭이면 1:1 이상이라 충분하다. 예전 768은 3배 낭비였다.
- 성능을 의심할 땐 프레임 간격 히스토그램을 볼 것. 중앙값은 멀쩡한데 33ms 넘는 프레임 몇 개가 버벅임의 정체인 경우가 많다.

**촬영 모드(`body.capture`).** 카메라 버튼을 누르면 HUD가 사라지고 우하단에 **설정 · 촬영 · 나가기**만 남는다. 설정을 누르면 `#capPanel`이 열리고 그 안에 비율 키(전체/1:1/4:3/3:4/9:16/16:9) · 시점(Face-on·Edge-on·전체 보기) · 배치(일렬 정렬) · 표시 토글(별 이름·궤도선)이 들어 있다.

- `capRatio`가 `full`이 아니면 `captureRect()`가 잘릴 영역을 계산하고 `drawCaptureGuide()`가 바깥을 어둡게 덮고 삼분할선을 그린다. **가이드는 `render()` 맨 끝에서 그린다** — 천체보다 먼저 그리면 천체가 그 위에 얹혀 틀이 안 보인다.
- 촬영은 `_hideGuide=true`로 두고 `render(clock)`을 한 번 더 돌린 뒤 `drawImage`로 잘라낸다. 이 플래그를 빼먹으면 삼분할선이 사진에 찍힌다.
- 궤도선 토글은 `globalAlpha=0`이 아니라 **블록 자체를 건너뛴다**. `strokeStarRing`이 내부에서 `globalAlpha`를 덮어써서 동반성 궤도만 살아남기 때문이다.
- `fitZoom()`은 촬영 중이면 화면이 아니라 `captureRect()` 안에 맞춘다. 세로 비율에서 계가 틀 밖으로 삐져나가면 안 된다.
- 촬영 모드를 나가면 `showLabels`/`showOrbits`/`capRatio`를 되돌린다. 촬영용 토글이 평소 화면까지 따라가면 궤도선이 사라진 채로 남는다.
- `syncCapUI()`는 `updateViewUI()`에서 불린다(드래그로 시점이 바뀌어도 버튼 상태가 따라오도록). 부팅 중 먼저 불릴 수 있어 `#capRatios`가 비어 있으면 즉시 빠져나온다 — 안 그러면 `let capRatio` TDZ에 걸린다.
- **일렬 정렬(`capAlign`).** 모든 천체를 같은 황경 `ALIGN_LON`(π, 화면 왼쪽)에 놓는다. `alignRevs()`가 케플러 방정식을 거꾸로 풀어(진근점이각 → 이심근점이각 → 평균근점이각) 그 위치가 되는 `revs`를 낸다. 동반성은 `ALIGN_LON+π`에 둔다 — 같은 편에 두면 SEP_OUT(0.30)과 첫 궤도(a=0.30)가 정확히 겹친다. 궤도 경사(≤0.25rad)로 1도 미만 어긋나는 건 무시한다. 정렬 중에는 공전이 멈추므로 선의 화면 방향은 카메라 방위각 드래그로 정한다.
- **중심별 이름표.** `S.star.namePos`(화면 비율 좌표)가 있으면 자유 배치, 없으면 별 아래에 붙는다. `_nameBox`에 히트박스를 남기고, **촬영 중에만** `pointerdown`에서 시점 회전보다 먼저 가로챈다 — 평소에도 열어두면 탭 추적·시점 드래그와 싸운다. 자유 배치는 사진 캡션이라 15px + 자간을 준다.
- **촬영 중에는 탭 추적을 막는다.** `followBody`가 확대·이동을 따라오게 해서 잡아둔 구도가 날아간다. 선택 링도 `_hideGuide`면 그리지 않는다.
- `cv.toDataURL()` 결과를 모달에 띄우고 다운로드 링크를 건다 — 모바일에서 `<a download>`가 막히는 경우가 있어 이미지를 직접 보여줘 길게 눌러 저장할 수 있게 했다.

## 밸런스 수치

조정 요청이 들어올 만한 값들.

| 항목 | 상수 | 현재 |
|---|---|---|
| 가챠 확률 | `RATE` | ★5 4% / ★4 22% / ★3 74% |
| 천장 | `PITY4`, `PITY5` | 10회 / 30회 (배너별 독립) |
| 등급 분포 | 각 POOL | ★3 37 · ★4 31 · ★5 19 |
| 배치 상한 | `MAX_PLANET`, `MAX_MOON`, `MAX_COMP` | 7 / 행성당 2 / 2 |
| 수집 총량 | 각 POOL | 항성 29 · 행성 30 · 위성 28 (총 87) |
| 포모도로 보너스 | `onPhaseDone` | `useLong`이 켜진 경우 `every`회차마다 토큰 2개 |
| 최소 집중 시간 | `MIN_FOCUS`, `LIMITS` | 10분 (관리자는 1분, 휴식은 1분, 긴 휴식은 5분부터) |
| 완료 팝업 시간 | `POP_MS` | 7초 |
| 기본 시점 / 줌 한계 | `TILT_DEFAULT`, `ZOOM_MIN/MAX` | 0.90rad · 0.45~5 |
| 동반성 궤도 간격 | `SEP_IN`, `SEP_OUT` | 0.165 / 0.30 (기준 장반경 단위) |
| PIN 자릿수 / 잠금 | `schema.sql` | 4~6자리 · 10회 실패 시 15분 |

타이머 입력은 **자연수만** 받는다(`validateField`). 소수점이나 문자를 넣으면 경고 후 직전 값으로 되돌린다.

가챠는 **중복이 나오지 않는다.** 미보유 풀에서만 뽑고, 해당 등급이 소진되면 상위 등급부터 대체한다(`pull()`). 이건 사용자가 명시적으로 요구한 규칙이다. 회귀 검증법: 배너를 비우고 풀 크기만큼 연속 추첨하면 중복 없이 전량이 나와야 한다.

**천체는 전부 실존 천체다.** `st` 배열의 지름·질량·온도·거리는 실측값이므로 **임의로 바꾸지 말 것.** 각 항목은 `n`(한글명) `en`(영문/기호) `d`(가챠 카드용 한 줄) `dd`(도감용 4~5줄) `st`(제원 표)를 갖는다. 행성 배너에는 태양계 13종 외에 실제 외계행성 17종이 들어 있다 — 사진이 없어 관측된 질량·온도·조성에 근거해 그린다. 항성의 `m`은 질량중심 계산에 직접 쓰이므로 값을 바꾸면 궤도가 달라진다.

**도감 구체 뷰어.** `TEX_IDS`에 든 천체만 실제 텍스처를 쓴다. 파일명은 천체 id와 정확히 같아야 하며(`textures/<id>.jpg`) 정거원통도법 이미지여야 한다. `drawGlobe()`가 픽셀 단위로 구면 매핑을 하는데 300×300에서 약 5 ms 걸린다 — 캔버스를 더 키우면 제곱으로 늘어나니 주의. 고리가 있는 천체는 구체를 절반 크기 오프스크린에 그린 뒤 앞뒤 고리 사이에 합성한다. **모달을 닫을 때 `stopGlobe()`를 반드시 부를 것** — 안 그러면 rAF 루프가 계속 돈다. Solar System Scope가 `_fictional`로 표기한 상상도는 실제 사진으로 오해될 수 있어 의도적으로 제외했다.

**절차적 천체 텍스처.** 실제 사진이 없는 73종은 `makeProcTexture()`가 정거원통도법 지도를 코드로 굽는다. 결과가 실제 텍스처와 같은 `{w,h,data}` 형식이라 같은 구면 뷰어를 그대로 타며, 그래서 사진 없는 천체도 드래그 회전이 된다. 768×384에 평균 400ms이고 `_procCache`에 캐시된다.

노이즈는 **구면 위에서 3D로 샘플링한다**(`vnoise3`/`fbm3`/`ridged3`). 2D로 하면 경도 0도에 이음매가 생기고 극에서 뭉개진다. `PROC_RECIPE`에 손으로 다듬은 천체(이오·타이탄·가니메데·트리톤)가 있고, 나머지는 `procGeneric()`이 `f` 플래그와 색으로 조립한다.

**옥타브 수와 문턱값은 함께 움직인다.** 옥타브를 바꾸면 fbm 출력 분포가 달라져 `sstep` 문턱이 엉뚱한 곳에 걸린다 — 이오와 가니메데를 6에서 5로 잘못 옮겼다가 표면이 뭉개진 적이 있다. 수식을 손댈 때는 렌더 결과를 눈으로 확인할 것.

**절차적 텍스처는 세로로 뒤집지 않는다.** 파이썬 프로토타입과 방향을 맞춘 결과이며, 실제 텍스처(북극이 0행)와는 규칙이 다르다. 두 경로를 섞지 말 것.

**구형이 아닌 천체가 있다.** 자체 중력으로 둥글어질 만큼 크지 않은 천체는 `SHAPE` 테이블에 실측 삼축 치수에서 뽑은 축 비(`ax`)와 표면 요철 세기(`lump`)를 둔다. 하우메아는 자전이 빨라 늘어난 것이라 매끄럽고(lump 거의 0), 히페리온·포보스 같은 것은 울퉁불퉁하다. 요철은 방향별 반지름을 `shapeMap()`이 저해상도 맵으로 한 번 구워 두고 이중선형으로 샘플링한다 — 픽셀마다 노이즈를 돌리면 회전 중 프레임이 무거워진다. 궤도 화면(`drawBody`)에서는 요철까지 가지 않고 축 비만큼 눌러 그린다.

**대기 산란은 대기가 있는 천체에만.** `drawGlobe`의 `opt.atmos`가 가장자리 빛번짐을 넣는데, 대기 없는 암석에 주면 가스에 싸인 것처럼 보인다. `f.atmos`/`f.ocean`/`f.glow`가 있을 때만 켠다. 요철 천체의 음영은 밝기 배율이 아니라 형상 맵의 기울기로 **법선을 틀어** 계산한다 — 밝기만 조절하면 물렁해 보인다.

**항성은 자체 발광이다.** `opt.emis`가 켜지면 램버트 대신 주연감광(`1-0.62*(1-mu)`)을 쓴다. 표면은 `starSurface()`가 쌀알무늬와 흑점으로 만든다. **흑점 양은 질량이 아니라 색온도로 정한다** — 질량으로 하면 무겁고 차가운 적색초거성을 놓친다. 코로나는 `drawStar`로 먼저 깔고 그 위에 구체를 얹는다. 블랙홀과 중성자별은 표면이랄 것이 없어 기존 연출을 유지한다.

**고리에는 행성 그림자가 필요하다.** 없으면 종이를 오려 붙인 것처럼 평평해 보인다. 고리 영역으로 클립한 뒤 별 반대 방향으로 어두운 그라디언트를 깐다. 텍스처의 푸른 기운도 덜어낸다 — 실제 고리 입자는 얼음과 암석이라 따뜻한 회백색이다.

**고리는 도감·궤도·썸네일이 같은 원반을 쓴다.** `ringDisc(id)`가 실제 텍스처를 위에서 내려다본 원반으로 한 번 구워 캐시한다. **앞뒤 반쪽의 밝기를 다르게 하면 안 된다** — 가로로 이음매가 생긴다. 반쪽으로 자르는 것은 가림 순서 때문이며 밝기는 같아야 한다. 원본 텍스처는 셰이더로 밝히라고 만든 어두운 값이라 `buildRingDisc`에서 1.95배 올린다. 납작한 정도는 궤도 화면에서는 카메라 기울기를 따르고(고리는 궤도면에 있다), 썸네일은 고정 각도를 쓴다.

**자전축 기울기는 `ax`(도)에 실측값으로 둔다.** 그리는 쪽에서는 캔버스를 그만큼 돌리고 **광원 각도를 같은 만큼 되돌린다** — 안 그러면 조명이 함께 돌아가 별이 엉뚱한 쪽에 있는 것처럼 보인다. 고리도 같이 돌려야 한다. 천왕성(97.8도)이 누워서 돌고 고리가 수직으로 서는 것이 이 처리의 확인 지점이다.

**궤도 화면에서 행성·위성 주변에는 빛을 그리지 않는다.** 표면 렌더링만으로 충분하고, 헤일로가 있으면 지저분해진다. 중심별과 동반성의 코로나는 유지한다.

**중심별은 다른 천체의 절반 크기다**(`g.R*VIEW.zoom*.035`). 스킨이 없으면 `centralStarBody()`가 고른 색으로 가상 천체를 만들어 표면 텍스처를 굽는다 — id 에 색이 들어 있어 색을 바꾸면 자동으로 다시 구워진다.

**실제 사진은 도감과 무관하게 불러와야 한다.** 예전에는 `showBodyDetail`에서만 `loadTexture`를 불러, 도감에서 그 천체를 열기 전까지 궤도 화면이 절차적 그림으로 남았다(천왕성이 못생겨 보이던 원인). `readyTexture()`가 `TEX_IDS` 소속이면 즉시 로드를 시작하고 도착 전까지만 절차적 그림을 쓴다. **새 렌더 경로를 만들 때 이 함수를 거치는지 확인할 것.**

**동반성도 중심별과 같은 표면을 입힌다.** 예전에는 `drawStar`만 불러 뭉갠 얼룩으로 보였다(시리우스가 못생겨 보이던 원인). 블랙홀·펄서는 표면이 없으므로 제외한다.

**고리 텍스처는 `drawRing`이 직접 지연 로드한다.** 예전에는 도감 구체 뷰어에서만 불러서, 도감을 한 번도 열지 않으면 궤도 화면의 토성 고리가 대체 그림으로 남았다. 로드가 끝나면 `_ringDiscs` 캐시를 비워 다시 굽는다.

**중심별과 동반성은 같은 크기다.** 반지름 상한을 낮게 잡으면 확대해도 커지지 않으니 주의(예전에 32px 상한 때문에 블랙홀이 고정 크기로 보였다). 블랙홀은 원반이 지평선의 4.6배까지 뻗으므로 그림자를 넘겨받은 반지름의 30%로 잡아야 전체 크기가 다른 별과 비슷해진다.

**블랙홀은 인터스텔라의 가르강튀아 형태다.** 얇은 강착원반이 그림자 앞뒤를 지나고, 렌즈된 후광이 그림자를 감싸며, 광자 고리와 도플러 밝기차가 있다. 원반 각도는 카메라에 반응하되 **0.38을 넘지 않게 눌러** 늘 원반다움을 유지한다 — 완전히 열리면 밝은 덩어리가 되어 블랙홀로 안 보인다.

**자전축과 강착원반은 카메라를 따라야 한다.** 자전축은 궤도면에 수직이므로 관측자 위도를 `π/2 - VIEW.tilt`로 넘긴다 — 위에서 보면 극이, 옆에서 보면 적도가 보여야 한다. 화면 기준으로 고정하면 시점을 바꿔도 늘 옆으로만 도는 것처럼 보인다. 블랙홀 강착원반도 같은 이유로 `extra.diskSquash`에 `cos(VIEW.tilt)`를 넘겨 face-on에서 원, edge-on에서 선이 되게 한다.

**궤도 화면은 매 프레임 실시간으로 그린다.** 미리 구운 프레임을 골라 쓰면 자전이 뚝뚝 끊긴다(12장이면 30도씩 튄다). 천체 크기가 작아 실시간 렌더가 오히려 싸다 — 기본 배율에서 프레임당 1.5ms, 3.5배 확대에서 10.7ms. 크기별 스크래치 캔버스(`scratchFor`)와 ImageData(`globeImageData`)를 재사용하는 것이 핵심이다. **매 프레임 `createImageData`를 부르면 7배 느려진다.** 텍스처가 아직 없으면 `readyTexture()`가 굽기를 예약하고 그동안 기존 절차적 그림으로 대체한다. 광원 방향은 `opt.light`로 넘겨 별 쪽을 비추게 한다.

**썸네일은 여전히 스프라이트를 쓴다.** 매 프레임 구면 매핑을 돌리면 무거우므로 천체마다 한 번 구워(`bodySprite`) 재사용한다. 스프라이트의 광원은 고정이고, 그릴 때 `lightAng-SPRITE_LIGHT`만큼 회전시켜 밝은 면이 별을 향하게 한다 — 구는 회전 대칭이라 이렇게 해도 조명이 맞는다. 자전 속도는 위성이 1(조석 고정), 행성이 6이다 — 실제 자전 주기를 그대로 쓰면 어지럽다. 굽는 작업은 `pumpSprites()`가 한 프레임에 하나씩 처리하고, 준비 전에는 기준 프레임이나 기존 절차적 그림으로 대체한다. 실제 텍스처가 뒤늦게 도착하면 `invalidateSprites()`로 다시 굽는다.

**도감에서는 천체 주변의 빛을 그리지 않는다.** 코로나와 대기 글로우가 정사각형 캔버스 밖으로 넘쳐 네모로 잘려 보이기 때문이다. 궤도 화면은 배경이 넓어 문제가 없으므로 그대로 둔다. 표면이 없는 블랙홀·펄서만 자기 연출을 쓰되 `thumbFitRadius()`로 전체가 캔버스 안에 들어오게 반지름을 줄인다 — 펄서 빔은 반지름의 7배까지 뻗으므로 그냥 그리면 반드시 잘린다.

**썸네일과 중심별 미리보기도 같은 스프라이트를 쓴다.** `paintThumb`은 준비 전에는 간단한 그림을 그리고 `onSpriteReady()`로 다시 그린다. 중심별에 스킨을 씌우면 `starLook().skinBody`로 그 항성의 실제 표면까지 입힌다 — 색만 바꾸면 스킨을 씌운 티가 나지 않는다.

**중심별은 색 또는 스킨이다.** `S.star.skin`이 항성 id면 그 항성의 색·연출·질량을 그대로 따르고, 없으면 `S.star.color`를 쓰며 질량은 `massFromColor()`가 색조에서 추정한다. 렌더링·질량중심·미리보기는 모두 `starLook()`을 거쳐야 한다 — `S.star.color`를 직접 읽으면 스킨이 무시된다.

## 남은 작업

사용자가 요청했으나 아직 안 한 것들.

- 가챠 배너에 **실제 성운/은하 배경** (말머리성운, 게성운, 안드로메다, 은하수)
- 타이머 시작 화면 **연출 선택** (망원경 탐사 화면 / 혜성 접근)
- **PWA 전환** — manifest + service worker. 알림 코드(`notifyDone`)는 이미 있고 권한만 받으면 페이지가 열려 있을 때 동작한다. 잠금화면 알림은 PWA 설치가 전제.

이미지 도입은 "단계적으로" 하기로 했다. 도감 텍스처 뷰어를 먼저 만들어 보고, 만족스러우면 궤도 화면까지 WebGL로 확장한다. 실제 사진이 존재하는 것은 태양계 천체뿐이며 태양 외의 항성은 표면 사진이 없다는 점을 전제로 설계할 것.
