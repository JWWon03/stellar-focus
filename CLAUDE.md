# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

집중/공부 타이머 + 항성계 수집 게임. 세션을 완주하면 가챠 토큰을 얻고, 뽑은 천체를 궤도에 배치해 자기만의 항성계를 키운다. **`README.md` 와 `docs.html` 은 사실과 다른 곳이 있다** — 초기 사양서에서 멈춰 있다. 무엇이 틀렸는지는 맨 아래 *남은 작업* 에 정리해 두었으니, 그 둘을 근거로 판단하지 말 것. **코드와 이 문서가 기준이다.**

| 파일 | 내용 |
|---|---|
| `index.html` | 앱 전체 (CSS·JS 포함, 이것 하나로 동작) |
| `supabase/schema.sql` | 계정/클라우드 저장용 Postgres 스키마 + RPC 함수 |
| `docs.html` | 처음 보는 사람을 위한 구조 설명 문서 |
| `README.md` | 규칙 요약, 모바일 앱 래핑 방법 |
| `textures/` | 실제 관측 텍스처 13종 + 토성 고리 + 출처·라이선스 문서 |
| `manifest.webmanifest` | PWA 매니페스트 (홈 화면 설치·standalone) |
| `sw.js` | 서비스워커 — 오프라인 캐시 + 알림 통로 |
| `icons/` | 설치 아이콘 (192·512·maskable·apple-touch) |
| `mobile/` | 안드로이드 래퍼 (Capacitor). 빌드 방법은 `mobile/README.md` |
| `fx-preview.html` | 집중 연출 실험장. 본편과 별개로 돌아가며 진행도를 슬라이더로 민다 |
| `ceres_cand.json` | 천체 데이터를 고를 때 쓴 후보 목록. 앱은 읽지 않는다 |

## 명령

빌드·번들러·테스트·의존성이 **없다.**

```bash
start index.html
```

**서비스워커·알림·클라우드를 건드리는 변경은 `file://` 로 확인할 수 없다.** 로컬 서버를 띄우고 볼 것. 캐시가 낀 것 같으면 쿼리를 붙여 새로 받는다(`?v=2`).

```bash
python -m http.server 8777
```

배포는 GitHub Pages. `main`에 push하면 1분 내로 https://jwwon03.github.io/stellar-focus/ 에 자동 반영된다.

```bash
git add -A && git commit -m "설명" && git push
```

배포본이 로컬과 같은지 확인 (PowerShell):

```powershell
$r = Invoke-WebRequest "https://jwwon03.github.io/stellar-focus/" -UseBasicParsing; $r.RawContentStream.Position = 0; (Get-FileHash -InputStream $r.RawContentStream -Algorithm SHA256).Hash -eq (Get-FileHash "index.html" -Algorithm SHA256).Hash
```

전역 `S`(상태)와 `startPhase` / `pull` / `renderSystem` / `renderGacha` / `sceneBodies` 등이 window에 노출돼 있어, 콘솔에서 상태를 심어 특정 화면을 바로 띄울 수 있다. 수동 테스트 시 유용하다. 단 **가짜 토큰으로 `AUTH`를 심으면 안 된다** — 클라우드 푸시가 인증 실패를 받아 자동 로그아웃되면서 상태가 초기화된다. 클라우드를 건드리지 않으려면 `cloudPush`를 빈 함수로 덮어쓸 것.

**특정 상황을 바로 띄우는 조리법.** 눈으로 확인해야 하는 변경(연출·궤도·연출 중 UI)은 25분을 기다릴 수 없다. 콘솔에 붙여넣으면 항성계가 채워진 채 집중 세션 한가운데로 들어간다. 진행도만 바꿔 가며 어느 대목이든 볼 수 있다.

```js
cloudPush = () => {};                      // 클라우드를 건드리지 않는다
S.created = true;
S.star = {name:'베텔기우스', color:'#ffd97a', skin:'st_betel', namePos:null};
const P = ['pl_mercury','pl_venus','pl_earth','pl_mars','pl_jupiter'];
for (let i=0;i<7;i++) S.sys.slots[i].p = P[i] || null;
S.owned = {star:['st_betel'], planet:P, moon:[]};
S.intro = 'probe';                         // none | scope | comet | transit | probe
save();
document.querySelectorAll('.mask.on').forEach(m => m.classList.remove('on'));
resetTimer();
setTimeout(() => {
  startPhase('focus');
  VIEW.tilt=VIEW.tiltTo; VIEW.az=VIEW.azTo; VIEW.zoom=VIEW.zoomTo;
  endStartFx();                            // 2.2초 접근 연출을 건너뛴다
  window.setq = q => S.timer.endAt = Date.now() + S.timer.total*(1-q);
  setq(0.35);                              // 진행도 35% 로 점프
}, 150);
```

빠뜨리기 쉬운 것 셋. **`cloudPush` 를 막지 않으면** 로그인 상태에서 이 가짜 상태가 서버로 올라간다. **`endStartFx()` 를 안 부르면** 처음 2.2초 동안 카메라를 `stepStartFx()` 가 몰아 좌표가 계속 변한다. **보간을 즉시 끝내지 않으면** 첫 1초의 시점이 목표값이 아니다 — 좌표를 재는 검증이 어긋난다.

측정으로 확인할 것. 스크린샷 눈대중은 이 프로젝트에서 여러 번 틀렸다. 프레임 간격 히스토그램(중앙값이 아니라 33ms 넘는 프레임 수), 좌표 단언, 구간별 비율 같은 것을 쓴다.

**자주 쓰는 검증 창구.** 이름을 잘못 잡으면 멀쩡한 코드를 버그로 오진한다.

| 확인하려는 것 | 이렇게 읽는다 |
|---|---|
| 항성 질량 | **`starMasses()`** — `starLook()` 의 필드는 `m` 이 아니라 **`mass`** 다 |
| 집중 중 밝기 | `litAt(a)` · `sceneBodies(geom())[0].lit`(중심별) |
| 수집 총량 | `STAR_POOL.length` · `PLANET_POOL.length` · `MOON_POOL.length` |
| 화면에 그려지는 것 | `sceneBodies(geom())` — 클릭 판정·추적·연출이 전부 이 배열을 쓴다 |
| 벨트 소천체 위치 | `kbPos(o)` → `toScreen(…, geom())` |
| 배포본이 로컬과 같은가 | 배포본을 받아 `index.html` 과 **해시 비교** (아래 PowerShell 한 줄) |

**프레임 시간은 같은 세션 안에서 교대로 재고 최솟값을 쓸 것.** 배경에 무거운 프로그램이 돌면 절대 시간이 통째로 흔들린다 — 실제로 게임 하나 때문에 기준선이 17ms 에서 33ms 로 뛴 적이 있다. 서로 다른 시점의 절대값을 비교하지 말 것.

RPC를 직접 두드려 서버만 확인할 수도 있다:

```powershell
Invoke-RestMethod -Uri 'https://bssawuumjkxvzyrmvscd.supabase.co/rest/v1/rpc/sf_login' -Method Post -Headers @{apikey=$key} -ContentType 'application/json' -Body '{"p_id":"__probe__","p_pin":"0000"}'
```

## 이 저장소에서 일하는 방식

사용자가 직접 요청했거나, 지키지 않아 손해를 본 것들이다.

**보이는 것을 바꿀 때는 적용하기 전에 후보를 보여 주고 고르게 한다.** "적용시키기 전에 어떤 모양일지 먼저 보여주고 판단하고 싶어"는 명시적인 요청이었고, 실제로 두 번 결과를 바꿨다(벨트 소천체 윤곽, 알갱이 크기·밝기). 후보는 **앱과 같은 난수·같은 명암·같은 크기**로 그려야 판단이 의미가 있다 — 크게만 그려 보여 주면 19px 로 줄였을 때 전혀 다른 것이 나온다. 자립 HTML 로 만들어 파일로 보내면 폰에서도 본다.

**애매하면 묻는다.** 사용자가 그렇게 요청했다. 다만 물을 때는 **재본 근거와 추천안을 같이** 낸다 — 선택지만 나열하면 사용자가 대신 재야 한다.

**끝낸 항목마다 커밋한다.** 한 요청이 여러 손질로 갈라져도 각각 커밋한다. 커밋 메시지에는 **무엇을 왜, 그리고 재본 값**을 적는다 — 이 저장소의 커밋 로그가 사실상 설계 기록이다.

**동작이 바뀌면 `APP_VER` 을 올린다.** 사용자가 배포본을 확인하는 유일한 수단이다.

**"안 바뀐 것 같다"는 보고를 받으면 배포본부터 확인한다.** 로컬과 해시를 비교해 보면 대부분 캐시가 아니라 다른 원인이다 — 실제로 한 번은 배포가 멀쩡했고 사용자가 본 것은 2배 확대 화면이었다.

## 외부 리소스 제약

CDN 스크립트와 웹폰트를 **일절 추가하지 않는다.** 궤도 화면의 모든 그래픽은 캔버스에 절차적으로 그린다. 오프라인 동작과 "파일 하나 더블클릭으로 실행"이 이 프로젝트의 핵심 성질이고, 나중에 Capacitor로 모바일 앱 래핑할 때의 전제이기도 하다.

예외가 둘 있고, 둘 다 **없어도 앱이 동작하도록** 만들어져 있다.

`textures/`의 이미지는 **그 천체가 처음 필요해질 때 지연 로드**된다(도감뿐 아니라 궤도 화면·썸네일도 같은 경로를 쓴다 — `readyTexture()`). 파일이 없거나 로드에 실패하면 조용히 절차적 렌더링으로 넘어가므로, index.html 하나만 있어도 앱은 정상 동작한다. CC BY 4.0 자료이므로 `textures/ATTRIBUTION.md`와 도감 화면의 출처 표기를 **지우지 말 것** — 저작자 표시는 이 라이선스의 조건이다.

또 하나의 예외가 **선택 기능인 클라우드 동기화**다. 계정 기능은 Supabase REST를 `fetch`로 직접 부르지만 라이브러리는 쓰지 않으며, `CLOUD` 상수가 비어 있거나 서버가 죽어도 앱은 오프라인으로 완전히 동작해야 한다.

## 구조

`index.html`의 `<script>`는 번호가 붙은 섹션으로 나뉜다 (`/* === N. 이름 === */`). 파일이 크므로 수정 전 해당 섹션 주석으로 위치를 잡을 것.

0 유틸 · 1 천체 데이터 풀 · 2 상태/저장 · 3 천체 렌더러 · 4 메인 캔버스 · 5 타이머 · 6 모달 · 7 인트로 · 8 가챠 · 9 항성계 설정 · 10 세션 완료 · 11 위젯 바인딩 · 13 계정/클라우드 · 12 부팅

(13이 12보다 앞에 있다. 부팅이 마지막에 오도록 나중에 끼워 넣은 결과다.)

번호가 붙지 않은 큰 덩어리도 있다. 4와 5 사이에 **카이퍼 벨트**와 **집중 연출**(혜성 근일점 · 엄폐(식) 관측 · 탐사선 항해가 각각 따로 표시돼 있다)이, 8 안에 **절차적 천체 텍스처**와 **도감 구체 뷰어**가 들어 있다.

## 한 판의 흐름

이 앱이 무엇을 하는 물건인지부터. **집중 세션을 완주하면 가챠 토큰이 나오고, 뽑은 실존 천체를 궤도에 배치해 자기 항성계를 키운다.** 궤도 화면은 늘 배경에 떠 있고 모든 기능은 그 위의 모달로 뜬다.

1. **부팅** `boot()` → `load()`가 localStorage 에서 `S` 를 복원한다. 중심별이 없으면(`!S.created`) `mIntro` 를 띄워 이름과 색부터 받는다. 마지막에 `cloudSyncOnBoot()`.
2. **집중 시작** `startPhase('focus')` → `endAt = Date.now() + 분*60000` 을 못박고, 시점을 연출에 맞춰 잠그고(`TILT_FACE`, 엄폐만 `TILT_EDGE`), `rollRadarMax()`로 이번 세션의 표적 수를 뽑고, `armNotify()`로 알림을 예약한다.
3. **흐르는 동안** `frame(now)`가 매 프레임 → `tickTimer()`(만료 검사) → 전역 `phase` 갱신 → 카메라 지수 보간 → `stepStartFx()` → `render(clock)`. 별도 루프는 없다. 집중 연출도 `render()` 맨 끝에 얹힌다.
4. **완주** `onPhaseDone()` → `S.stats` 누적, `S.tokens += 1`(포모도로 보너스면 2), `reveal` 웨이브 시작, `chime()`, `notifyDone()`, `showTokenPopup()`. 포모도로면 이어서 휴식 단계로 넘어가고, 그 휴식이 끝나면 `mDone` 이 다음 회차를 권한다.
5. **가챠** `pull()` / `pullMany(n)` → `drawOne(banner)`이 **미보유 풀에서만** 뽑아 `S.owned[kind]` 에 넣는다.
6. **배치** `mSystem`(항성계 설정)에서 `S.sys.slots[i].p` 와 `.m[k]`, `S.sys.comp[]` 를 채운다.
7. 다음 프레임부터 그 천체가 실제로 궤도를 돈다. 배치된 가장 바깥 행성이 **기준 궤도**가 되어 카이퍼 벨트 위치와 AU 환산까지 따라 움직인다.

## 상태 `S` — 저장되는 전부

키 하나(`stellarFocus.v1`, 로그인하면 `stellarFocus.v1.<아이디>`)에 통째로 들어간다. `freshState()`가 기준 모양이고 `load()`가 방어적으로 복구한다.

```
S = {
  created   : 중심별을 만들었는가. false 면 mIntro 가 뜬다
  intro     : 집중 연출  'none'|'scope'|'comet'|'transit'|'probe'
  star      : {name, color, skin, namePos}   skin=STAR_POOL id 또는 null
                                             namePos=촬영용 이름표 자유 배치(화면 비율)
  tokens    : 가챠 토큰
  owned     : {star:[id…], planet:[id…], moon:[id…]}   보유 도감
  pity      : {star:{p4,p5}, planet:…, moon:…}         배너별 천장 카운터
  sys : {
    comp  : [id|null, id|null]                          동반성 최대 2
    slots : [{p:id|null, m:[id|null,id|null]} × 7]      행성 7 · 행성당 위성 2
  }
  timer : {
    mode  : 'count'|'pomo'
    phase : 'idle'|'focus'|'break'|'longbreak'
    running, endAt, remain, total                       endAt 이 진실, remain 은 일시정지용
    count, focus, brk, long, every, round, useLong      설정값
    _startPhase                                         세션 시작 시점의 전역 phase. 진행률→공전 변환의 기준
  }
  stats     : {sessions, focusMs, day, dayMs}          day/dayMs 는 오늘치뿐
  log       : {'YYYY-MM-DD': 밀리초, …}                날짜별 집중 기록. 최근 400일
  ses       : {'YYYY-MM-DD': [[시작분,길이분],…]}       날짜별 세션 구간. 24시간 띠용
  notif     : [{t, k:'focus'|'break', …}, …]           완료 알림. 최신순 50개
  notifSeen : 밀리초                                   이 시각 이후가 '읽지 않음'
  sound, showName, mini, invX, invY, menuOpen           환경설정
  updatedAt : writeLocal() 이 매번 찍는다. 클라우드 비교의 근거
}
```

`save()`는 180ms 디바운스, `saveNow()`는 즉시. 둘 다 `writeLocal()`로 모이고 거기서 `updatedAt` 을 찍은 뒤 로그인 상태면 `cloudPush()` 까지 부른다. **마지막 동기화 시각(`syncedAt`)만은 `S` 밖의 별도 LS 키에 둔다** — `S` 안에 두면 그 값 자체가 계속 달라져 비교가 망가진다.

## 좌표계와 단위

궤도 코드를 읽으려면 이것부터 알아야 한다. 세 층이 있다.

**① 월드 (임의 단위).** 슬롯 `i` 의 장반경은 `orbitEl(i).a = 0.30 + i*0.11667` 이다. 실제 거리와 무관한 자체 단위다. `orbitPos(el, revs)` 가 케플러 방정식을 풀어 `{x,y,z}` 를 낸다. `revs` 는 **누적 회전수**(1이면 한 바퀴)다.

**② 투영.** `project(p)` → `{sx, sy, depth}`. `VIEW.az` 로 z축 둘레를 먼저 돌린 뒤 `VIEW.tilt` 로 눕힌다. **그리기 순서는 화면 y 가 아니라 `depth` 로 정렬한다** — depth 가 클수록 카메라에 가깝다.

**③ 화면 px.** `toScreen(p, g)` → `{x, y, depth}`. `g = geom()` 이 `{cx, cy, R}` 을 주고 배율은 `g.R * VIEW.zoom`, 추적 오프셋 `VIEW.ox/oy` 를 뺀다.

거리를 사람이 읽는 값으로 바꿀 때는 **기준 궤도 = 30 AU** 로 놓는다.

| 이름 | 뜻 |
|---|---|
| `outerSlot()` | 행성이 배치된 가장 바깥 슬롯 번호 (없으면 -1) |
| `beltRef()` | 그 슬롯의 장반경 = **기준 궤도**. 비어 있으면 0.34 |
| `AU_REF` = 30 | 기준 궤도를 30 AU 로 본다 |
| `auPerUnit()` | `30 / beltRef()`. 월드 단위 → AU |
| 전역 `phase` | **기준 궤도의 누적 회전수.** 집중 중에는 `_startPhase + 진행률`, 대기 중에는 240초에 한 바퀴 |

그래서 카이퍼 벨트(`KB_MID` 1.43 · `KB_TUBE` 0.19)가 약 37~49 AU 로 나오고, 실제 카이퍼 벨트(30~50 AU)와 맞는다. **행성이 늘어 기준 궤도가 바깥으로 밀리면 벨트도 연출도 함께 밀린다.**

`sceneBodies(g)` 가 이번 프레임에 그릴 것을 한 배열로 만든다. 각 원소는 `{key, kind:'star'|'comp'|'planet'|'moon', body, world, scr, r, lit, el, revs, …}` 이고, 클릭 판정·추적·연출이 모두 이 배열을 쓴다. `bodies[0]` 은 항상 중심별이다.

## 천체 데이터

`STAR_POOL`(29) · `PLANET_POOL`(30) · `MOON_POOL`(28) 세 배열. 한 항목의 모양은

```
{ id, n:한글명, en:영문/기호, cls:분류, t:등급(3|4|5), sz:상대 크기,
  ax:자전축 기울기(도), m:질량(항성만 — 질량중심 계산에 직접 쓰인다),
  c1,c2:주/보조 색, f:{플래그}, d:가챠 한 줄, dd:도감 4~5줄, st:[[항목,값]…] }
```

`f` 가 그리기를 정한다 — `ring` 고리 · `atmos` 대기 · `glow` 자체 발광 · `bands` 가스 줄무늬 · `crat` 크레이터 · `dune` 사구 · `ocean`/`lava`/`ice`/`cryst` 표면 종류 · `acc` 강착원반(블랙홀) · `beam` 펄서 빔 · `puls` 맥동. 절차적 텍스처(`procGeneric`)도 같은 플래그를 읽는다.

**id 는 저장 데이터의 열쇠다.** 바꾸거나 지우면 기존 사용자의 `owned` 와 `sys` 가 깨진다.

## 화면

궤도 캔버스가 배경이고 나머지는 전부 `.mask` 모달이다. `open(id)` / `close(id)` 로 여닫는다.

| id | 무엇 | 어디서 |
|---|---|---|
| `mIntro` | 중심별 만들기 (이름·색) | 첫 부팅 자동 |
| `mGacha` | 가챠 배너·뽑기 | 우상단 메뉴 |
| `mMulti` | 연속 뽑기 요약 (단발은 모달 없이 망원경 시야에서 끝난다) | `pullMany()` |
| `mSystem` | 항성계 설정 — 슬롯에 천체 배치 | 우상단 메뉴 |
| `mPick` | 보관함에서 천체 고르기 | `mSystem` 안 |
| `mDex` | 도감 (최상위 메뉴다. 가챠 안에 숨기지 말 것) | 우상단 메뉴 |
| `mDetail` | 천체 상세 + 구체 뷰어 | 도감·결과 카드 |
| `mSettings` | 설정 · 집중 연출 선택 · 알림/설치 | 우상단 메뉴 |
| `mAccount` | 로그인/가입/로그아웃 | 좌상단 위젯의 계정 버튼 |
| `mRecord` | 집중 기록 — 하루(24시간 띠) ↔ 달력 두 화면 | 좌상단 **날짜·시각을 누르면** |
| `mInbox` | 완료 알림 목록 | 우상단 **토큰 왼쪽 종 버튼** |
| `mStarEdit` / `mWheel` | 중심별 수정 / 색상환 | `mSystem` |
| `mShot` | 촬영 결과 이미지 | 촬영 모드 |
| `mDone` | **범용 결과/선택 틀.** 휴식 종료 안내와 클라우드 동기화 선택이 이 하나를 나눠 쓴다 | `onPhaseDone()` · `adoptCloud()` |
| `mAsk` | 확인 다이얼로그 (`await ask(…)`) | 어디서나 |

## 무엇을 고치려면 어디를 보나

| 하려는 일 | 볼 곳 |
|---|---|
| 천체 추가·수정 | 섹션 1 의 세 POOL. `id` 는 건드리지 말 것 |
| 궤도 모양·간격 | `orbitEl(i)` 하나만. 경로·연출이 전부 여기를 샘플링한다 |
| 천체가 그려지는 모습 | `drawBody` / `drawStar`(궤도 화면) · `drawGlobe`(도감 구체) · `makeProcTexture`(절차적 지도) |
| 카메라·시점 | `VIEW` + `project` + `frame()` 의 보간 |
| 집중 연출 | `focusFxMode()`가 켤지 정하고 `drawFocusFx()`가 갈라 부른다 — `fxRadar`(벨트 탐사) · `fxComet` · `fxTransit` · `fxProbe` |
| 타이머 규칙 | `startPhase` / `tickTimer` / `onPhaseDone` |
| 확률·천장 | `RATE`, `PITY4/5`, `drawOne()` |
| 가챠 연출 | `scopeStart` / `scopeDraw` / `scopeGlobe` · `SCOPE_TIER` (등급별 강도) |
| 저장 스키마 | `freshState()` 와 `load()` 를 **함께** 고친다 |
| 클라우드 | `cloudPush` / `cloudSyncOnBoot` / `adoptCloud` + `supabase/schema.sql` |
| HUD·설정 UI | 섹션 11, 그리고 `renderSettings()` |
| 카이퍼 벨트 | `kbelt()`(생성) · `drawBelt()`(그리기) · `KB_*` 상수 한 묶음 |
| 집중 기록 | `S.log`·`S.ses` · `renderRecordDay()`/`renderRecordCal()` · `onPhaseDone()` 의 `pushSession()` |
| 타이머 위젯 모양 | `#timerPanel` 마크업 + `.tTab`/`.tBody`/`.tFoot` CSS + `updateTimerUI()` |
| 메뉴 아이콘·호버 색 | 우상단 `.menuStack` 마크업(인라인 SVG) + `#btn*:hover` CSS |

## 알아야 할 불변조건

여러 곳에 흩어져 있어 한 파일만 읽어선 안 보이는 것들.

**색 헬퍼는 반드시 hex를 반환한다.** `shade` / `lighten` / `mix`의 반환값이 다시 `hx()` / `rgba()`로 들어가기 때문에, `rgb(...)` 문자열을 돌려주면 파싱이 깨져 캔버스가 조용히 잘못 그려진다. 새 색 헬퍼를 만들 때도 `toHex()`를 거칠 것.

**알파를 두 번 곱하지 말 것.** `strokeStyle`에 이미 알파가 든 `rgba(...)`를 넣고 `globalAlpha`까지 걸면 실효 알파가 곱해져 선이 사라진다. 동반성 궤도가 안 보이던 원인이 이것이었다. 알파를 인자로 받는 그리기 함수에는 불투명 색을 넘긴다.

**작은 캔버스에 천체를 그릴 때 헤일로가 잘린다.** 항성 코로나는 기본 반경의 6.6배, 발광체 글로우는 2.5배, 고리는 2.55배까지 뻗는다. 캔버스 밖으로 나가면 알파가 0이 아닌 지점에서 잘려 **사각형 테두리**가 보인다. `drawStar`의 `extra.haloK`로 코로나를 줄이고, 본체 반경은 캔버스 절반 나누기 배율 이하로 잡는다. 썸네일은 `paintThumb`, 미리보기는 `paintPreview`에 이미 보정값이 들어 있다.

**궤도 물리.** `phase`는 "기준 궤도의 누적 회전수"다. 기준 궤도 = 행성이 배치된 가장 바깥 슬롯. 집중 세션 동안 기준 궤도가 정확히 1바퀴를 돌고(`phase = _startPhase + 진행률`), 안쪽 행성은 케플러 제3법칙 `revs = phase * (a_ref/a)^1.5`로 더 빠르게 돈다. 대기 중에는 240초에 1바퀴로 천천히 표류. 이 "타이머 시간 = 1바퀴 공전"은 제품의 핵심 컨셉이므로 바꾸지 말 것.

궤도는 원이 아니라 **타원**이다. 슬롯마다 장반경·이심률·경사·승교점·근점이 `orbitEl(i)`에 결정론적으로 고정돼 있고, `orbitPos()`가 케플러 방정식을 뉴턴법으로 풀어 위치를 낸다(제1·2법칙 — 근점에서 빨라진다). 궤도 경로·꼬리는 모두 이 함수를 샘플링해 그리므로, 궤도 모양을 바꾸려면 `orbitEl`만 손대면 된다.

**카메라 `VIEW`.** `tilt`(0=face-on, π/2=edge-on), `az`(방위각), `zoom`, `ox/oy`(추적 오프셋), `follow`(추적 대상 key). `project()`가 az로 z축 둘레를 돌린 뒤 tilt로 눕히고 `depth`를 함께 돌려주며, 그리기 순서는 이 depth로 정렬한다(화면 y 정렬이 아니다). 매 프레임 지수 보간으로 부드럽게 따라간다. `fitZoom()`은 **현재 tilt를 반영**해 배율을 계산한다 — edge-on일 때 세로가 눌리므로 그만큼 더 당겨야 화면이 찬다.

드래그 회전의 부호는 "손으로 붙잡고 돌리는" 감각에 맞춰져 있다(`azTo -= dx`, `tiltTo -= dy`). 바꾸기 전에 화면 좌표로 방향을 확인할 것 — 처음 구현 때 두 축 모두 반대로 넣었다가 고쳤다.

**항성 질량중심.** `starPositions()`가 실제 쌍성처럼 공통 질량중심을 원점에 두고 각 별의 위치를 낸다. 동반성이 둘이면 **계층적 삼중성**(안쪽 쌍성 + 그 질량중심을 도는 바깥 동반성)으로 푼다 — 실제 삼중성계가 안정적으로 존재하는 방식이다. 질량은 `PALETTE[].m`(중심별, 분광형 기준)과 `STAR_POOL[].m`에서 온다. 궤도선은 각 별이 **자기가 속한 질량중심** 둘레를 돌도록 그려야 한다(원점 기준으로 그리면 안쪽 쌍성이 어긋난다). **검증법**: 모든 별의 질량가중 위치 평균이 (0,0)이어야 한다. 질량은 **`starMasses()`** 에서 가져올 것 — `starPositions()` 가 쓰는 것이 그것이다. `starLook()` 의 질량 필드는 `m` 이 아니라 **`mass`** 라서, `.m` 으로 읽으면 `undefined` 가 되어 *질량중심이 원점이 아니다* 라는 엉뚱한 결론이 나온다(실제로 그렇게 헛짚었다). 제대로 재면 오차가 1e-18 수준이다.

**타이머는 절대 시각(`endAt`) 기준이다.** 남은 시간을 감산하지 않고 매번 `endAt - Date.now()`로 계산한다. 덕분에 탭이 백그라운드로 내려가거나 앱이 절전에 들어가도 어긋나지 않는다. 부팅 시 이미 만료된 세션은 즉시 완료 처리하고 토큰을 지급한다. 이 방식을 감산 방식으로 되돌리지 말 것.

**집중 중 밝기 / 리빌.** 처음에는 집중 중 행성·위성이 완전한 검은 구체(밝기 0)였다. 흐림·모눈이 깔리면서 아예 안 보이게 되어 **`FOCUS_LIT`(0.62)** 로 바꿨다 — 색과 표면은 보이되 완주 후보다는 어둡다. **중심별은 예외로 항상 밝다** — 사용자가 명시적으로 요청한 사항이니 되돌리지 말 것. 완료 시 `reveal` 웨이브가 중심에서 퍼지며 `litAt(a)`가 0.62 → 1로 올린다. 휴식 중에는 전부 밝다.

**집중 중 잠금.** `isLocked()`가 참이면 가챠·항성계 설정·모드·시간 입력이 모두 막힌다. 차단은 `disabled` 속성이 아니라 각 핸들러의 `guard()`가 한다 — `disabled`면 클릭 이벤트가 오지 않아 안내 문구를 띄울 수 없기 때문이다. 새 메뉴를 추가하면 핸들러 첫 줄에 `if(guard())return;`을 넣을 것.

- **타이머 자체는 잠기지 않는다.** 예전에는 집중 중 [종료] 버튼 하나만 남았지만(옛 요구사항 16), 사용자 요청으로 **집중 중에도 일시정지**할 수 있게 열었다. 잠기는 것은 메뉴·설정이고 타이머는 세우거나 끝낼 수 있다. 일시정지해도 진행률이 멈추므로 "집중 시간 = 한 바퀴"는 그대로 성립한다.

**집중 중 시점 고정은 `lockFocusView()` 한 곳이다.** `startPhase('focus')` 와 **부팅 복구가 똑같이 부른다** — 둘 중 하나만 부르면 어긋난다(바로 아래 항목).

**시점과 배율은 반드시 이 순서로 정한다 — 시점 먼저, 배율 나중.** `fitZoom()` 이 `cos(VIEW.tiltTo)` 로 세로 눌림을 반영하기 때문이다. 순서가 뒤바뀌면 엉뚱한 시점 기준으로 배율이 잡혀 **벨트 표적이 화면 밖으로 나간다**(실제로 그랬다). 벨트 탐사는 표적이 카이퍼 벨트에 흩어져 있어 `fitZoom()` 을 그대로 쓴다 — 조금이라도 당기면 바깥 표적이 잘린다. 나머지 연출만 1.12배 당긴다.

**집중 중에는 시점이 완전히 잠긴다.** 세션을 시작하면 `azTo=0` 과 함께 **엄폐(식)만 `TILT_EDGE`, 나머지 셋(벨트 탐사·혜성 근일점·탐사선 항해)은 `TILT_FACE`** 로 맞추고, 드래그(`pointermove`)·Face-on/Edge-on 버튼(`viewLocked()`)·탭 추적(`endDrag`)이 모두 막힌다. 관측 장비 화면이라 시점을 유지해야 한다. **확대축소만 열려 있다** — 이건 의도적이다.

- **`VIEW` 는 저장되지 않는다.** 부팅하면 늘 기본 시점(`tilt` 0.90)에서 시작한다. 그래서 **세션 도중에 앱을 다시 열면 시점 고정이 풀린다** — 그런데 집중 중에는 버튼이 막혀 있어 되돌릴 길이 없고, 세션이 끝날 때까지 엉뚱한 시점으로 굳는다. *"엄폐 연출에서 미지 천체가 가리는 시점은 edge-on 인데 배경이 face-on 이다"* 라는 보고의 원인이 이것이었다 — 연출은 자기 좌표로 그리고 배경만 어긋나 있었다. 새로고침·홈 화면 복귀·PWA 재시작이 모두 해당한다.
- 그래서 부팅에서 진행 중인 집중 세션을 복구할 때 `lockFocusView()` 를 다시 부른다. 거기엔 접근 연출이 없으므로 **방위각·배율까지 즉시 맞춘다**(`VIEW.az=VIEW.azTo; VIEW.zoom=VIEW.zoomTo`) — 안 그러면 보간이 남아 화면이 튄다.
- **검증법**: 연출을 고르고 세션을 시작한 뒤 새로고침해 `VIEW.tilt` 를 잰다. 엄폐는 1.48, 나머지 셋은 0.06 이어야 한다. 화면으로 보려면 `sceneBodies(geom())` 의 세로/가로 비를 재면 된다 — edge-on 0.15 안팎, 어긋나면 0.65 안팎이다.

**놓친 완료 알림은 종 버튼에서 다시 본다.** 팝업이 7초 뒤 저절로 닫히므로 자리를 비우면 무엇을 받았는지 알 길이 없었다. `S.notif`(최신순, `NOTIF_MAX`=50)에 남기고 `S.notifSeen` 이후의 것을 읽지 않음으로 본다 — 있으면 종에 금색 점이 켜진다. 열면 모두 읽음이 된다. `log`·`ses` 와 마찬가지로 최상위 값이라 `fingerprint` 에 안 들어가고, 그래서 동기화 창을 띄우지 않는다. 완주할 때 `pushNotif()` 로 남기고 `paintInbox()` 로 점을 갱신한다 — **부팅과 계정 전환에서도 불러야** 자리를 비운 사이 쌓인 것이 보인다.

**완료 팝업은 모달이 아니다.** `showTokenPopup()`은 7초 뒤 자동으로 닫히며 그 동안 타이머가 계속 흐른다(포모도로는 휴식이 즉시 시작된다). 토큰은 팝업과 무관하게 이미 지급돼 있고 확인 버튼은 조기 닫기일 뿐이다. 이걸 블로킹 모달로 되돌리면 요구사항이 깨진다.

**`confirm()` / `alert()` 를 쓰지 말 것.** 홈 화면에 추가한 PWA나 일부 모바일 웹뷰에서 조용히 차단되어 `false`를 돌려준다 — 버튼이 먹통으로 보이는 원인이다. 확인이 필요하면 `await ask(제목, 설명, 버튼라벨, danger)`를 쓴다. 확인창은 다른 모달 위에 겹치므로 z-index가 더 높고 배경 막을 옅게 깐다.

**HUD 컨테이너는 두 글자 클래스를 쓴다 — 새 클래스 이름이 겹치지 않는지 볼 것.** `.tl` `.tr` `.bl` `.br` 이 네 모서리 컨테이너이고 `.hud` 와 함께 붙는다. 집중 기록의 24시간 띠에 `.tl` 을 붙였다가 **좌상단 시계 패널이 `position:fixed` 와 높이를 빼앗겨 시각이 반쯤 잘렸다.** 같은 특정도(클래스 하나)라 나중에 선언한 쪽이 이긴다. 증상이 "엉뚱한 위젯이 깨졌다" 로 나타나서 원인을 찾기 어렵다 — 새 클래스는 화면 이름을 접두어로 붙일 것(`.recTl` 처럼). `.sm` `.num` `.lb` `.day` 같은 짧은 이름도 후보를 먼저 `grep` 할 것.

**회전한 요소는 클릭 영역도 회전한다.** 다이얼의 진행률 SVG가 `rotate(-90deg)` 되어 있어 히트 영역이 원래 박스 밖으로 삐져나와 위쪽 입력창을 덮었다. 장식용 요소에 `transform`을 걸면 `pointer-events:none`을 함께 줄 것.

**가챠 연출은 화면 전체를 쓰는 망원경 시야다**(`scopeStart`). 모달이 아니라 그 위에 덮는 `#scopeFx` 이고, 결과 카드 모달(`mPull`)은 없앴다 — 발견한 천체를 시야 안에서 바로 보여 준다.

- **박자 여섯.** 시야 열림(0~.10) · 스캔(.10~.30) · 판정(.30~.58) · 확인(.58~.70) · 줌인(.70~.90) · 발견(.90~1). 성단은 늘 같은 것(M13)을 본다.
- **등급은 확인 순간에만 실린다.** 표적 수(3~6)·전체 길이(4.2초)·성단이 모두 같아야 마지막까지 등급이 새지 않는다. **개수나 길이로 드러내지 말 것.** ★3은 조용한 초록, ★4는 보라로 바뀌며 충격파 하나, ★5는 금색·충격파 셋·흔들림이다(`SCOPE_TIER`).
- **확인 단계에서 먼저 조준을 붙이고 그다음 배율을 올린다.** 중심이 어긋난 채 배율을 올리면 그 어긋남이 그대로 증폭돼 표적이 화면 밖으로 밀린다.
- **성단의 별에 후광을 그리지 말 것.** 점마다 빛이 번지면 관측 사진이 아니라 애니메이션처럼 보인다(사용자 지적). 밝기는 크기와 알파로만 낸다.
- **줌이 시작되면 브래킷은 물러난다.** 끝까지 남으면 천체를 가로막는다. 진 표적은 문구 없이 그냥 꺼진다.
- **섬광은 표적 둘레에만.** 화면 전체를 덮으면 성단이 묻힌다.
- 발견한 천체는 **도감과 같은 `drawGlobe` 로 천천히 자전한다**(`scopeGlobe`, 0.16 rad/s). 320px 에 구워 확대하며 합성할 때 `globeFit()` 을 곱한다 — 안 곱하면 가장자리에 링이 남는다.
- **아무 곳이나 누르면** 진행 중에는 건너뛰기, 발견 화면에서는 닫기다. 별도 버튼이 없다.
- **10연차는 연출을 한 번만** 탄다(가장 높은 등급의 천체로). 닫으면 `mMulti` 요약이 뜬다.
- 연출 중에는 `pulling`이 참이라 뽑기 버튼이 잠긴다. 새 연출을 시작할 때 `cancelAnimationFrame(_scopeRaf)`로 이전 루프를 반드시 끊어야 한다 — 안 끊으면 연타 시 여러 루프가 겹쳐 돈다.

**전체 초기화는 클라우드까지 지워야 한다.** 로컬만 지우면 로그인 상태에서 다음 부팅에 `cloudSyncOnBoot()`가 되살려 초기화가 안 된 것처럼 보인다. 빈 상태를 만든 뒤 `cloudPush(true)`로 덮어쓴다.

**계정은 선택 기능이다.** `CLOUD.url`/`CLOUD.key`가 비어 있으면 계정 메뉴가 "미설정"으로 뜨고 앱은 완전히 오프라인으로 돈다. 이 성질을 깨지 말 것 — 서버가 죽어도 게임은 돌아가야 한다. 로그인하면 저장 키가 `stellarFocus.v1.<id>`로 갈라져 한 기기에서도 계정별 항성계가 분리된다. 서버 접근은 `supabase/schema.sql`의 RPC 함수로만 하며 테이블은 RLS로 잠겨 있다(정책을 만들지 않는 것이 곧 차단이다). publishable 키는 공개되어도 되는 값이다. 로컬과 클라우드에 서로 다른 데이터가 있으면 `adoptCloud()`가 사용자에게 선택을 받는다 — 조용히 덮어쓰지 말 것.

**비교는 `canon()`으로 표준형을 만든 뒤 한다.** 서버에 있는 데이터는 예전 스키마라 필드가 없거나 키 순서가 다를 수 있다. 날것끼리 `JSON.stringify` 로 비교하면 늘 "다르다"가 나와 **부팅할 때마다 동기화 창이 뜬다** — `star.namePos` 를 추가했을 때 실제로 그랬다. `canon()`이 키를 정렬하고 없는 필드와 `null` 을 같게 보며, 보유 목록은 정렬해 순서를 지운다. **`S` 의 지속 필드를 추가·변경할 때 이 지문이 여전히 성립하는지 확인할 것.**

**집중 기록은 `stats` · `log` · `ses` 셋으로 나뉜다.** `stats.dayMs` 는 오늘치만 들고 있어 하루가 바뀌면 사라진다. 며칠 집중했는지·연속 며칠인지를 보려면 `log`(날짜별 밀리초)가 필요하고, **24시간 띠를 그리려면 `ses`(날짜별 세션 구간)가 필요하다** — `log` 는 총량만 알아 언제 했는지를 모른다. 완주할 때 셋 다 갱신하며, 값이 어긋나면 화면이 서로 다른 숫자를 보여 준다.

- **`ses` 는 `{'YYYY-MM-DD': [[시작분, 길이분], …]}`.** 시작분은 자정부터의 분(0~1439). 세션 하나가 숫자 둘이라 400일치가 20KB 남짓이다. `cleanSes()`가 `log` 와 **같은 기간(`LOG_DAYS`)**만 남겨 둘이 어긋나지 않게 한다.
- `pushSession()`이 완주 시각에서 길이만큼 거슬러 올라가 시작으로 삼는다. 자정을 넘긴 세션은 **시작한 날**에 남고 24시에서 잘린다. 일시정지가 있었으면 실제 벽시계 구간보다 짧지만 집중한 길이는 맞다.
- **합칠 때는 총량이 큰 쪽 하루를 통째로 쓴다**(`mergeSes`). 구간을 섞으면 같은 세션이 두 번 들어가고, 총량 기준이라야 `mergeLog` 와 결과가 어긋나지 않는다.
- **`ses` 가 생기기 전에 쌓인 날은 띠가 빈다.** 총량·세션 수는 정상으로 나오므로 "시간대 기록이 없습니다"로 갈라 보여 준다.

- **무한정 쌓이게 두지 말 것.** 저장 키 하나에 들어가고 클라우드로도 매번 올라간다. `cleanLog()`가 최근 `LOG_DAYS`(400)일만 남긴다.
- **날짜 키는 모양만 봐서는 안 된다.** `/^\d{4}-\d{2}-\d{2}$/` 는 `2026-13-99` 를 통과시킨다. `validDay()`가 실제 `Date` 로 만들어 되돌려 보고 같은 문자열이 나오는지 확인한다(윤년도 이걸로 걸러진다).
- **`log` 와 `ses` 는 최상위 값이라 `fingerprint` 에 들어가지 않는다.** `star` 아래에 넣었으면 부팅할 때마다 동기화 창이 떴을 것이다.
- **어느 항성계를 쓸지 골라도 기록은 잃지 않는다.** `adoptCloud` 의 두 갈래가 `keepRecord()` 로 양쪽 `stats`·`log`·`ses` 를 합친다 — 항성계를 하나 고른 대가로 몇 달치 기록이 사라지면 안 된다. 같은 날은 큰 쪽을 쓴다.
- 기록은 **좌상단 날짜·시각을 눌러** 여는 `mRecord` 에 뜬다. **로그인 여부와 무관하게** 보여 준다 — 기록은 로컬에 쌓이고 로그인은 그것을 기기 간에 잇는 것뿐이다. 화면에는 **숫자와 이름표만** 둔다(설명 문구·부제 없음). 여기에 안내를 다시 붙이지 말 것.
- **화면이 둘이다.** `renderRecordDay()`(하루)와 `renderRecordCal()`(달력). 머리의 `←` 는 달력에서는 하루 보기로, 하루 보기에서는 창을 닫는다. **달력 화면에서는 달력 버튼을 감춘다**(`.hide`) — 이미 그 화면이다.
  - **하루 보기**: 24시간 띠(1시간 격자·6시간마다 진하게, 블록이 반투명이라 격자가 비쳐 보인다) + `누적 집중`·`완주한 세션` **그 날 것** + 날짜 넘기기. 블록을 누르면 시작~종료 시각이 뜬다. 클래스는 `.recTl*` 다 — **`.tl` 로 짓지 말 것**(아래 항목).
  - **달력**: 오늘은 **노란 테두리**, 집중한 날은 칸이 그 달 최대치 대비 비율만큼 차오르고 분이 찍힌다. 하단에 `이 달 집중`·`전체 집중`·`연속 기록` 셋. `이 달` 만 달을 넘길 때 따라 바뀐다.
  - **전체 누적 4칸과 최근 14일 막대는 뺐다**(사용자 요청). 되살리지 말 것 — 하루 보기는 그 날 숫자만, 전체는 달력 하단만 본다.
- 시계 위젯 안의 **계정 버튼은 누르는 영역 밖에 둔다**(`#clockTap` 이 날짜·시각·오늘 집중만 감싼다). 위젯 전체를 누르게 하면 계정 버튼과 싸운다.
- **집중 중에는 잠긴다**(`openRecord()` 가 `isLocked()` 를 본다). 예전에는 읽기 전용이라 열어 뒀지만 사용자 요청으로 막았다. `guard()` 의 공용 문구는 '설정을 변경할 수 없습니다'라 이 화면에 맞지 않으므로 문구를 따로 낸다.

**기록(`stats`)은 정체성에서 뺀다.** 기록이 조금 어긋났다고 "어느 항성계를 쓸까요?" 를 띄우는 건 과하다. 지문이 같으면 `mergeStats()`로 큰 쪽을 살려 합치고 조용히 올린다. 타이머 런타임 상태도 제외한다(기기마다 다르므로).

**동기화 창은 "정말 갈라졌을 때"만 떠야 한다.** 판단 순서가 정해져 있다.

1. 지문이 같으면 → 기록만 합치고 끝.
2. `updatedAt`(로컬 저장마다 갱신)과 기기 로컬의 `syncedAt`(마지막 성공 시각)을 비교해, 한쪽에만 새 변경이 있으면 그쪽을 조용히 채택한다. 한 기기만 쓰면 여기서 끝난다.
3. 시각을 못 믿으면(옛 기록엔 `updatedAt` 이 없다) `dominates()`로 진행도를 본다. 보유 천체 수와 세션 수는 되돌아가지 않는 값이라, 한쪽이 둘 다 크거나 같고 하나라도 앞서면 그쪽이 뒤쪽을 포함한다 — 잃을 게 없으니 물어볼 이유도 없다. **토큰은 뽑으면 줄어서 진행도가 못 된다.**
4. 그래도 못 정하면 그때만 `adoptCloud()`가 묻는다. 창에 `diffFields()`로 **무엇이 달라서 묻는지** 표시한다.

`adoptCloud`의 두 갈래는 **둘 다 푸시해야 한다.** 클라우드를 골랐을 때 올리지 않으면 서버엔 옛 스키마가 그대로 남아 다음 부팅에 또 어긋난다.

**푸시는 4초 디바운스라 앱을 나가면 날아간다.** 모바일은 화면을 벗어나는 순간 프로세스가 얼어붙어 예약된 `setTimeout` 이 실행되지 않는다. 클라우드가 뒤처지면 다음 부팅에 동기화 창이 뜬다. `flushPush()`가 `visibilitychange`(document 에서 난다)와 `pagehide` 에서 밀린 것을 즉시 밀어 넣는다.

**관리자 계정.** `ADMIN_IDS`에 든 아이디로 로그인하면 토큰 무제한(`∞` 표시), 집중 시간 1분부터 설정 가능(`limMin()`), 천체 종류별 도감 초기화가 열린다. **클라이언트 판정이라 진짜 권한 통제가 아니다** — 테스트 편의 기능이다. 도감을 초기화할 때는 보유 목록만 지우지 말고 궤도 배치도 함께 해제할 것(`resetOneDex`). 안 그러면 유령 천체가 남는다.

**localStorage 는 접근만 해도 예외를 던질 수 있다.** 시크릿 모드나 저장소 차단 환경이 그렇다. 반드시 `lsGet` / `lsSet` / `lsDel` 래퍼로만 접근한다.

**저장 스키마.** 키 하나에 전체 상태를 넣는다. `load()`가 방어적 복구를 한다 — 존재하지 않는 천체 id 제거, 슬롯 배열 길이 보정, 행성 없는 슬롯의 위성 제거. **풀에서 id를 바꾸거나 지우면, 그리고 슬롯 구조를 바꾸면 기존 사용자 데이터가 깨진다.** `load()`의 복구 로직을 함께 갱신하거나 키 버전을 올릴 것.

**집중 연출이 이 앱에서 가장 비싼 한 곳이다.** 태블릿에서 발열 보고를 받고 재보니, 8.3M 픽셀에서 한 프레임 116.6ms 중 **94.3ms 가 연출**이었다. 벨트·천체·궤도선은 모두 측정 오차 수준이었다 — **짐작으로 벨트를 의심하지 말 것.** 연출은 흐림·복사·모눈·노이즈가 전부 전면적 연산이라 비용이 픽셀 수에 그대로 붙는다.

- 그래서 **연출 중에는 백킹스토어를 `FX_PIXEL_CAP`(2.6M) 으로 묶는다**(`targetDPR()`). 큰 태블릿 기준 6.4M → 2.6M 로 59% 준다. 폰은 원래 그 아래라 그대로다. `syncFxRes()` 가 연출이 켜지고 꺼질 때만 `resize()` 를 부른다.
- 관측 장비 화면이라 배경이 원래 흐리고, 선명해야 하는 브래킷·수치는 흐림 **뒤에** 그려지므로 해상도를 낮춰도 눈에 띄는 손해가 거의 없다.
- `applyScopeOptics` 는 예전에 매 프레임 화면 전체를 복사했다. 살릴 곳은 작은 사각형 몇 개뿐이라 그 영역만 뜨도록 바꿨고, 흐림도 저해상도(`OPT_MAX`)로 만들어 늘린다.
- **내부를 더 쪼개 재려 하지 말 것.** 광학·모눈·노이즈·글자·표적을 하나씩 꺼 봤지만 전부 잡음에 묻혔다. 전면적 연산이 여럿이라 하나만 꺼도 나머지가 그대로다 — 픽셀 수를 줄이는 것이 유일하게 효과가 확실한 손잡이다.

**태블릿 성능은 이렇게 재현한다.** 폰·태블릿에서 렉이 보고되면 화면을 키우고 `DPR` 을 올려 픽셀 수부터 맞춘다. `resize()` 는 `DPR` 을 2 로 제한하지만, 재현 시에는 더 올려 병목을 드러내는 것이 편하다.

```js
DPR=3; cv.width=W*3; cv.height=H*3; ctx.setTransform(3,0,0,3,0,0);
```

화면이 클수록 **화면 전체를 훑는 연산**이 먼저 터진다. 지금까지 걸린 것: 카이퍼 벨트의 전체 화면 경계상자, 매 프레임 전체 화면 `drawImage`, 궤도선 21 개의 큰 경계상자. JS 계산량이 아니라 **칠해지는 면적**을 먼저 의심할 것.

**반응형 분기점 760px이 CSS와 JS 양쪽에 있다.** `geom()`이 `W < 760`일 때 항성계 중심을 위로(`H*0.32`) 올리고 반경을 줄여 좌하단 타이머 위젯을 피한다. CSS `@media (max-width:760px)`도 같은 값이다. 한쪽만 바꾸면 폰에서 위젯이 중심별을 덮는다.

**설정 화면의 설명은 ⓘ 로 접는다.** 행마다 설명을 깔면 문구가 화면을 채워 정작 토글이 안 보인다. `INFO[키]` 에 HTML 을 담고 `<button class="iBtn" data-info="키">` 를 제목 옆에 두면 `bindInfo(root)` 가 연결한다. `SETTINGS` 의 `d` 가 그대로 설명이므로 문구는 한 군데에만 있다.

- 풍선(`#infoPop`)은 `position:fixed` 라 **모달 본문이 스크롤돼도 따라오지 않는다.** 스크롤·리사이즈·바깥 누름에서 닫는다. 스크롤 감시는 **캡처 단계**(`addEventListener('scroll',…,true)`)로 걸 것 — 모달 본문의 스크롤 이벤트는 window 까지 올라오지 않는다.
- ⓘ 는 설정 행 안에 있으므로 클릭에서 **`stopPropagation` 을 반드시 할 것.** 안 하면 설명을 보려다 설정이 토글된다.
- 열기 전에 `left/top` 을 0 으로 되돌리고 재야 폭이 제대로 나온다. 이전 위치가 화면 끝이면 잘못 잰다.
- 상태 문구는 접지 않는다 — `완료 알림` 행의 "차단됨 · 브라우저 사이트 설정에서 풀어야 합니다" 는 설명이 아니라 지금 상태다.

**설정 화면의 행 핸들러는 `[data-k]` 로 좁혀 잡는다.** `renderSettings()`가 `$$('#setBody .setRow')` 를 통째로 훑으면 알림 행(`#rowNotif`)까지 잡혀 `renderPwaRows()`가 건 핸들러를 덮어쓴다. 그러면 권한 요청은 일어나지 않고 `S[undefined]` 만 토글되어, **켠 것처럼 보였다가 다시 들어가면 꺼져 있다.** 실제로 그 버그가 있었다. 설정에 특수한 행을 추가할 때 이 규칙을 지킬 것.

**`ask()` 는 밖에서 취소할 수 있어야 한다.** 버튼으로만 풀리게 두면 뒤로가기 등으로 창이 닫혔을 때 약속이 영원히 안 풀리고, `onBack` 의 `_exiting` 이 참으로 굳어 **그때부터 뒤로가기가 통째로 죽는다.** `_askRes` 로 취소 통로를 남기고 `_exiting` 은 `finally` 로 푼다. 확인창이 떠 있는 동안에도 덫을 깔아 둬야 그 사이 뒤로가기가 앱 밖으로 나가지 않는다.

**뒤로가기가 안 먹는다는 보고를 받으면 설정 맨 아래 진단줄부터 확인할 것.** `버전 · 뒤로가기 준비됨/미설정 (히스토리 길이) · 앱/홈화면/브라우저` 가 표시된다. "미설정"이면 덫이 사라진 것이고, "준비됨"인데도 반응이 없으면 그 환경이 `popstate` 를 안 주는 것이다. 참고로 **열린 창이 있으면 뒤로가기는 그 창부터 닫는다** — 동기화 창처럼 뭔가 떠 있었다면 첫 번째 뒤로가기는 종료를 묻지 않는다.

**덫은 반드시 사용자 조작 중에 깔아야 한다.** Chrome 은 사용자 조작 없이 `pushState` 로 만든 항목을 **건너뛸 수 있는 것으로 표시한다**(history manipulation intervention — 뒤로가기를 가두는 사이트를 막으려는 장치). 부팅하자마자 깔면 뒤로가기가 그 항목을 건너뛰어 **앱이 그대로 닫힌다.** 실제로 그래서 안 먹었다. `pointerdown`/`touchstart`/`keydown`/`click` 에서만 깐다. **`pageshow` 나 `visibilitychange` 로 깔면 안 된다** — 조작 없이 만든 항목이 하나라도 생기면 `trapArmed()` 가 참이 되어, 정작 조작 때 제대로 된 항목을 못 깐다.

**뒤로가기는 히스토리 덫으로 받는다.** 부팅 때 `pushTrap()`이 항목을 하나 밀어 넣고 `popstate`에서 `onBack()`이 받는다. 열린 모달이 있으면 그것부터 닫고, 촬영 모드면 빠져나오고, 아무것도 없을 때만 `ask()`로 종료를 묻는다. **처리할 때마다 덫을 다시 깔아야** 다음 뒤로가기도 받을 수 있다. `pushTrap()`은 이미 깔려 있으면 겹쳐 쌓지 않고(`trapArmed()`), `visibilitychange`·`focus`·`pageshow` 에서도 다시 깐다 — 앱을 오래 떠나 있으면 브라우저가 히스토리를 정리하는 경우가 있다. Capacitor 가 있으면 `App.backButton` 도 같은 `onBack()` 으로 보낸다. 종료를 고르면 `exitApp()`이 밀린 동기화를 즉시 밀어 넣은 뒤 Capacitor `App.exitApp()`을 시도한다 — 브라우저는 스크립트로 창을 닫지 못하는 경우가 많아 그때는 안내만 한다.

**`APP_VER`은 설정 화면 맨 아래에 표시된다.** 사용자가 어느 빌드를 쓰는지 확인하는 유일한 수단이다. 배포했는데 증상이 그대로라는 보고를 받으면 먼저 이 값을 물어볼 것 — 서비스워커나 HTTP 캐시 때문에 옛 파일을 쓰고 있을 수 있다. **동작을 바꾸는 배포에서는 이 값을 올릴 것.**

**호버로 버튼을 움직이지 말 것.** 커서가 스칠 때마다 화면이 들썩인다 — 사용자가 빼 달라고 했다. `:hover` 에서는 색·테두리만 바꾸고, 자리 이동이나 크기 변화는 **누를 때**(`:active`)만 준다. 색상환의 색 조각(`.sw`)은 예외로 두었다 — 버튼이 아니라 고르는 대상이라 살짝 커지는 것이 "고를 수 있다"는 신호다.

**HUD 4모서리 배치는 제품 요구사항이다.** 사용자가 직접 스케치한 배치이므로 임의로 옮기지 말 것.

- 좌상단: 날짜/시각 위젯 (계정 버튼이 그 안에 들어 있다). **날짜·시각을 누르면 집중 기록**이 뜬다
- 우상단: **알림 종 + 토큰 수**(같은 줄) + 세로 메뉴 스택 — 가챠 · 항성계 · 도감 · 카메라 · 설정
- 좌하단: 타이머 위젯 (모드는 탭, 오른쪽에 사운드/접기)
- 우하단: Face-on · Edge-on · 확대축소

**메뉴 아이콘 두 개는 사용자가 직접 그려 준 모양이다.** 임의로 바꾸지 말 것.

- **항성계**는 사용자가 1024px 로 그린 그림을 좌표째 옮긴 것이다(교차하는 두 궤도 + 노란 별 + 빨강·하늘색 행성). **원본을 그대로 넣으면 안 된다** — 궤도의 장단축비가 9% 라 19px 로 줄이면 선이 안을 메워 덩어리가 된다. 세로만 2.4배 늘리고 궤도 두께를 1.5배로 열었다. 비대칭 배율이라 기울기도 함께 바뀐다: `atan(tan θ · 2.4)`. 별·행성의 상대 위치와 크기 비율은 원본 그대로다. 색은 `currentColor` 다 — 그림의 원래 색(파랑 궤도·노란 별)을 박아 넣었더니 이 버튼만 호버 색이 안 따라와서 되돌렸다. 모양은 그림 그대로, 색만 나머지와 같이 간다.
- 메뉴 버튼의 호버 색은 다섯이 모두 다르다: 가챠 보라(`--acc-2`) · 항성계 노랑(`--gold`) · 도감 흰색+발광 · 카메라 하늘색(`--acc`) · 설정 회색(`#aab0ba`).
- **도감과 설정은 둘 다 무채색이라 밝기만으로는 구분되지 않는다.** `.mBtn:hover` 의 기본 색이 이미 `#e8eefc`(거의 흰색)여서 둘이 그 근처에 몰리기 때문이다. 축을 하나 더 써서 갈랐다 — 도감에만 `drop-shadow` 발광을 주고 설정은 평평하게 낮췄다.
- **회색을 더 어둡게 하고 싶어도 기본색보다 어두워지면 안 된다.** 마우스를 올렸는데 오히려 흐려져 호버로 안 읽힌다. `#9aa2ad` 까지 내려 봤다가 상대 휘도를 재서(0.356 대 기본 0.381) 되돌렸다. 지금 값은 0.431 로 기본보다 밝고 도감(1.0)과는 확실히 벌어진다. **색을 눈으로만 고르지 말고 휘도를 계산해 볼 것.**
- **설정**은 톱니바퀴. 예전 것(원 + 방사선)은 해·밝기 아이콘처럼 보였다. 경로는 8개 톱니를 계산해 뽑은 것이라 손으로 좌표를 고치지 말고 다시 생성할 것.
- **손그림을 아이콘에 넣을 때는 19px 로 줄여 먼저 볼 것.** 선 굵기가 그림 폭의 6~8% 는 돼야 살아남는다. 120px 에서는 손그림 특유의 흔들림이 보이지만 19px 에서는 반듯한 도형과 구분되지 않는다 — 확인해 봤다.

메뉴 접기 버튼은 메뉴 스택과 **가로 폭이 같은 납작한 알약**이고, 접혀 있으면 아래쪽(펼치기) 펼치면 위쪽(접기)을 가리킨다. 타이머 위젯을 접으면 **남은 시간과 펼치기 버튼만** 남는다(`.tMini`) — 모드 탭까지 남기면 접은 의미가 없다. 우하단 버튼은 좁은 화면에서 타이머 위젯과 겹치지 않는 크기여야 한다(Face-on 86×29, 줌 27×24).

**처음 쓰는 사람을 위한 안내(`showHint`)는 지금 아무도 부르지 않는다.** 어디를 눌러야 하는지 알려 주던 문구 셋(중심별 생성 직후 · 부팅 시 가챠 권유 · 부팅 시 배치 권유)을 뺐다 — 첫날에만 잠깐 보이는데다, **어떻게 드러낼지 다시 정하기로 했다.** 틀(`#hint` + `showHint()`)은 남아 있으니 부르기만 하면 되살아난다. 죽은 코드로 보고 지우지 말 것.

- **되살릴 때 위치부터 확인할 것.** 화면 아래 한가운데(바닥에서 96px, 좁은 화면 84px)라 **좌하단 타이머 위젯에 앞부분이 가린다** — 창 폭 680px 에서 50px, 폰 폭에서는 그보다 더 잘렸다. 1280px 에서는 겹치지 않는다.

**타이머 위젯의 안내 문구는 `#stTxt` 한 줄뿐이다.** 사용자가 빼 달라고 한 것들이다 — `집중 중`·`대기 중`, `집중 중 · 메뉴가 잠겨 있습니다`, `가챠에서 행성을 얻어 배치해 보세요`, 그리고 마지막으로 **`가장 바깥 행성이 1바퀴 돌면 완료`·`궤도 주기 = N분 · 행성 N기`**. 숫자가 줄고 버튼이 `⏸ ■`면 집중 중인 것을 이미 안다.

- **`#tNote` 는 통째로 지웠다**(마크업·CSS·`updateTimerUI` 의 갱신까지). 되살리지 말 것. 궤도 규칙은 이 문서와 `docs.html` 이 설명할 일이지 타이머 위젯이 매 순간 되뇔 일이 아니다.

- **`휴식 중`·`긴 휴식 중`·`일시정지`·`집중 N회차`는 남긴다.** 그건 화면 어디에서도 알 수 없는 정보다.
- 내용이 없으면 `display:none` 으로 아예 감춘다. 빈 칸이 자리를 차지하면 다이얼이 어긋나 보인다.

**타이머 위젯은 탭 · 본문 · 조작 줄 세 층이다.** 사용자가 직접 그린 배치이므로 임의로 바꾸지 말 것.

- **위쪽 줄은 두 탭이 반씩 나눠 갖는다.** 활성 탭은 바탕이 투명하고 아래 테두리가 없어 본문과 한 덩어리로 이어지고, 비활성 탭은 어둡게 깔리고 아래 테두리가 남아 뒤로 물러난다 — 브라우저 탭과 같은 읽기다.
- **다이얼 원은 없다.** 진행률 링을 빼고 그 자리를 숫자가 쓴다. 되살리지 말 것.
- **카운트다운은 큰 숫자 하나 + 프리셋 3개**(`PRESETS`=10·45·60). 프리셋은 **폭을 채우지 않고 가운데로 모은 작은 알약**이다(35×17px · 간격 18px) — 큰 숫자가 주인공이라 프리셋이 눈을 끌면 안 된다. **포모도로는 집중·휴식 두 칸**이고 그 아래 `긴 휴식` 한 줄(체크 + 분 + 회마다)이 붙는다. 긴 휴식은 사진에 없었지만 **포모도로 보너스 토큰이 `useLong`·`every` 를 직접 읽으므로** 빼면 그 규칙이 죽는다.
- **시작하면 프리셋·최소값 안내가 접히고 그만큼 숫자가 커진다**(`#timerPanel.run`, 0.42초). `max-height` 로 접어야 transition 이 걸린다 — `display:none` 은 애니메이션이 안 된다. 포모도로는 진행 중인 단계만 크게, 반대쪽은 흐려진다.
- **숫자를 눌러 고친다.** 평소에는 표시, 누르면 그 자리가 입력창(`.bigIn` / `.pomoIn`)으로 바뀐다(`_editing` 이 `'count'|'focus'|'brk'|null`). `blur` 만 믿지 말고 `change` 에서도 닫을 것 — 환경에 따라 `blur` 가 안 온다. **편집 핸들러는 `LIMITS` 루프보다 뒤에 걸어야** `validateField` 가 먼저 값을 확정한다.
- **조작 줄은 구분선 아래에 있다.** 왼쪽 `▶`(시작·일시정지·이어서) 와 `■`(종료), 오른쪽 `🔔`(알림 소리) 와 `—`(접기). **`■` 는 세션이 있을 때만 나타난다** — 대기 중에는 되돌릴 것이 없어 예전 `초기화` 는 빈 동작이었다. 폭 0에서 펼쳐진다.
- **아이콘은 전부 인라인 SVG(`TICO`)다.** 글자 아이콘은 폰트 대체에 따라 기기마다 모양이 달라진다. 완료 소리는 **스피커**다 — 종으로 그리면 우상단 알림 버튼과 같은 그림이 되어 구분이 안 된다. 끄면 **사선을 긋는다**(밝기 하나로만 갈리면 켠 건지 끈 건지 애매하다).
- **`■` 는 반드시 한 번 묻는다.** 묻는 동안 타이머를 세워 두고, 취소하면 `endAt` 을 다시 못박아 이어 간다 — 물어보는 사이에 몇 초를 손해 보면 안 된다.
- 최소값 안내는 `#tHint` **한 곳에만** 둔다. 모드에 따라 문구가 달라진다.
- `.longRow` 의 입력칸은 **스피너를 꺼야 한다.** 안 끄면 그 폭만큼 먹혀 두 자리 숫자가 잘리고, `white-space:nowrap` 이 없으면 `회마다` 가 두 줄로 접힌다. 288px 안에 들어가야 한다.

**집중 중 전체 보기(`#vbFit`)는 배율만 맞춘다.** `resetView()`는 기울기·방위각까지 되돌려 Face-on 잠금을 깨고 화면이 돌아간다.

**글자 아이콘(`⤢` 등)을 쓰지 말 것.** 폰트 대체에 따라 기기마다 모양이 달라진다 — 모바일에서 전체 보기 아이콘이 다르게 보인다는 보고가 그 원인이었다. 인라인 SVG 로 고정한다.

**도감은 최상위 메뉴다.** 예전에는 가챠 창 안의 토글이라 사용자가 찾지 못했다. 다시 숨기지 말 것.

**텍스처 굽기는 절대 통짜로 돌리지 말 것.** 절차적 텍스처 한 장이 `PROC_W*PROC_H`(512×256) 픽셀짜리 동기 루프다. 그대로 돌리면 그 시간 내내 프레임이 통째로 멈춘다 — 폰에서 천체 하나당 0.5초 이상이고, 항성계가 다 차면 그런 게 10번 넘게 온다. `bakeSlice()`가 매 프레임 `BAKE_MS`(5ms)만큼 위도 줄만 채우고 넘긴다. 총 시간은 같지만 화면이 계속 돈다.

- 새 코드에서 `makeProcTexture()`(통짜)를 직접 부르지 말 것. `readyTexture(b, 화면반지름)`로 예약하고 준비될 때까지 null 을 받아 넘어가야 한다. 실제로 `pumpSprites()`가 이걸 어겨서 시간 분할을 우회하고 있었다 — 썸네일 하나 구울 때마다 200ms씩 멈췄다.
- 큐는 `_texPri`(화면상 반지름)로 정렬한다. 중심별이 절차적인데 위성부터 구우면 눈에 띈다.
- **텍스처는 쓰일 크기별로 굽는다.** `PROC_TIERS`(160×80 / 320×160 / 512×256)에서 `procTier(px)`가 고르고, 캐시 키는 `id@단계`다. 경도 180도가 화면 px 만큼의 텍셀로 덮이면 1:1 → 폭 ≈ 지름×2. 74px 썸네일에 512폭을 구우면 16배를 버리는 셈이고, 그게 도감이 느리게 채워지던 이유였다. `readyTexture(b, 화면지름)`에 **반지름이 아니라 지름**을 넘길 것.
- 요청한 단계가 아직이면 `bestProc()`이 이미 구워둔 더 작은 단계를 내준다. 빈 화면 대신 흐린 그림으로 버티다 올라간다.
- **모달은 전면을 덮고 blur(10px)까지 걸린다.** 그 뒤 궤도를 60fps로 그릴 이유가 없어 세 프레임에 한 번만 그리고(`hidden()`), 굽기 예산을 5ms→13ms로 올린다. 도감 채우는 시간이 2.6초에서 0.47초로 줄었다.
- 스프라이트 큐(`spriteSlice`)도 프레임 예산 안에서 여러 장씩 처리한다. 한 장마다 `setTimeout`을 걸면 타이머 지연만으로 몇 초가 샌다. 텍스처가 아직이면 예약만 하고 뒤로 미루되, 큐를 한 바퀴 다 돌아도 진전이 없을 때만 한 프레임 쉰다 — 고정 지연을 주면 그게 직렬로 쌓인다.
- 성능을 의심할 땐 프레임 간격 히스토그램을 볼 것. 중앙값은 멀쩡한데 33ms 넘는 프레임 몇 개가 버벅임의 정체인 경우가 많다.
- **`render()` 안을 JS 타이머로 재면 절반만 보인다.** `ctx.fill()`/`stroke()` 는 명령만 기록하고 래스터화는 뒤에서 일어난다. 실제로 `drawBelt` 는 JS 로 1.4ms 였는데 통째로 끄면 6.7ms 가 빠졌다. **부위를 하나씩 꺼 보고 rAF 간격을 재는 A/B 가 유일하게 믿을 수 있는 방법이다.**
- **비교는 반드시 한 페이지 안에서 번갈아 할 것.** 새로 고침 사이에는 이 기계에서만도 2ms 넘게 흔들린다 — 궤도 조각 수를 줄여 놓고 다시 로드했더니 오히려 1.2ms 느리게 나왔고, 같은 세션에서 번갈아 재니 0.4ms 빠른 것이 드러났다.

**촬영 모드(`body.capture`).** 카메라 버튼을 누르면 HUD가 사라지고 우하단에 **설정 · 촬영 · 나가기**만 남는다. 설정을 누르면 `#capPanel`이 열리고 그 안에 비율 키(전체/1:1/4:3/3:4/9:16/16:9) · 시점(Face-on·Edge-on·전체 보기) · 배치(일렬 정렬) · 표시 토글(별 이름·궤도선)이 들어 있다.

- `capRatio`가 `full`이 아니면 `captureRect()`가 잘릴 영역을 계산하고 `drawCaptureGuide()`가 바깥을 어둡게 덮고 삼분할선을 그린다. **가이드는 `render()` 맨 끝에서 그린다** — 천체보다 먼저 그리면 천체가 그 위에 얹혀 틀이 안 보인다.
- 촬영은 `_hideGuide=true`로 두고 `render(clock)`을 한 번 더 돌린 뒤 `drawImage`로 잘라낸다. 이 플래그를 빼먹으면 삼분할선이 사진에 찍힌다.
- 궤도선 토글은 `globalAlpha=0`이 아니라 **블록 자체를 건너뛴다**. `strokeStarRing`이 내부에서 `globalAlpha`를 덮어써서 동반성 궤도만 살아남기 때문이다.
- `fitZoom()`은 촬영 중이면 화면이 아니라 `captureRect()` 안에 맞춘다. 세로 비율에서 계가 틀 밖으로 삐져나가면 안 된다.
- 촬영 모드를 나가면 `showLabels`/`showOrbits`/`capRatio`를 되돌린다. 촬영용 토글이 평소 화면까지 따라가면 궤도선이 사라진 채로 남는다.
- `syncCapUI()`는 `updateViewUI()`에서 불린다(드래그로 시점이 바뀌어도 버튼 상태가 따라오도록). 부팅 중 먼저 불릴 수 있어 `#capRatios`가 비어 있으면 즉시 빠져나온다 — 안 그러면 `let capRatio` TDZ에 걸린다.
- **일렬 정렬(`capAlign`).** 모든 천체를 같은 황경 `ALIGN_LON`(π, 화면 왼쪽)에 놓는다. `alignRevs()`가 케플러 방정식을 거꾸로 풀어(진근점이각 → 이심근점이각 → 평균근점이각) 그 위치가 되는 `revs`를 낸다. 동반성은 `ALIGN_LON+π`에 둔다 — 같은 편에 두면 SEP_OUT(0.30)과 첫 궤도(a=0.30)가 정확히 겹친다. 궤도 경사(≤0.25rad)로 1도 미만 어긋나는 건 무시한다. 정렬 중에는 공전이 멈추므로 선의 화면 방향은 카메라 방위각 드래그로 정한다.
- **중심별 이름표.** `S.star.namePos`(화면 비율 좌표)가 있으면 자유 배치, 없으면 별 아래에 붙는다. **자유 배치는 사진 캡션이라 촬영 중에만 유효하다** — 평소 화면까지 따라가면 촬영 창을 나가도 이름이 엉뚱한 자리에 남는다(실제 보고). `drawStarName`이 `inCapture()`로 갈라 밖에서는 늘 별 아래에 붙인다. 보일지 말지도 두 갈래다: 촬영 중에는 촬영 패널의 `showLabels`, 평소에는 설정의 `S.showName`. `showLabels`는 촬영에 들어갈 때 `S.showName`에서 시작하고 나갈 때 되돌릴 것이 없다(밖에서는 읽지 않는다). `_nameBox`에 히트박스를 남기고, **촬영 중에만** `pointerdown`에서 시점 회전보다 먼저 가로챈다 — 평소에도 열어두면 탭 추적·시점 드래그와 싸운다. 자유 배치는 사진 캡션이라 15px + 자간을 준다.
- **촬영 중에는 탭 추적을 막는다.** `followBody`가 확대·이동을 따라오게 해서 잡아둔 구도가 날아간다. 선택 링도 `_hideGuide`면 그리지 않는다.
- `cv.toDataURL()` 결과를 모달에 띄우고 다운로드 링크를 건다 — 모바일에서 `<a download>`가 막히는 경우가 있어 이미지를 직접 보여줘 길게 눌러 저장할 수 있게 했다.

## 밸런스 수치

조정 요청이 들어올 만한 값들.

| 항목 | 상수 | 현재 |
|---|---|---|
| 가챠 확률 | `RATE` | ★5 4% / ★4 22% / ★3 74% |
| 천장 | `PITY4`, `PITY5` | 10회 / 30회 (배너별 독립) |
| 등급 분포 | 각 POOL | ★3 37 · ★4 31 · ★5 19 |
| 배치 상한 | `MAX_PLANET`, `MAX_MOON`, `MAX_COMP` | 7 / 행성당 2 / 2 |
| 수집 총량 | 각 POOL | 항성 29 · 행성 30 · 위성 28 (총 87) |
| 포모도로 보너스 | `onPhaseDone` | `useLong`이 켜진 경우 `every`회차마다 토큰 2개 |
| 최소 집중 시간 | `MIN_FOCUS`, `LIMITS` | 10분 (관리자는 1분, 휴식은 1분, 긴 휴식은 5분부터) |
| 카운트다운 프리셋 | `PRESETS` | 10 · 45 · 60분 |
| 기록 보존 기간 | `LOG_DAYS` | 400일 (`log`·`ses` 공통) |
| 알림 보관 개수 | `NOTIF_MAX` | 50개 |
| 완료 팝업 시간 | `POP_MS` | 7초 |
| 기본 시점 / 줌 한계 | `TILT_DEFAULT`, `ZOOM_MIN/MAX` | 0.90rad · 0.45~5 |
| 동반성 궤도 간격 | `SEP_IN`, `SEP_OUT` | 0.165 / 0.30 (기준 장반경 단위) |
| PIN 자릿수 / 잠금 | `schema.sql` | 4~6자리 · 10회 실패 시 15분 |

타이머 입력은 **자연수만** 받는다(`validateField`). 소수점이나 문자를 넣으면 경고 후 직전 값으로 되돌린다.

가챠는 **중복이 나오지 않는다.** 미보유 풀에서만 뽑고, 해당 등급이 소진되면 상위 등급부터 대체한다(`pull()`). 이건 사용자가 명시적으로 요구한 규칙이다. 회귀 검증법: 배너를 비우고 풀 크기만큼 연속 추첨하면 중복 없이 전량이 나와야 한다.

**천체는 전부 실존 천체다.** `st` 배열의 지름·질량·온도·거리는 실측값이므로 **임의로 바꾸지 말 것.** 각 항목은 `n`(한글명) `en`(영문/기호) `d`(가챠 카드용 한 줄) `dd`(도감용 4~5줄) `st`(제원 표)를 갖는다. 행성 배너에는 태양계 13종 외에 실제 외계행성 17종이 들어 있다 — 사진이 없어 관측된 질량·온도·조성에 근거해 그린다. 항성의 `m`은 질량중심 계산에 직접 쓰이므로 값을 바꾸면 궤도가 달라진다.

**도감 구체 뷰어.** `TEX_IDS`에 든 천체만 실제 텍스처를 쓴다. 파일명은 천체 id와 정확히 같아야 하며(`textures/<id>.jpg`) 정거원통도법 이미지여야 한다. 세로 방향은 `loadTexture` 가 맞춰 준다(위의 *텍스처의 0행은 남극이다* 참고). `drawGlobe()`가 픽셀 단위로 구면 매핑을 하는데 300×300에서 약 5 ms 걸린다 — 캔버스를 더 키우면 제곱으로 늘어나니 주의. 고리가 있는 천체는 구체를 절반 크기 오프스크린에 그린 뒤 앞뒤 고리 사이에 합성한다. **모달을 닫을 때 `stopGlobe()`를 반드시 부를 것** — 안 그러면 rAF 루프가 계속 돈다. Solar System Scope가 `_fictional`로 표기한 상상도는 실제 사진으로 오해될 수 있어 의도적으로 제외했다.

**절차적 천체 텍스처.** 실제 사진이 없는 74종은 `makeProcTexture()`가 정거원통도법 지도를 코드로 굽는다. 결과가 실제 텍스처와 같은 `{w,h,data}` 형식이라 같은 구면 뷰어를 그대로 타며, 그래서 사진 없는 천체도 드래그 회전이 된다. 최대 단계가 `PROC_W×PROC_H`(512×256)이고 `_procCache`에 `id@단계`로 캐시된다.

노이즈는 **구면 위에서 3D로 샘플링한다**(`vnoise3`/`fbm3`/`ridged3`). 2D로 하면 경도 0도에 이음매가 생기고 극에서 뭉개진다. `PROC_RECIPE`에 손으로 다듬은 천체(이오·타이탄·가니메데·트리톤)가 있고, 나머지는 `procGeneric()`이 `f` 플래그와 색으로 조립한다.

**옥타브 수와 문턱값은 함께 움직인다.** 옥타브를 바꾸면 fbm 출력 분포가 달라져 `sstep` 문턱이 엉뚱한 곳에 걸린다 — 이오와 가니메데를 6에서 5로 잘못 옮겼다가 표면이 뭉개진 적이 있다. 수식을 손댈 때는 렌더 결과를 눈으로 확인할 것.

**텍스처의 0행은 남극이다.** 이 프로젝트 전체가 그 규칙이다.

- `procRow`가 `py=0` 을 `lat=-pi/2` 로 굽고, `drawGlobe` 는 텍스처의 **마지막 행**을 화면 위로 보낸다. 두 규칙이 맞물려 절차적 천체는 북극이 위로 온다.
- **실제 사진은 반대다(북극이 0행).** 그래서 `loadTexture` 가 받는 즉시 `scale(1,-1)` 로 한 번 뒤집어 규칙을 맞춘다. 이걸 빼면 사진을 입힌 13종만 **위아래가 뒤집힌 채로** 그려진다 — 실제로 그 상태였고 "지구가 거꾸로다"라는 보고로 드러났다. 지구·달처럼 지형을 아는 천체가 아니면 눈으로 알아채기 어려우니 주의할 것.
- 새 텍스처를 추가하거나 `drawGlobe` 의 `v` 매핑을 손대면 **지구와 달로 검증할 것.** 지구는 사하라가 있는 북반구가 위, 달 앞면은 어두운 바다가 위쪽·중앙이고 밝은 고지대가 아래(남반구)다. 극지방 밝기는 양쪽 다 희어서 판정에 못 쓴다.

**구형이 아닌 천체가 있다.** 자체 중력으로 둥글어질 만큼 크지 않은 천체는 `SHAPE` 테이블에 실측 삼축 치수에서 뽑은 축 비(`ax`)와 표면 요철 세기(`lump`)를 둔다. 하우메아는 자전이 빨라 늘어난 것이라 매끄럽고(lump 0.02), 히페리온·포보스 같은 것은 울퉁불퉁하다. 방향별 반지름은 `shapeMap()`이 맵으로 한 번 구워 두고 이중선형으로 샘플링한다 — 픽셀마다 노이즈를 돌리면 회전 중 프레임이 무거워진다. 궤도 화면(`drawBody`)도 같은 맵을 쓴다.

- **매끄러운 노이즈만 쓰면 표면이 물결친다.** 예전에는 낮은 주파수 fbm 한 겹이 전부였는데, 바다 물결처럼 굽이쳐 돌덩이가 아니라 녹아내린 것처럼 보였다(사용자 보고). 실제 소천체의 표면을 만드는 것은 거의 전부 **충돌 크레이터**다. 지금은 ① 완만한 저주파로 전체 윤곽만 잡고 ② 그 위에 크레이터를 크기 멱법칙으로 70~140개 찍는다(`stampCrater`). 크레이터는 포물선 그릇 + 가장자리에 솟은 테두리다.
- **윤곽을 깎는 깊이와 음영의 세기를 따로 둔다**(`GEO` / `GK`). 변위를 그대로 쓰면 파인 곳에서 `r2 > Rd²` 가 되어 픽셀이 버려지는데, 그 자리가 배경이 비치는 **검은 틈**이 된다 — 히페리온에서 몸통 안쪽의 7.7% 가 그랬다. `GEO`(0.40)로 얕게만 깎고 생김새는 법선 교란이 맡으면 0.9% 로 떨어진다. 어차피 부풀어 오른 쪽은 `r2>1` 에서 이미 잘리므로 변위를 키워도 얻는 것이 없다.
- **법선 교란은 잘라내지 말고 부드럽게 포화시킨다.** 기본 법선을 넘어서면 뒤집혀 검은 얼룩이 되고, 그렇다고 하드 클램프하면 넘치는 구간이 전부 같은 값이 되어 판판해진다. `g *= GMAX/sqrt(g²+GMAX²)` — 작은 기울기는 그대로 지나간다.
- **크레이터는 걸치는 경도 폭까지 계산해 훑는다.** 위도만 좁히고 경도는 전 열을 돌면 천체당 13~18ms 다 — 프레임 하나가 통째로 날아간다. 구면 코사인 법칙으로 폭을 구하면 8~10ms 이고 결과는 **완전히 동일하다**(전 셀 순회와 비교해 최대차 0).
- **한 프레임에 한 장만 굽는다.** 요철 천체를 여럿 배치하면 첫 프레임에 전부 몰려 80ms 가 날아간다. `shapeMap()`이 12ms 안에 이미 구웠으면 `null` 을 돌려주고, `drawGlobe` 는 그 프레임만 `lump=0` 으로 매끈하게 그린다. 맵이 늦게 오면 **`invalidateSprites()`로 썸네일을 다시 굽는다** — 안 그러면 매끈한 스프라이트가 캐시에 굳는다.
- 세기를 조정할 때 확인할 것: 히페리온(lump 0.22)은 스펀지처럼 깊게 파여야 하고, 하우메아(0.02)는 매끈해야 한다. 몸통 안쪽에 배경이 비치는 틈이 없어야 한다(행 단위로 좌우 끝 사이의 투명 픽셀을 세면 잡힌다).

**대기 산란은 대기가 있는 천체에만.** `drawGlobe`의 `opt.atmos`가 가장자리 빛번짐을 넣는데, 대기 없는 암석에 주면 가스에 싸인 것처럼 보인다. `f.atmos`/`f.ocean`/`f.glow`가 있을 때만 켠다. 요철 천체의 음영은 밝기 배율이 아니라 형상 맵의 기울기로 **법선을 틀어** 계산한다 — 밝기만 조절하면 물렁해 보인다.

**항성은 자체 발광이다.** `opt.emis`가 켜지면 램버트 대신 주연감광(`1-0.62*(1-mu)`)을 쓴다. 표면은 `starSurface()`가 쌀알무늬와 흑점으로 만든다. **흑점 양은 질량이 아니라 색온도로 정한다** — 질량으로 하면 무겁고 차가운 적색초거성을 놓친다. 코로나는 `drawStar`로 먼저 깔고 그 위에 구체를 얹는다. 블랙홀과 중성자별은 표면이랄 것이 없어 기존 연출을 유지한다.

**고리에는 행성 그림자가 필요하다.** 없으면 종이를 오려 붙인 것처럼 평평해 보인다. 고리 영역으로 클립한 뒤 별 반대 방향으로 어두운 그라디언트를 깐다. 텍스처의 푸른 기운도 덜어낸다 — 실제 고리 입자는 얼음과 암석이라 따뜻한 회백색이다.

**고리는 도감·궤도·썸네일이 같은 원반을 쓴다.** `ringDisc(id)`가 실제 텍스처를 위에서 내려다본 원반으로 한 번 구워 캐시한다. **앞뒤 반쪽의 밝기를 다르게 하면 안 된다** — 가로로 이음매가 생긴다. 반쪽으로 자르는 것은 가림 순서 때문이며 밝기는 같아야 한다. 원본 텍스처는 셰이더로 밝히라고 만든 어두운 값이라 `buildRingDisc`에서 1.95배 올린다. 납작한 정도는 궤도 화면에서는 카메라 기울기를 따르고(고리는 궤도면에 있다), 썸네일은 고정 각도를 쓴다.

**자전축 기울기는 `ax`(도)에 실측값으로 둔다.** 그리는 쪽에서는 캔버스를 그만큼 돌리고 **광원 각도를 같은 만큼 되돌린다** — 안 그러면 조명이 함께 돌아가 별이 엉뚱한 쪽에 있는 것처럼 보인다. 고리도 같이 돌려야 한다. 천왕성(97.8도)이 누워서 돌고 고리가 수직으로 서는 것이 이 처리의 확인 지점이다.

**텍스처를 얹을 항성은 본체 원반을 그리지 않는다**(`extra.noBody`). `drawStar`의 본체 그라디언트는 중심이 왼쪽 위로 치우쳐 있어 가장자리 색이 방향마다 다르다. 텍스처가 안티에일리어싱으로 가장자리 한 픽셀을 반투명하게 남기면 그 틈으로 아래 원반이 **한쪽만** 밝은 호로 비친다. 코로나만 깔고 그 위에 표면을 얹을 것. 텍스처가 아직 없으면 예전대로 본체를 그려 빈 구멍을 막는다.

**`drawGlobe`는 캔버스 안쪽 2px을 비우고 구를 그린다.** 그대로 확대해 얹으면 가장자리에 아래 그림이 링으로 남는다 — 반지름 10px이면 20%가 링이다. 합성할 때 반드시 `globeFit(need)`를 곱할 것.

**꼬리 대신 궤도선.** 예전에는 행성·위성 뒤에 짧은 꼬리를 그렸는데 궤도가 어디인지 읽히지 않았다. 위성도 행성과 같은 `strokeOrbit` 실선을 쓰되 더 옅게(0.13) 깐다.

**배경은 순수한 검정이다.** 사진(ESO 은하수)과 절차적 성운·반짝이 별을 모두 시도했다가 걷어냈다 — 항성계가 주인공인데 배경이 시선을 나눠 가져간다. `render()` 첫 줄의 `fillRect` 한 번(`BG` = `#04050b`)이 전부다. 예전에는 화면 크기 배경 캔버스를 따로 들고 매 프레임 얹었는데, 단색 한 겹에 DPR 3 기준 57MB 를 쓰는 일이라 걷어냈다. 불투명이라 `clearRect` 도 필요 없다.

**궤도 화면에서 행성·위성 주변에는 빛을 그리지 않는다.** 표면 렌더링만으로 충분하고, 헤일로가 있으면 지저분해진다. 중심별과 동반성의 코로나는 유지한다.

**중심별은 다른 천체의 절반 크기다**(`g.R*VIEW.zoom*.035`). 스킨이 없으면 `centralStarBody()`가 고른 색으로 가상 천체를 만들어 표면 텍스처를 굽는다 — id 에 색이 들어 있어 색을 바꾸면 자동으로 다시 구워진다.

**실제 사진은 도감과 무관하게 불러와야 한다.** 예전에는 `showBodyDetail`에서만 `loadTexture`를 불러, 도감에서 그 천체를 열기 전까지 궤도 화면이 절차적 그림으로 남았다(천왕성이 못생겨 보이던 원인). `readyTexture()`가 `TEX_IDS` 소속이면 즉시 로드를 시작하고 도착 전까지만 절차적 그림을 쓴다. **새 렌더 경로를 만들 때 이 함수를 거치는지 확인할 것.**

**동반성도 중심별과 같은 표면을 입힌다.** 예전에는 `drawStar`만 불러 뭉갠 얼룩으로 보였다(시리우스가 못생겨 보이던 원인). 블랙홀·펄서는 표면이 없으므로 제외한다.

**고리 텍스처는 `drawRing`이 직접 지연 로드한다.** 예전에는 도감 구체 뷰어에서만 불러서, 도감을 한 번도 열지 않으면 궤도 화면의 토성 고리가 대체 그림으로 남았다. 로드가 끝나면 `_ringDiscs` 캐시를 비워 다시 굽는다.

**중심별과 동반성은 같은 크기다.** 반지름 상한을 낮게 잡으면 확대해도 커지지 않으니 주의(예전에 32px 상한 때문에 블랙홀이 고정 크기로 보였다). 블랙홀은 원반이 지평선의 4.6배까지 뻗으므로 그림자를 넘겨받은 반지름의 30%로 잡아야 전체 크기가 다른 별과 비슷해진다.

**블랙홀은 인터스텔라의 가르강튀아 형태다.** 얇은 강착원반이 그림자 앞뒤를 지나고, 렌즈된 후광이 그림자를 감싸며, 광자 고리와 도플러 밝기차가 있다. 원반 각도는 카메라에 반응하되 **0.38을 넘지 않게 눌러** 늘 원반다움을 유지한다 — 완전히 열리면 밝은 덩어리가 되어 블랙홀로 안 보인다.

**자전축과 강착원반은 카메라를 따라야 한다.** 자전축은 궤도면에 수직이므로 관측자 위도를 `π/2 - VIEW.tilt`로 넘긴다 — 위에서 보면 극이, 옆에서 보면 적도가 보여야 한다. 화면 기준으로 고정하면 시점을 바꿔도 늘 옆으로만 도는 것처럼 보인다. 블랙홀 강착원반도 같은 이유로 `extra.diskSquash`에 `cos(VIEW.tilt)`를 넘겨 face-on에서 원, edge-on에서 선이 되게 한다.

**궤도 화면은 매 프레임 실시간으로 그린다.** 미리 구운 프레임을 골라 쓰면 자전이 뚝뚝 끊긴다(12장이면 30도씩 튄다). 천체 크기가 작아 실시간 렌더가 오히려 싸다 — 기본 배율에서 프레임당 1.5ms, 3.5배 확대에서 10.7ms. 크기별 스크래치 캔버스(`scratchFor`)와 ImageData(`globeImageData`)를 재사용하는 것이 핵심이다. **매 프레임 `createImageData`를 부르면 7배 느려진다.** 텍스처가 아직 없으면 `readyTexture()`가 굽기를 예약하고 그동안 기존 절차적 그림으로 대체한다. 광원 방향은 `opt.light`로 넘겨 별 쪽을 비추게 한다.

**썸네일은 여전히 스프라이트를 쓴다.** 매 프레임 구면 매핑을 돌리면 무거우므로 천체마다 한 번 구워(`bodySprite`) 재사용한다. 스프라이트의 광원은 고정이고, 그릴 때 `lightAng-SPRITE_LIGHT`만큼 회전시켜 밝은 면이 별을 향하게 한다 — 구는 회전 대칭이라 이렇게 해도 조명이 맞는다. 자전 속도는 위성이 1(조석 고정), 행성이 6이다 — 실제 자전 주기를 그대로 쓰면 어지럽다. 굽는 작업은 `pumpSprites()`가 한 프레임에 하나씩 처리하고, 준비 전에는 기준 프레임이나 기존 절차적 그림으로 대체한다. 실제 텍스처가 뒤늦게 도착하면 `invalidateSprites()`로 다시 굽는다.

**도감에서는 천체 주변의 빛을 그리지 않는다.** 코로나와 대기 글로우가 정사각형 캔버스 밖으로 넘쳐 네모로 잘려 보이기 때문이다. 궤도 화면은 배경이 넓어 문제가 없으므로 그대로 둔다. 표면이 없는 블랙홀·펄서만 자기 연출을 쓰되 `thumbFitRadius()`로 전체가 캔버스 안에 들어오게 반지름을 줄인다 — 펄서 빔은 반지름의 7배까지 뻗으므로 그냥 그리면 반드시 잘린다.

**썸네일과 중심별 미리보기도 같은 스프라이트를 쓴다.** `paintThumb`은 준비 전에는 간단한 그림을 그리고 `onSpriteReady()`로 다시 그린다. 중심별에 스킨을 씌우면 `starLook().skinBody`로 그 항성의 실제 표면까지 입힌다 — 색만 바꾸면 스킨을 씌운 티가 나지 않는다.

**중심별은 색 또는 스킨이다.** `S.star.skin`이 항성 id면 그 항성의 색·연출·질량을 그대로 따르고, 없으면 `S.star.color`를 쓰며 질량은 `massFromColor()`가 색조에서 추정한다. 렌더링·질량중심·미리보기는 모두 `starLook()`을 거쳐야 한다 — `S.star.color`를 직접 읽으면 스킨이 무시된다.

## PWA와 알림

**`navigator.serviceWorker.ready` 를 조건식에 쓰지 말 것.** 이건 Promise 라 **항상 참**이다. `if(navigator.serviceWorker.ready)` 로 갈래를 타면 서비스워커가 등록되지 않은 환경에서 그 약속이 영영 안 풀려 **알림이 오류도 없이 사라진다.** 실제로 그 버그로 알림이 통째로 죽어 있었다. `swReg()`가 1.5초 제한을 걸고, 실패하면 페이지 알림으로 떨어진다.

**설정에 알림 자가진단이 있다.** `지금 알림 테스트`(알림 자체가 되는지)와 `10초 뒤 알림`(백그라운드에서 오는지)이 나뉘어 있다. "알림이 안 온다"는 보고를 받으면 이 둘 중 어디서 막히는지부터 가르게 할 것. 진단줄의 `SW 활성/없음/등록실패` 도 같이 본다.

**알림은 반드시 `showNotif()`를 거친다.** 홈 화면에 추가한 PWA, 특히 iOS 에서는 페이지의 `new Notification()`이 동작하지 않고 **서비스워커 등록본의 `showNotification()`만** 통한다. `showNotif()`가 서비스워커를 먼저 시도하고 실패하면 페이지 알림으로 떨어진다.

**화면이 꺼지면 rAF 가 멈춘다.** 완료 판정이 렌더 루프에 있으므로 탭이 뒤로 가면 알림이 제때 안 뜬다. `armNotify()`가 세션 시작 시 `endAt`에 맞춰 `setTimeout`을 걸어 둔다. 시작·정지·초기화·부팅(진행 중 세션 복구)에서 짝을 맞출 것 — `disarmNotify()`가 반대다.

**웹에서는 앱을 완전히 종료하면 알림이 오지 않는다.** 예약 알림을 보내려면 푸시 서버(Web Push + VAPID)가 필요한데 이 앱에는 없다. 설정 화면 `알림과 설치` 옆 ⓘ(`INFO.notify`)에 이 한계를 명시해 두었으니 지우지 말 것 — "알림이 안 온다"는 문의의 대부분이 이 경우다.

**안드로이드 래퍼에서는 이 한계가 없다.** `mobile/`의 Capacitor 앱은 `@capacitor/local-notifications`로 **OS에 알림을 예약**하므로 앱을 종료해도 뜬다(`allowWhileIdle`로 도즈 모드까지 관통). `nativeArm()`/`nativeDisarm()`이 그 경로이고, `isNative()`가 거짓인 브라우저에서는 통째로 꺼져 기존 웹 경로만 돈다. **두 경로가 동시에 알림을 띄우지 않도록** 웹 타이머는 `isNative()`면 스스로 빠지고, 렌더 루프가 완료를 처리하면 `nativeDisarm()`으로 예약을 지운다.

Capacitor 플러그인은 번들러 없이 `window.Capacitor.Plugins.<이름>`으로 부른다 — 이 프로젝트에 빌드 단계를 들이지 않기 위한 선택이다. `mobile/sync-web.mjs`가 루트에서 웹 자산을 긁어가며 **서비스워커 등록을 끈다**(네이티브 WebView 는 로컬 파일을 읽으므로 캐시가 불필요하고, sw 가 끼면 앱을 새로 깔아도 낡은 화면이 남는다).

**서비스워커 갱신 규칙이 자산마다 다르다.** `index.html`은 네트워크 우선(캐시 우선이면 배포해도 낡은 앱을 계속 쓴다), 텍스처·아이콘은 캐시 우선. 캐시 이름에 `VER`이 들어 있어 올리면 옛 캐시가 지워진다. **`sw.js`를 고치면 `VER`을 올릴 것.**

`manifest.webmanifest`의 `start_url`/`scope`/아이콘 경로는 모두 **상대 경로**다. GitHub Pages 가 `/stellar-focus/` 하위에 배포하므로 절대 경로로 바꾸면 설치가 깨진다.

서비스워커는 `file://`과 일부 내장 브라우저에서 등록되지 않는다. 그건 정상이고 앱은 그대로 동작해야 한다.

## 카이퍼 벨트

`kbelt()`가 소천체 2800개를 **도넛(토러스) 단면**에 결정론적으로 뿌린다(37~49 AU). 관 각도 `th`와 관 반지름 `rt`를 뽑아 궤도 반지름과 **궤도 경사**를 정하는데, 경사가 곧 관의 세로 두께다. `rt`에 `sqrt`를 쓰는 이유는 원 면적이 반지름 제곱에 비례해서다 — 균등하게 뽑으면 중심축만 빽빽해진다. 이심률을 눌러(0.004~0.03) 띠가 뭉개지지 않게 했다.

**마지막 행성과의 틈은 장반경 기준 0.24 다**(안쪽 가장자리 1.24). 중심을 1.31 로 두던 때는 0.12 였는데, 궤도가 타원이라 **원일점까지 보면 0.034 밖에 안 남아 거의 붙어 있었다.** 두 배로 벌린 값이고, 37~49 AU 가 되어 실제 카이퍼 벨트의 밀집 구간(42~48 AU)과도 더 맞는다. 밀어낼 때는 `fitZoom()` 이 `KB_OUT` 까지 담는지 확인할 것 — 하한 0.5 에 걸리면 벨트가 화면 밖으로 나간다(현재 가장 좁은 화면에서 0.549 로 여유가 있다).

**띠를 넓히면 같은 개수로는 오히려 성글어진다.** 화면에 비치는 고리의 면적이 대략 `KB_MID × 2·KB_TUBE` 에 비례한다. 두께를 0.105 → 0.19 로 넓히면 면적이 1.82배가 되므로, 밀도를 1.34배로 올리려면 1150 → 2800 개가 필요하다. 더 키우고 싶은 유혹이 있지만 비용이 가파르다 — 3500개·두께 0.24 는 벨트 비용을 3배로 만든다(DPR 3·14.4M 픽셀에서 5.2ms → 14.9ms). 2800·0.19 는 DPR 2 에서 vsync 안에 들어온다. 생성은 7ms 한 번뿐이라 부팅 프레임을 떨어뜨리지 않는다.

벨트는 **배치된 가장 바깥 궤도**(`beltRef()`)를 기준으로 깔리므로 행성이 늘면 함께 밀려난다. `fitZoom()`도 `KB_OUT`까지 포함해 화면을 맞춘다 — 안 그러면 레이더 표적이 화면 밖으로 나간다.

- **점을 원으로 그리지 말 것.** 예전에 어두운 원 안에 밝은 원을 겹쳐 명암을 냈는데 유리구슬 하이라이트가 되어 **비눗방울처럼** 보였다. 소천체마다 고정된 불규칙 꼭짓점 일곱 개(`shape`)를 갖는다. 1px 미만이면 윤곽이 안 보이므로 그때만 원으로 그린다.
- **모서리는 곡선으로 잇는다.** 직선으로 이으면 각진 결정처럼 보여 실제 소천체 같지 않다(사용자가 각진 형태를 빼 달라고 했다). 이웃한 두 꼭짓점의 중점에서 시작해 꼭짓점 자체를 제어점으로 삼는 이차 곡선으로 잇는다 — 곡선이 꼭짓점을 지나지 않으므로 모서리가 깎이면서도 개체별 울퉁불퉁함은 남는다. 더 둥글게(요철 절반)나 타원도 만들어 비교했는데, 전부 비슷한 공처럼 보여 비눗방울 쪽으로 되돌아간다. 곡선 비용은 25.4M 픽셀에서 직선 대비 +1.8ms, 실제 DPR 2 에서는 0.5ms 미만이다.
- **꼭짓점 배열을 매번 새로 만들지 말 것.** `kbShape`가 한 프레임에 2800 번 불린다. 스크래치 버퍼(`_kbSX`/`_kbSY`)를 돌려 쓴다.
- **큰 것만 자갈처럼 그린다.** 한 겹으로 칠하면 종이를 오려 붙인 것처럼 납작하다(사용자 보고). 문턱(`rad*1.9`)을 그 프레임의 기준 반지름에 비례시키면 배율·화면 크기와 무관하게 늘 상위 8% 안팎, 80 개 남짓만 걸린다. 그것만 개체별 방사 그라디언트(중심을 빛 쪽으로 밀어 부드러운 종결선을 만든다)로 칠하고 나머지는 예전 그대로 타일 경로 한 겹이다. DPR 2·6.4M 픽셀에서 벨트 전체가 0.4ms 다.
  - **문턱은 개수로 묶는다.** 크기 2.0px 로 고정했더니 큰 화면에서 383 개가 걸려 프레임이 멈췄고(화면이 커지면 `g.R` 이 커져 모두가 커진다), 기준 반지름에 비례시켰더니 소천체 수를 늘릴 때 같이 늘어났다. 지금은 크기 분포(`o.sz`) 히스토그램을 큰 쪽부터 훑어 `KB_CAP`(90)을 넘지 않는 문턱을 찾는다 — 배율·화면 크기·소천체 수와 무관하게 늘 90 개 안팎이다.
  - 전부 그라디언트로 칠하거나 스프라이트로 바꾸는 것은 **둘 다 실패한다** — 16px 스프라이트 1150 장이 49ms(경로는 17ms)다. 텍스처 축소 필터링 때문이고, 8px 로 줄여도 본전이다. 다각형을 3단으로 겹치는 방법은 싸지만 확대하면 등고선처럼 층이 진다.
- **밝기를 알파가 아니라 색에 녹인다.** 예전에는 `globalAlpha` 로 밝기를 냈다. 검은 배경 위에서 결과 픽셀은 같지만 인상이 다르다 — 반투명한 그림처럼 배경에 스며들어 알갱이의 윤곽이 녹는다("모래알이 아니라 반투명한 그림 같다"는 보고). 불투명하게 칠하면 같은 밝기라도 단단한 알갱이로 읽히고, 알파 합성이 없어져 **더 싸다**(14.3M 픽셀에서 벨트 비용 4.1ms).
  - `KB_FLOOR`(0.52)는 가장 어두운 위상의 알갱이도 배경에서 뜨게 하는 바닥값, `KB_LIFT`(0.35)는 그라디언트의 그늘 끝을 중간색 쪽으로 끌어올려 **가장자리가 배경에 녹지 않게** 한다. `KB_GAIN`(0.80)이 전체 밝기 손잡이인데 **1.25 를 넘기면 얼음질의 밝은 면이 흰색으로 뭉갠다.**
  - **확대했을 때가 진짜 시험대다.** 기본 배율에서는 무엇을 해도 고운 점으로 보이지만, 2 배만 확대해도 알갱이가 콩알만 해진다. 굵고 허옇던 시절의 보고가 그것이었다. `KB_SIZE`(0.65)로 굵기를 줄이고 `KB_COOL`(0.35)로 얼음질의 흰 하이라이트를 암석 쪽으로 눌렀다. **`KB_SIZE` 를 더 낮추지 말 것** — 그라디언트 문턱(2.0px)을 넘는 개체가 줄어 평평해진다.
  - 재질·밝기 버킷이 고정이라 색표(`_kbFlat`/`_kbGrad`)는 모듈 로드 때 한 번만 만든다. 매 프레임 만들지 말 것.
- 자갈 색(`KB_PEB`)은 **중간 색을 축으로 양끝을 벌린 값**이다. 대비를 더 올리려면 그 비율만 키우면 되는데, 1.26 배를 넘기면 그늘이 거의 검정이 되어 윤곽이 배경에 묻힌다. 지금은 1.18 배다.
- 입체감은 **개체별 밝기**로도 낸다. 카메라 방향 `V`(= `project()`의 depth 방향)와 별 방향의 사이각으로 보이는 밝은 면의 비율을 구한다 — 별 너머는 보름달, 이쪽 편은 초승달이다.
- **칠하기는 경로의 경계상자만큼 값을 치른다.** 이게 이 화면에서 가장 비싼 한 가지였다. 1150 개를 한 경로에 몰면 그 상자가 화면 전체가 되어, 버킷 여덟 번이 곧 화면을 여덟 번 훑는 일이 된다. 태블릿 크기(1600×1000 · DPR 3 · 14.4M 픽셀)에서 프레임의 **4.6ms**를 이것만으로 먹었다. 화면을 `KB_TILE`(4×4)로 나눠 담으면 상자가 1/16 로 줄어 같은 그림이 훨씬 싸다 — 22.5ms → 17.9ms. 더 잘게 쪼개도 나아지지 않는다(8×8·16×16 모두 같은 값).
  - **원인을 개수·모양·수학에서 찾지 말 것.** 전부 재봤고 각각 1.8ms · 0.8ms · 0.3ms 였다. 개수를 절반으로 줄여도 1.8ms 밖에 안 빠진다 — 상자가 그대로이기 때문이다. 스프라이트 `drawImage` 1150 번도 경로와 같은 값이 나온다(축소 필터링).
  - 겹친 소천체가 다른 타일로 갈리면 알파가 두 번 곱해져 아주 조금 밝아진다. 재어 보니 어긋난 픽셀 88,853 개가 전부 1~4/255 였고 5 이상은 6 픽셀뿐이다. 반올림 수준이라 그대로 둔다.
- 1150개에 그라디언트를 쓰면 프레임이 무너진다. **[재질][밝기] 버킷으로 묶어 여덟 번의 칠하기**로 끝낸다. 크기는 멱법칙(`0.30+2.0·r^2.6`)이라 대부분 작은 소행성이고 왜소행성급은 드물다.

## 집중 연출

`S.intro`(`none`/`scope`/`comet`/`transit`/`probe`)로 고른다. 네 연출 모두 **남은 시간을 화면으로 읽을 수 있게** 만들어져 있다 — 표적 수, 혜성까지의 거리, 광도 곡선의 진행, 탐사선의 항해 거리.

- **혜성 근일점(`comet`)** — 이심률 0.86 타원. 케플러 시간을 그대로 쓰면 원일점 근처에서 아주 느려 세션의 3분의 2를 화면 밖에서 보낸다. 그래서 **거리를 진행도에 비례해 줄이고 그 거리의 진근점이각을 궤도 방정식에서 거꾸로 구한다** — 경로는 정확한 타원, 다가오는 속도는 타이머에 비례, 끝나는 순간 근일점이다. 꼬리는 둘이다: 이온 꼬리는 곧게 뻗고 매듭이 흐르며, 먼지 꼬리는 **지나온 궤도 위치에서 반태양 방향으로 밀어낸 점들을 이어** 휘어진 부채꼴을 만든다. 근일점은 `beltRef()*0.22` — 안쪽까지 파고들어야 '가장 가까워지는' 순간이 산다. 화면 밖이면 표식도 수치도 그리지 않는다.
- **선명하게 남길 영역은 원으로 오린다.** `applyScopeOptics`가 `{x,y,r}`을 받으면 원, `{x,y,w,h}`면 사각형으로 자른다. 천체를 남길 때 사각형으로 자르면 창문처럼 보여 어색하다 — 브래킷 창(벨트 탐사)만 사각형이 맞다.
- **캔버스에 패널을 놓을 때 HUD 좌표를 읽을 것.** HUD 는 DOM 이라 캔버스 위에 뜬다. 광도 곡선을 고정 좌표에 뒀다가 좌상단 날짜 위젯에 가려진 적이 있다. `$('#clock').getBoundingClientRect().bottom` 을 읽어 그 아래에 놓는다.
- **엄폐(`transit`)** — **궤도면을 옆에서 봐야 보인다.** 이 모드만 `TILT_EDGE`로 잠그되, `tiltTo` 만 바꾸면 지수 보간 때문에 처음 1초가 비스듬하다. **`tilt` 도 함께 즉시 맞출 것.** 통과 전부터 밝기가 완전히 회복될 때까지 이어지고, 광도 곡선은 **관측된 구간만** 그린다. 깊이 → 반지름비, 통과 시간 → 궤도 거리와 주기, 끝에 절대 광도와 거리를 차례로 뽑는다. 중심별과 미지의 천체만 선명하게 남기고 모눈은 초록이다.
- **탐사선(`probe`)** — **왕복** 항해다. 발사 → 중력도움 1 → 목적지 도달 → 탐사 → 중력도움 2 → 귀환. **갈 때도 올 때도 행성 곁을 스치며**, 행성이 그 사이 계속 공전하므로 두 랑데부 지점이 서로 다르다. 목적지는 카이퍼 벨트의 특정 소천체다.

  **시간을 손으로 나누지 말 것.** 예전에는 구간 경계를 상수로 박고(`qA1=0.20` …) 완급 곡선을 따로 먹였는데, 그러면 화면에서 움직이는 속도와 옆에 찍히는 km/s 가 **서로 다른 것을 가리킨다** — 숫자는 11 km/s 로 제일 빠른데 화면에서는 제일 느리게 기어갔다. 완급 곡선을 지수로 만들려는 시도도 실패한다: 1 보다 크면 구간 시작에서 멈추고, 1 보다 작으면 변화율이 무한대라 경계에서 53px 씩 튄다.

  지금은 순서를 뒤집었다. ① 경로의 모양을 정하고(극좌표 선형 보간, 완급 없음) ② 각 점의 속력을 **비스-비바 식**으로 정한 뒤 ③ 시간을 `dt = ds/v` 로 적분해 얻는다(`pbMeasure`). 빠른 곳은 적은 시간에 지나므로 `화면이동 ÷ 진행도 = ds/dt = v` — 화면과 수치가 정확히 비례한다(집중 시점이 face-on 으로 잠겨 있어 화면 거리가 실제 거리에 비례하는 것이 전제다). **구간 길이도 물리가 정한다.** `PB.gamma` 를 1 미만으로 낮추면 완급이 완만해지지만 비례는 깨진다.

  **스윙바이는 구간마다 장반경(`pbSemi`)이 달라지는 것으로 표현된다** — 실제로 중력도움이 하는 일이 궤도 에너지를 행성에서 빌리는 것이다. 그래서 별도의 가속 연출 없이 1번 뒤에 저절로 빨라지고 2번 뒤에 저절로 느려진다. 장거리 순항 두 구간은 원일점을 목적지 너머(`aphK`)에 두어 도착·출발이 기어가지 않게 한다.

  **랑데부는 순환이라 되풀이해 수렴시킨다.** 지점이 도착 시각에 달렸는데 그 시각을 구하려면 다시 지점이 필요하다. **곧바로 새 몫으로 갈아타면 안 된다** — 중력도움 행성은 한 세션에 여러 바퀴를 돌아 경계가 7% 만 움직여도 랑데부가 70도 넘게 튀고, 두 값 사이를 오가며 영영 수렴하지 않는다. 절반씩 옮겨 눌러 앉힌다(`pbTable`, 최대 20회, 세션당 1~6ms). **검증법**: 모든 구간에서 `화면이동(px) ÷ 표시속력(km/s)` 이 같아야 하고(현재 구간 간 3.5% 이내), 경계에서 탐사선과 행성의 화면 거리가 2px 이내여야 한다.

  탐사 구간만은 물리가 아니라 연출이 시간을 정한다(`PB.survey`) — '멈춰 서서 관측하는' 대목이라 거리로 나누면 0 으로 붕괴한다. 그동안 탐사선은 목적지에 붙어 함께 돌고, 표시 속력은 그 천체의 공전 속도다.

  **큰 원이나 브래킷으로 감싸면 실제보다 훨씬 커 보이므로** 목적지는 회색 점 하나에 십자 표식만 얹는다. 탐사 중에는 탐사선이 목적지에 붙어 있어 두 수치 상자가 겹치므로 하나로 합친다. 중력도움 천체는 **사용자가 실제로 배치한 행성**이라 공전한다 — 지금 자리가 아니라 **도착할 자리(랑데부)를 겨냥**한다.

**`fx-preview.html`은 연출 실험장이다.** 본편에 넣기 전에 진행도를 슬라이더로 밀어 보며 판단하는 용도로 만들었다. 새 연출을 검토할 때 여기서 먼저 세우면 본편을 건드리지 않고 돌려볼 수 있다. **시작할 때 잠깐이 아니라 세션이 흐르는 내내 지속된다.** 별도 루프를 돌리지 않고 `render()` 맨 끝에 얹는다 — 연출용 rAF를 따로 돌리면 기존 루프와 겹쳐 프레임이 어긋난다.

**벨트 탐사(`scope`)** — 카이퍼 벨트의 소천체를 사진 프레임(네 모서리 브래킷)으로 포착하고 황경·황위를 띄운다. **남은 표적 수가 곧 남은 시간이다**: 세션마다 `rollRadarMax()`가 7~13개를 뽑고 진행도에 따라 하나씩 놓아 끝에는 하나만 남는다. 씨앗이 `S.timer.endAt`이라 같은 세션에서는 새로고침해도 같은 값이 나온다.

- 표적은 **초기 황경 순으로 정렬한 후보**(`_kb.pick`)에서 균등 간격으로 뽑는다. 그냥 인덱스로 고르면 화면 한쪽에 뭉쳐 프레임과 좌표가 겹쳐 읽을 수 없다.
- 브래킷은 **맥동하지 않는다**. 크기·밝기가 고정이어야 '포착'으로 읽힌다. 마지막까지 남는 표적일수록 또렷한 것은 고정값이다.
- 좌표 라벨은 오른쪽에 자리가 없으면 왼쪽에 붙인다 — 안 그러면 폰에서 잘린다.
- 상태 문구는 `drawScopeReadout()`이 **Face-on 버튼 좌표를 읽어** 그 위에 오른쪽 맞춤으로 놓는다. HUD 위치가 화면 폭에 따라 달라지기 때문이다.

**그리는 순서가 곧 선명도다.** `applyScopeOptics`가 그때까지의 화면을 통째로 흐리게 만들므로, **선명해야 하는 것은 반드시 그 뒤에 그린다** — 브래킷·수치·상태 메시지·모눈·스캔라인, 그리고 항성 테두리(`_starRim`)가 그렇다. 항성 테두리를 천체와 함께 그렸다가 같이 뭉개진 적이 있어, 좌표만 기억해 두고 뒤에서 그린다.

**표적 라벨은 겹치면 아무것도 못 읽는다.** 표적이 최대 13개에 5줄씩이라 충돌이 필연이다. `taken` 목록에 이미 놓은 자리를 기억해 두고 오른쪽·왼쪽·위·아래 순으로 빈 곳을 찾으며, 자리가 없으면 식별 부호만 남긴다. 예약 영역은 4px 부풀려 블록끼리 붙지 않게 한다.

**브래킷과 라벨을 잇는 선을 다시 넣지 말 것.** 라벨이 겹침을 피해 자리를 옮기면 그 선이 옆 표적의 라벨을 대각선으로 가로지른다 — 충돌 회피가 라벨 사각형만 예약하고 선 경로는 모르기 때문이다. 라벨은 늘 브래킷 바로 옆에 붙으므로 선 없이도 짝이 분명하다. 굳이 넣으려면 선분과 모든 예약 영역의 교차를 검사하는 2패스가 필요하다.

**거리 척도는 기준 궤도 = 30 AU**(`AU_REF`)다. 그래야 벨트가 35~43 AU 로 실제 카이퍼 벨트(30~50 AU)와 맞는다. `kbSpec(o)`가 표적마다 색·상태 메시지·지름(km)·SNR 을 id 씨앗으로 고정 배정한다 — 같은 천체는 늘 같은 값이 나와야 관측 기록처럼 읽힌다.

**스캔라인·노이즈는 타일을 구워 패턴으로 반복한다.** 매 프레임 픽셀을 돌리면 무겁다. 노이즈는 프레임마다 타일 위치만 밀어 지글거림을 만든다.

**집중 중 항성은 코로나를 줄이고 테두리를 세운다**(`haloK:1.55` + `strokeStarRim`). 대기·휴식·촬영 중에는 기존의 부드러운 코로나를 유지한다 — 사용자가 촬영은 예전 모습이 낫다고 했다.

**관측 장비 화면(`applyScopeOptics`)** — 브래킷 안쪽만 선명하고 나머지는 흐리며 그 위에 모눈이 깔린다. 캔버스에는 영역별 흐림이 없으므로 **지금까지 그린 장면을 통째로 복사**해 두고, 흐리게 한 번 깔고 표적 사각형으로 클립해 선명한 원본을 되살린다. 브래킷과 좌표는 이 처리 **뒤에** 그려야 선명하다. 흐림은 GPU 가속이라 274만 픽셀(DPR 3)에서도 0.18ms다.

시작 직후 `APPROACH_MS`(2.2초) 동안 `stepStartFx()`가 시점을 직접 몬다. 기본 지수 보간에 맡기면 0.5초 만에 붙어 다가가는 느낌이 사라진다. `frame()`의 보간 직후, `render()` 앞에서 불러야 한다. 타이머는 연출과 무관하게 이미 시작돼 있다(절대 시각 기준).

## 남은 작업

사용자가 요청했으나 아직 안 한 것들.

- 가챠 배너에 **실제 성운/은하 배경** (말머리성운, 게성운, 안드로메다, 은하수)
- **안드로이드 APK 빌드.** `mobile/` 의 코드는 준비돼 있고 툴체인(약 2.2GB) 설치만 남았다. 사용자가 결정할 사항.
- **처음 쓰는 사람을 어디로 안내할지** 다시 정하기로 했다. 기존 문구 셋은 뺐고 `showHint()` 틀만 남아 있다(위의 항목 참고).
- **뒤로가기가 일부 환경에서 안 먹는다.** 설정 맨 아래 진단줄을 받아야 원인을 가릴 수 있다(위의 뒤로가기 항목 참고).

곁딸린 문서 셋이 코드보다 뒤처져 있다. 손댈 일이 있으면 함께 고칠 것.

**`README.md` — 코드와 대조해 확인한 오류 일곱.** 값은 이미 확인해 두었으니 그대로 갈아 끼우면 된다.

| 줄 | 지금 적힌 것 | 실제 |
|---|---|---|
| 21 | 카운트다운/포모도로를 **체크박스**로 전환 | `<button class="tTab">` — **탭** |
| 24 | 집중 중 모든 천체가 **검은 구체**, 중심별도 어둡게 꺼짐 | `FOCUS_LIT`=0.62 — 색·표면이 보이되 어둡다. **중심별은 항상 `lit`=1** |
| 39 | 동반성은 **실제 쌍성 역학을 계산하지 않고 시각 연출만** | `starPositions()` 가 질량중심·계층적 삼중성까지 계산 (오차 1e-18) |
| 41 | 수집 총량 항성 12 · 행성 18 · 위성 16 = **46종** | **29 · 30 · 28 = 87종** |
| 48 | 우상단: 토큰 + **가챠 배너 버튼** | 토큰 + 메뉴 **다섯** (가챠 · 항성계 · 도감 · 촬영 · 설정) |
| 50 | 우하단: **항성계 설정 버튼** | **Face-on · Edge-on · 확대축소** |
| 47 | 좌상단: 날짜 / 시각 / 오늘 누적 | 거기에 **계정 버튼**, 그리고 **누르면 집중 기록**이 뜬다 |

맞게 적혀 있는 것: 확률(4/22/74) · 천장(10/30) · 배치 상한(7 · 2 · 2) · 중심별 12색 · 포모도로 기본 주기 4.

**`docs.html` — 틀린 것 둘, 빠진 것 다섯.**

- 틀림: `46종(항성 12 · 행성 18 · 위성 16)` 두 군데(591 · 684 줄), 그리고 `집중 중에는 천체가 검은 구체입니다 / 색 없는 검은 공으로만 보입니다`. 다만 **중심별만은 예외로 항상 밝다는 설명은 맞다.**
- 빠짐: 카이퍼 벨트 · 집중 연출 넷 · PWA/알림 · 촬영 모드 · 절차적 텍스처. 그 뒤에 생긴 **집중 기록**도 없다.
- `fx-preview.html` — 본편이 앞서 나갔다. 여기서 새 연출을 세울 때는 본편의 최신 구현을 먼저 옮겨올 것

이미지 도입은 "단계적으로" 하기로 했다. 도감 텍스처 뷰어를 먼저 만들어 보고, 만족스러우면 궤도 화면까지 WebGL로 확장한다. 실제 사진이 존재하는 것은 태양계 천체뿐이며 태양 외의 항성은 표면 사진이 없다는 점을 전제로 설계할 것.
