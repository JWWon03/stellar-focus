# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

집중/공부 타이머 + 항성계 수집 게임. 세션을 완주하면 가챠 토큰을 얻고, 뽑은 천체를 궤도에 배치해 자기만의 항성계를 키운다. 사용자 대면 규칙 요약은 `README.md` 참고.

## 명령

빌드·번들러·테스트·의존성이 **없다**. `index.html` 하나가 전부다.

```bash
start index.html
```

배포는 GitHub Pages. `main`에 push하면 1분 내로 https://jwwon03.github.io/stellar-focus/ 에 자동 반영된다.

```bash
git add -A && git commit -m "설명" && git push
```

배포본이 로컬과 같은지 확인 (PowerShell):

```powershell
$r = Invoke-WebRequest "https://jwwon03.github.io/stellar-focus/" -UseBasicParsing; $r.RawContentStream.Position = 0; (Get-FileHash -InputStream $r.RawContentStream -Algorithm SHA256).Hash -eq (Get-FileHash "index.html" -Algorithm SHA256).Hash
```

개발 중 상태 초기화는 콘솔에서 `localStorage.removeItem('stellarFocus.v1')` 후 새로고침. 앱 안에서는 항성계 설정 → `전체 초기화`.

전역 `S`(상태)와 `startPhase` / `pull` / `renderSystem` / `renderGacha` 등이 전부 window에 노출돼 있어, 콘솔에서 상태를 심어 특정 화면을 바로 띄울 수 있다. 수동 테스트 시 유용하다.

## 절대 제약: 외부 리소스 0개

CDN 스크립트, 웹폰트, 이미지, 네트워크 요청을 **일절 추가하지 않는다.** 오프라인 동작과 "파일 하나 더블클릭으로 실행"이 이 프로젝트의 핵심 성질이고, 나중에 Capacitor로 모바일 앱 래핑할 때의 전제이기도 하다. 모든 그래픽은 캔버스에 절차적으로 그린다.

## 구조

`index.html`의 `<script>`는 번호가 붙은 12개 섹션으로 나뉜다 (`/* === N. 이름 === */`). 파일이 크므로 수정 전 해당 섹션 주석으로 위치를 잡을 것.

0 유틸 · 1 천체 데이터 풀 · 2 상태/저장 · 3 천체 렌더러 · 4 메인 캔버스 · 5 타이머 · 6 모달 · 7 인트로 · 8 가챠 · 9 항성계 설정 · 10 세션 완료 · 11 위젯 바인딩 · 12 부팅

## 알아야 할 불변조건

여러 곳에 흩어져 있어 한 파일만 읽어선 안 보이는 것들.

**색 헬퍼는 반드시 hex를 반환한다.** `shade` / `lighten` / `mix`의 반환값이 다시 `hx()` / `rgba()`로 들어가기 때문에, `rgb(...)` 문자열을 돌려주면 파싱이 깨져 캔버스가 조용히 잘못 그려진다. 새 색 헬퍼를 만들 때도 `toHex()`를 거칠 것.

**작은 캔버스에 천체를 그릴 때 헤일로가 잘린다.** 항성 코로나는 기본 반경의 6.6배, 발광체 글로우는 2.5배, 고리는 2.55배까지 뻗는다. 캔버스 밖으로 나가면 알파가 0이 아닌 지점에서 잘려 **사각형 테두리**가 보인다. `drawStar`의 `extra.haloK`로 코로나를 줄이고, 본체 반경은 캔버스 절반 나누기 배율 이하로 잡는다. 썸네일은 `paintThumb`, 미리보기는 `paintPreview`에 이미 보정값이 들어 있다.

**궤도 물리.** `phase`는 "기준 궤도의 누적 회전수"다. 기준 궤도 = 행성이 배치된 가장 바깥 슬롯. 집중 세션 동안 기준 궤도가 정확히 1바퀴를 돌고(`phase = _startPhase + 진행률`), 안쪽 행성은 케플러 제3법칙 `revs = phase * (r_ref/r)^1.5`로 더 빠르게 돈다. 대기 중에는 240초에 1바퀴로 천천히 표류. 이 "타이머 시간 = 1바퀴 공전"은 제품의 핵심 컨셉이므로 바꾸지 말 것.

**타이머는 절대 시각(`endAt`) 기준이다.** 남은 시간을 감산하지 않고 매번 `endAt - Date.now()`로 계산한다. 덕분에 탭이 백그라운드로 내려가거나 앱이 절전에 들어가도 어긋나지 않는다. 부팅 시 이미 만료된 세션은 즉시 완료 처리하고 토큰을 지급한다. 이 방식을 감산 방식으로 되돌리지 말 것.

**검은 구체 / 리빌.** 집중 중에는 `isFocusDark()`가 참이라 모든 천체가 검은 구체로, 중심별은 `blaze=0.12` 잉걸불로 그려진다. 완료 시 `reveal` 웨이브가 중심에서 퍼지고 `litAt(orbitR)`이 궤도 반경별로 밝기를 결정한다. 휴식 중에는 밝은 상태다.

**localStorage 스키마.** 키 하나(`stellarFocus.v1`)에 전체 상태를 넣는다. `load()`가 방어적 복구를 한다 — 존재하지 않는 천체 id 제거, 슬롯 배열 길이 보정, 행성 없는 슬롯의 위성 제거. **풀에서 id를 바꾸거나 지우면, 그리고 슬롯 구조를 바꾸면 기존 사용자 데이터가 깨진다.** `load()`의 복구 로직을 함께 갱신하거나 키 버전을 올릴 것.

**반응형 분기점 760px이 CSS와 JS 양쪽에 있다.** `geom()`이 `W < 760`일 때 항성계 중심을 위로(`H*0.32`) 올리고 반경을 줄여 좌하단 타이머 위젯을 피한다. CSS `@media (max-width:760px)`도 같은 값이다. 한쪽만 바꾸면 폰에서 위젯이 중심별을 덮는다.

**HUD 4모서리 배치는 제품 요구사항이다.** 좌상단 날짜/시각, 우상단 토큰+가챠, 좌하단 타이머 위젯, 우하단 항성계 설정. 임의로 옮기지 말 것.

## 밸런스 수치

조정 요청이 들어올 만한 값들. 전부 섹션 1~2의 상수다.

| 항목 | 상수 | 현재 |
|---|---|---|
| 가챠 확률 | `RATE` | ★5 4% / ★4 22% / ★3 74% |
| 천장 | `PITY4`, `PITY5` | 10회 / 30회 (배너별 독립) |
| 배치 상한 | `MAX_PLANET`, `MAX_MOON`, `MAX_COMP` | 7 / 행성당 2 / 2 |
| 수집 총량 | 각 POOL | 항성 12 · 행성 18 · 위성 16 |
| 포모도로 보너스 | `onPhaseDone` | `every`회차마다 토큰 2개 |
| 궤도 기울기 | `SQ` | 0.62 |

가챠는 **중복이 나오지 않는다.** 미보유 풀에서만 뽑고, 해당 등급이 소진되면 상위 등급부터 대체한다(`pull()`). 이건 사용자가 명시적으로 요구한 규칙이다.

동반성은 실제 쌍성 역학을 계산하지 않는다. 중심별 안쪽 고정 궤도를 도는 시각 연출일 뿐이며, 이는 복잡도를 피하려고 의도적으로 내린 결정이다.
